# Path of Immortals

## 新线程启动必读（2026-09-18，置顶规则）

适用于在本项目内开始处理任务的每个新线程，也适用于上下文恢复后缺少项目状态的任务。不要依赖其他线程的记忆；在规划或修改项目之前，主动读取：

1. 本文件 `AGENTS.md`：当前协作约束；下方历史说明有冲突时按较新明确决定处理。
2. `PROJECT_PROGRESS.md`：先读顶部最新记录，再检索并阅读本次任务相关的历史记录，确认已完成、未完成、验证结果和下一步；不能只凭文件名或旧对话推断进度。
3. `docs/project-startup.md`：按文档索引读取本次任务所需的最新设计；原文、建议、确认版和已实现状态必须区分。
4. `README.md`：当前运行入口和操作方式，并核对相关代码／配置与 `git status --short`，保留已有本地修改。

第一次进度说明中简短说明已读到的当前状态和本次范围；真正读完后才可称“已读取”。所需文档缺失或输出截断时继续定位／分段读取，有影响任务的歧义才询问。每次任务结束追加进度记录；新增／替换最新设计时同步维护启动索引。默认不提交或同步Git。

此为项目级启动阅读流程，不是定时任务；不在没有用户任务时自行执行开发。将本节保持在历史记录之前，避免长文件截断隐藏启动规则。

## UI验证偏好（2026-09-19）

用户明确要求：纯视觉、尺寸、间距、颜色、字体、背景和普通布局调整，不运行鼠标命中、悬停计时、点击链路或拖拽类检查。此类任务只做必要的静态检查与渲染确认；只有任务本身修改鼠标交互行为时，才运行直接相关的鼠标检查。不要因综合UI测试默认包含鼠标用例而反复复跑。

## 项目规则与历史修订

- 水系寒潮锁阵确认（2026-09-23）：主清单docs/seven-elements-review-2026-09-23.md第4.41水34～水36已定。寒潮锁阵初学16水伤＋命中6寒霜／CD8秒／12灵力／0体力，本次通常最多触发1层；积霜改总计10层。骤冻沿原已定规则：施法前目标已有寒霜时，本次常规冻结＋0.2秒，不因新产层满足预存条件。金1～金39、木1～木39、水1～水36确认；下一步归墟断流。仅主清单与入口，不回填原七系、不实现、不模拟、不提交同步Git。

- 水系断脉印／枯脉双方向确认（2026-09-23）：主清单docs/seven-elements-review-2026-09-23.md第4.40水31～水33已定。断脉印初学18水伤＋命中4枯脉／CD6秒／8灵力／0体力；封灵／困体择一，修得后分别让自身施加的枯脉每跳原定扣灵／扣体额外＋25%，与蚀脉／深枯同一基础相加，强化原定量后只做一次不足转扣且不重复加成，不另建Debuff。未来多来源共享枯脉池需保存来源归属并细化混合来源分账。金1～金39、木1～木39、水1～水33确认；仍只改主清单与入口、不回填原七系、不实现、不模拟、不提交同步Git。

- 水系燃灵指确认（2026-09-23）：主清单docs/seven-elements-review-2026-09-23.md第4.39水28～水30已定。燃灵指初学10水伤＋命中即时扣目标6灵力／CD4秒／费用4灵力／0体力，灵障分担伤害不阻止扣灵；深蚀扣灵量＋50%为9，回流按目标实际扣灵的50%恢复自身灵力，二选一，目标无灵力不视为扣满。金1～金39、木1～木39、水1～水30确认；下步断脉印等三术。仅主清单及入口，不回填原七系、不改游戏、不模拟、不提交同步Git。

- 水系寒月天河确认（2026-09-23）：主清单docs/seven-elements-review-2026-09-23.md第4.38水25～水27已定。寒月天河初学60纯水法伤／CD10秒／20灵力／0体力，一次主动事件；断江直接伤害＋40%，进入护甲的部分再＋15个百分点护甲伤害修正，超额结转待完整流水线；覆海直接伤害－20%、命中产6寒霜，通常当次最多触发1层，初霜额外层当次不触发。金1～金39、木1～木39、水1～水27已确认；下步《沧澜燃灵诀》，仅主清单与入口，不回填原七系、不实现、不模拟、不提交同步Git。

- 水系沧澜寒潮确认（2026-09-23）：主清单docs/seven-elements-review-2026-09-23.md第4.37水22～水24已定。沧澜寒潮初学40纯水法伤／CD8秒／12灵力／0体力，一次主动事件；叠浪CD和费用各－25%，单独应用为6秒／9灵力，伤害不变；凝潮直接伤害＋30%、命中产2寒霜，通常本次最多触发1层，初霜额外层当次不触发。金1～金39、木1～木39、水1～水24已确认；仍只改主清单及入口、不回填原七系、不实现、不模拟、不提交同步Git。

- 水系玄冰枪确认（2026-09-23）：主清单docs/seven-elements-review-2026-09-23.md第4.36水19～水21已定。玄冰枪初学26伤／CD6秒／8灵力／0体力，水属性＋穿刺标签但法术来源；贯寒直接伤害＋30%，克制被水克属性时再＋10个百分点；裂冰进入护甲层的部分额外＋30%护甲损失，不强化盾或气血，超额结转待完整流水线。金1～金39、木1～木39、水1～水21已确认；只更新主清单与入口、不回填原七系、不改游戏、不模拟、不提交同步Git。

- 水系寒月刃首术确认（2026-09-23）：主清单docs/seven-elements-review-2026-09-23.md第4.35水16～水18已定。寒月刃初学基础12伤／CD4秒／4灵力／0体力，水属性＋斩击标签但法术来源；凝锋直接伤害＋25%，进入护甲层的部分另＋10个百分点护甲伤害修正，超额结转留完整伤害流水线；覆霜命中产3层寒霜，本术通常最多触发1层，初霜额外层当次不能触发。金1～金39、木1～木39、水1～水18已确认；仅主清单及入口，不回填原七系、不改游戏、不模拟、不提交同步Git。

- 水系总纲五重确认（2026-09-23）：主清单docs/seven-elements-review-2026-09-23.md第4.34水13～水15已定。玄水第四重启式合法水法术起手窗口＋1秒、承式水法术后续节点连携追加比例＋10个百分点；第五重沧澜同流每新增连续水节点使后续水本体伤害与水系灵力／体力恢复各＋10%、封顶20%，玄水两仪在自身润脉且目标枯脉或寒霜时使主动水伤与水系资源恢复各＋10%，敌方双状态不重复叠。金1～金39、木1～木39、水1～水15已确认；继续水系两本战斗功法，仍只改主清单及入口、不回填原七系、不实现、不模拟、不提交同步Git。

- 水系总纲前三重确认（2026-09-23）：主清单docs/seven-elements-review-2026-09-23.md第4.33水10～水12已定。玄水第一重水锋主动水伤＋10%／流息水系直接恢复灵力与体力各＋10%（普通丹药不算，转移不重复加成）；第二重乘克额外＋10个百分点／穿水甲逆克抵消最多10点克制增伤；第三重内鸣润脉每有效层1%→1.5%，外鸣目标枯脉／寒霜各1%→1.25%，读取上限不变。金1～金39、木1～木39、水1～水12已确认；只更新主清单及入口，不回填原七系、不实现、不模拟、不提交同步Git。

- 水系水8～水9最新确认（2026-09-23）：主清单docs/seven-elements-review-2026-09-23.md第4.32记霜回活水一次水攻实际触发寒霜后只获1层润脉，高级法术同一攻击事件触发多层仍只获1层；寒潮留霜对每层触发独立20%概率不耗层，凝霜保证保留的每第5次不重复判20%。金1～金39、木1～木39、水1～水9已确认；水系其余数值继续逐项确认。只更新主清单和入口，最终确认前不回填七系原方案，不改游戏、不模拟、不提交同步Git。

- 水系水4～水7最新确认（2026-09-23）：docs/seven-elements-review-2026-09-23.md第4.31已记。养脉／长养对润脉每跳原定灵力、体力恢复量分别＋10%／＋20%，蚀脉／深枯对枯脉每跳原定双资源扣量分别＋10%／＋20%；同向同选各按原定量合成＋30%，转移量不重复加成。凝霜沿用每第5次实际触发寒霜必定不耗层的既有确认。枯极生寒一次枯脉结算只要正数资源降至0，最多追加1层寒霜，两项同降0也只加1层。金1～金39、木1～木39、水1～水7已确认；仅更新主清单及入口，最终确认前不回填原七系、不实现、不模拟、不提交同步Git。

- 水系第一重数值最新确认（2026-09-23）：docs/seven-elements-review-2026-09-23.md第4.30已记水1～水3。活泉／潜枯仅在润脉／枯脉从0新生时把首次结算等待2秒改为1.5秒，后续仍每秒结算；初霜在目标攻击前无寒霜且本次成功施加时额外加1层，但这层不能由生成它的同一次攻击触发，普通新施加层仍可按先施加后结算触发。金1～金39、木1～木39与水1～水3已确认；后续仍只改主清单和入口，最终确认前不回填原七系，不实现、不模拟、不提交同步Git。

- 木系数值确认最新入口（2026-09-23）：先读docs/seven-elements-review-2026-09-23.md第4.16～4.29。金1～金39、木1～木39已确认，木系八术第一版初学值已齐；森罗灵矢通用命中计算结果额外＋15个百分点（覆盖先前＋10点及“待定”）。CD／持续时间中间计算保留原精度，只在最终落值时四舍五入到小数点后两位；疾藤5秒×75%＝3.75秒，不向上对齐0.5秒。木系御兽、通用命中上下限及跨系平衡仍待以后设计。后续确认仍只更新主清单及入口，用户最终确认前不回填原七系方案；不改游戏、不模拟、不提交同步Git。下方木16～木27等旧阶段记录保留历史状态，不覆盖本条。

- 木系战斗功法第一组与初学值（2026-09-23最新）：先读docs/seven-elements-review-2026-09-23.md第4.21～4.24。木16～木27已确认：青木刺锐刺＋25%／毒芽命中＋3毒蚀，基础10伤／4秒／4灵力；青藤刃连刃CD和费用各－25%／缚刃命中＋5缠绕，基础22伤／6秒／7灵力；森罗灵矢穿林＋30%／毒翎伤血＋4毒蚀，基础36伤／8秒／12灵力，高命中具体值仍待；万木森罗森罗尽发＋40%／枯荣夺生实际伤血每10点得1生机、至少1层且四舍五入，统一木属性法术伤害且一次攻击事件，基础60伤／10秒／20灵力。四术自身均0体力。缚灵藤基础命中＋3缠绕，盘根总5层或疾藤CD费用各－25%二选一；伤害、CD与费用尚待。金1～金39、木1～木15及金33内鸣更正见下条和主清单；御兽以后专章。仍只更新主清单和入口，不回填原七系、不实现、不模拟、不提交同步Git。

- 庚金第五重／化神器具连携及木系前十五项（2026-09-23阶段记录）：先读docs/seven-elements-review-2026-09-23.md第4.13～4.20。金1～金39已确认；纯金贯式要求合法连携连续两个金属性节点，兵法合流要求至少一个金系法术与武器／法器参与，且器具连携仍在化神开放。第五重两路线各在合格时使追加比例＋10个百分点，可与第四重合格＋10点按同一20%基础相加至40%。化神器具后续节点沿用实际扣特殊盾／灵障／护甲／气血合计为有效伤害，按比例产生无属性法术追加包。木1～木15已定：早春／潜毒首次等待2→1.5秒，盘根每次合格事件额外＋1缠绕；生机／毒蚀专精第二＋10%、第四＋20%，同选每跳效果＋30%；缠绕专精第二＋20%、第四＋30%，同选每层火／雷增伤2%→3%；第五重生生不息合法溢出50%转灵障，蚀骨难消／盘根不绝每第3次合格结算不耗层；青木第一重木锋／木息各＋10%，第二重乘克＋10点／逆克减最多10点，第四重木启式窗口＋1秒、承式追加比例＋10点，第五重万木同息连续木节点每次＋10%封顶20%、生杀并济主动木伤／木系回血各＋10%。木12同时更正金33：金／木内鸣每有效层1%→1.5%，外鸣1%→1.25%，旧金33两条均1.25%废止；金启式／承式均为追加比例＋10点，不是木启式增时。御兽以后专章；具体合法配对、窗口终值和追加包自身流水线仍待设计；炼器与阵法另成体系。仅更新主清单和入口，不回填七系原案、不实现、不模拟、不提交同步Git。

- 七系连携基值／庚金第四重确认（2026-09-23历史阶段）：主清单docs/seven-elements-review-2026-09-23.md第4.14金34～金36已确认：第二法术正常结算实际扣除的特殊盾／灵障／护甲／气血合计为有效伤害，不计过量及后续派生；元婴合法连携基础追加比例20%；启式／承式各额外＋10个百分点（二选一），基础20%时合格为30%。追加仍为无属性法术伤害，自己的防御与暴击细节待完整流水线。后续金37～金39已由上条最新记录确认。仅清单及索引，不回填原七系、不实现、不模拟、不提交同步Git。

- 七系金22～30确认／八术初学表落档（2026-09-23最新）：主清单docs/seven-elements-review-2026-09-23.md中金1～金30现均已确认。4.9记玄庚分支，4.10记附灵／返锋各留一份、仅百兵最强武器承接且增加量不乘权重；4.11八术初学基础伤害／CD／灵力费用已定，法术自身0体力不免除御兵完整武器费用与百兵50%武器费用。4.12为已定公式例算，疾御CD6秒／4.5灵力、骤雨CD4.5秒。当前无已提出未回复问题；4.8剩成长、庚金总纲、炼器／阵法，不宣称整系数值完成。仅清单与入口，不回填原七系、不实现、不模拟、不提交同步Git。

- 七系第3.3闭合／金系21项确认（2026-09-23最新）：先读docs/seven-elements-review-2026-09-23.md。第3.3确认护盾先吸属性再物理、反击先于追击；第4.1～4.7金1～金21均已确认，覆盖御兵／百兵参与规则、兵甲／返锋、保留率、附灵／御兵强化、百兵权重、金煞第三／五重参数。最新为反锋镇兵延长1秒、锋痕入骨每层＋1%武器本体伤害、血势不绝每次合格主动攻击＋1流血。无已提出未回复问题；第4.8仍列玄庚剑诀、八术CD／费用／成长、总纲／炼器与组合边界，不能宣称全金系数值完成。金煞留痕25%不等于玄庚同名分支的施加层数。仅维护主清单及入口，最终确认前不回填原七系方案；不实现、不模拟、不提交同步Git。

- 七系Q1～Q5确认（2026-09-23最新）：五项已在docs/seven-elements-review-2026-09-23.md第3.1／3.2关闭，仍只更新清单不回填原七系方案。伤害百分比加减成含共鸣按同一基础相加；普通雷攻引雷成功后不再触发连环，无引雷仍可连环，连霆可一次引雷再一次连环，原伤害保留且不开放派生递归。重伤第2秒按原层数生效，第3秒首次减层；盈流成护起始双满可直接50%转灵障，经过50%资源转移的最终余量再乘50%；比例产层／返层四舍五入至整数，已有专门半数消耗公式不改。D1／D2及内容数值仍后定，完整流水线待最终审定。仅文档，无游戏实现／模拟／Git同步。

- 七系复盘集中确认／暂停回填（2026-09-23最新）：用户明确后续确认先只更新docs/seven-elements-review-2026-09-23.md，待其最终确认后再把关键点补回七系原属性方案；不得再随每次答复批量改七系正文、原状态表和战斗总设计，也不回滚此前改动。项目入口仅维护该优先级。最新15项／3.2答复及追问已汇入主清单；明显语音错字可校正，涉及机制歧义须保留待确认，不以猜测定案。雷蕴第10个只触发生盾、扣池中9层后显示0，连霆5／4层仍10＋8＝18，双霆仍最多两次雷击均已澄清。后续明确效果锁定扣层前值、先扣层／移除再结算；回风引不能选自身，采用减CD不作用于减CD目标的互推限制。其余已定／未决与待填数据以主清单为准，下方历史摘要不覆盖最新答复。本轮仅文档，不实现、不模拟、不提交同步Git。

- 雷盾替换麻痹／主清单第3节复核（2026-09-23最新）：用户明确“雷盾被替换也触发麻痹”，覆盖上一条替换不触发麻痹；剑幕被替换不因此自动返剑，其他破盾监听与多层替换事件次数尚待定义。已直接重写docs/seven-elements-review-2026-09-23.md第3节，按原15项列已定／未定；3.2解释反击、雷击成环、治疗转盾与取整，3.3伤害流水线仅为建议，3.4列以后设计。基础攻击＋强化取最大、无攻击则无雷击伤害、追击默认不暴击、预备式开启／关闭与不足继续待发、寒霜每水攻一次／冻结时长累加、水雷交替加时取消、计数归零／移出不重置且战后重置、无层数药效按独立效果整体等概率驱散、回澜缺失百分比权重、法术1/1/1/2格及境界重数表、多总纲并行／整本重置默认0均不重问。真未决集中同击状态触发、雷系派生引雷许可范围与次数、时钟、零资源成本、满血反噬、枯极生寒正数归零与舍入、CD自指／就绪／互推。雷盾被派生打破与麻痹的防御响应不再单独当作冲突；仍保留明确递归边界。仅文档，不实现、不模拟、不提交同步Git。

- 七系边界逐项讨论最新入口（2026-09-23后续）：用户要求原清单第3节15项一项一项处理，先读docs/seven-elements-boundary-followup-2026-09-23.md及其链接的用户原文。本轮先第1项，已定雷盾先算总吸收再按生成先后扣层、余雷蕴保留、法器修正率必须指定盾类型；特殊盾仅保留一种类型已澄清：雷盾同类可叠层，剑幕与全部雷盾相互替换，替换直接作废不触发破盾麻痹／返剑，并移除原雷盾内鸣贡献。其余新决定已记录，优先于下方历史：取消水雷交替50%加时、每次水攻只触发一次寒霜、回澜改按缺失百分比权重、派生闪电可触发雷印等，后续按序解释同步。不得把疑问句、0恢复与0成本混淆、8重与4术混淆或“生成雷印”的可能笔误强写定案。原文保留；仅设计文档，不开发、不运行模拟、不提交同步Git。

- 七系复盘答复／分层雷盾确认（2026-09-23）：最新以docs/seven-elements-review-2026-09-23.md第1节及七系正文为准，第3节未决边界、第4／5节待填数值分开。坚韧／疲惫／轻灵／失衡专精首次＋0.4个百分点、第二次＋0.6个百分点（单选1.4%／1.6%，同选2%）；驱散额外概率＋10／＋15个百分点，同选25%。总纲上阵才生效，状态专精学会常驻，每种法术唯一且最多上阵一个；分支只能特定丹药整本重置。所有道具／法术手自动可切换并有统一切换按钮；实际发动扣资源，待发不转下轮CD，资源不足或麻痹不发动，镇脉截断重开CD特例保留。同击默认一次二次追击，追风优先不耗失衡；追击取首击系数后／盾甲前武器伤害×50%，再过防御。雷击＝己方阵盘武器／法器／法术最高攻击×1.5，单纯施加雷印不引雷。雷盾替换旧单池：每10雷蕴消耗生成一层独立盾，存活每层按10层内鸣；整次攻击共用60%额度再逐层扣，每破一层各施加麻痹。伤害100、两层各50时扣50＋10，余40入甲。雷盾与灵障互斥，全破后下一击才启用30%灵障，同击不接续。剑幕独立30值／40%吸收、护主50值，优先承伤；返剑在剑幕耗尽后发动15点金攻。麻痹未结束新增寒霜或反向衔接，新控制＋50%（0.5→0.75秒），不追溯旧控制。法术相克与穿刺修正相加，基础只计一次。剩余取值、排序、容量／概率等不自行补成定案；仅文档，不实现、不模拟、不提交或同步Git。

- 七系复盘／灵障30%最新修订（2026-09-23）：用户将普通防护资源【灵障】的基础吸收率由50%明确改为30%，实际吸收min（当前灵障值，进入本阶段的伤害×30%）；【雷盾】仍60%，其他50%转移／追击规则不变。现行设计与索引已同步，以下9月22日进度式说明中的50%为历史值，以本条为准。复盘见docs/seven-elements-review-2026-09-23.md，涵盖七系35本功法／56法术的待填参数及共用边界。土／风四重覆盖二重、功法生效容量、法术手自动／预备式、双标签伤害、双盾顺序／来源、追击竞争、控制与CD连锁、雷击无武器基准等建议均未定案，不得直接实施。只更新文档，不运行Godot或平衡模拟，不同步Git。

- 雷系功法正式第一版基线（2026-09-22）：用户指定docs/thunder-cultivation-design-2026-09-22.md单独记录雷系“蓄／引／连／镇”定位、五本功法、8个战斗法术、阵法技艺、雷盾防反、雷印引雷、连环高频与麻痹控制，作为七属性最后一份正式第一版内容基线。附件常规护盾30%与已确认灵障50%冲突，现行正式规则继续采用灵障50%；雷盾新定基础吸收60%，实际吸收min（当前雷盾值，伤害×60%），基础生成值和总容量主要由境界决定且不绑护甲上限。破盾麻痹必中；普通麻痹重复施加刷新到至少1秒，高阶能力可追加。雷印由成功命中的主动雷武器／法术触发，默认一次雷攻最多1次；双霆引印可消耗20层触发2次且最多2次。灵障／雷盾先后与具体数值待后续。本轮只记录设计，不实现功法／雷盾／雷击／麻痹，不同步Git。

- 风系功法正式第一版基线（2026-09-22）：用户指定docs/wind-cultivation-design-2026-09-22.md单独记录风系“灵／准／追／扰”定位、五本功法、8个战斗法术、探索追踪、穿刺攻伐、闪避／追击事件、预备式、驱散与CD干预，作为继金／木／水／火／土后的第六份正式属性内容基线。角色基础闪避0%；轻灵与听风步判定独立但都产生“成功闪避事件”，失衡与追风式来源独立但都产生“二次追击事件”。新版驱散池包含七属性Buff（包括轻灵）、持续恢复、毒性免疫和其他明确可驱散效果；新施加的风系Buff仍先与驱散按同属性规则直接抵消，已存在的风系Buff可被后续随机驱散。自动单目标CD干预按阵盘从第一行左至右逐行扫描首个合法目标，手动模式可选合法目标；CD推进至0后按物品自动／手动设定继续，延长可沿用既有突破原CD上限规则。必中与必闪优先级、具体数值及若干事件边界仍待后续。本轮只记录设计，不实现功法／状态／驱散或CD干预，不同步Git。

- 土系功法正式第一版基线（2026-09-22）：用户指定docs/earth-cultivation-design-2026-09-22.md单独记录土系“稳／重／震／压”定位、五本功法、8个战斗法术、勘脉采矿、预备式、暴击攻防、镇脉截断与玄岳反震，作为继金／木／水／火后的第五份正式属性内容基线。破岳式必暴独立于疲惫，不读取／消耗疲惫或触发疲惫专属效果，但仍属于真实暴击事件；守岳式独立于坚韧。重伤从0层获得时等待2秒，进入生效状态后新增层数直接并入且不重启等待。镇脉诀增耗导致资源不足时截断本次发动并让物品／法术重开完整CD；玄岳反震只在物理攻击经灵障／雷盾等规则后实际造成护甲损失时触发。定身范围、具体数值与若干暴击边界仍待后续流水线细化。本轮只记录设计，不实现功法／状态／控制或反震，不同步Git。

- 火系功法正式第一版基线／灵障方向修订（2026-09-22）：用户指定docs/fire-cultivation-design-2026-09-22.md单独记录火系“蓄／灼／熔／爆”定位、五本功法、8个战斗法术、炼丹、爆燃／熔爆、灼骨／熔甲难复及主要Build，作为继金／木／水后的第四份正式属性内容基线。爆燃读取灼烧追加派生火伤但不消耗状态、不读共鸣、不递归；熔爆读取已有破甲，只对护甲追加火伤，不绕甲、不读共鸣、不产生派生链。正式资源名为灵障，基础吸收率50%，实际吸收min（当前灵障值，伤害×吸收率），剩余继续结算；特定法器可提高吸收率上限及实际吸收率，最高100%。容量／生成／生命周期、多个法器修正合并及雷盾先后仍待统一。破甲不能绕过灵障／雷盾，先经护盾类规则再拆进入护甲阶段的剩余火伤；灼骨只强化真正进入生命层的灼烧。该文不代表功法／状态／灵障规则已实现，不同步Git。

- 水系功法正式第一版基线／寒霜细化（2026-09-22）：用户指定docs/water-cultivation-design-2026-09-22.md单独记录水系“流／寒／耗／封”定位、五本功法、8个战斗法术、制符、装备语言与主要Build，作为继金／木后的第三份正式属性内容基线。冰／霜／寒水均属水属性；任何角色可获得和使用符箓，水灵根与灵墨玄符录提供深层制作优势。寒霜基础每层冻结0.5秒，必须由成功命中的主动水攻触发，不要求伤害进入护甲／气血，灵障基础吸收50%且不阻止；同次攻击先施加再触发，一般器具／法术每次事件最多1层，高级法术可明确多层／清空并纳入本次新增层；冻结轮转／入阵CD和就绪发动，不冻状态／药效跳动、持续时间及撤退。凝霜每第5次触发必定不耗层；寒潮留霜为每层触发时独立概率不耗，第5次由凝霜直接确定、不重复判概率；玄霜令每层1秒，骤冻令已有寒霜时本次常规冻结+0.2秒。归墟按现有资源平均缺失比例追加，最高与基础伤害相等，总伤最高2倍；回澜以实际追加伤害作恢复总额并按两项当前剩余比例反向分配，达到资源上限后的溢出量直接作废、不再转移。水系本轮三个小边界均已闭合。本轮只更新文档，不代表机制已实现，不同步Git。

- 木系功法正式第一版基线／连携术语统一（2026-09-22）：用户指定docs/wood-cultivation-design-2026-09-22.md单独记录木系“生／蚀／缚”定位、五本功法、8个独立战斗法术、装备语言与主要Build。五本书名全部保留：百兽驭灵录／青木真解／枯荣生灭经／万木生杀诀／青萝化生诀；百兽第五重使用主契／群狩，三本功法的重名及枯荣分支名采用用户本轮最终修订，8个法术名保留。该文是继金系后的第二份正式属性内容基线，不代表游戏已实现。今后现行设计文档统一使用“连携”，不再使用旧英文术语；历史原文归档不做追溯改写。木系后续先读该文、21状态设计与装备／功法总纲，不同步Git。

- 金系功法正式第一版基线（2026-09-22）：用户指定docs/metal-cultivation-design-2026-09-22.md单独记录金系属性定位、反锋／锋痕／流血、装备语言、五本功法、8个独立战斗法术、大庚剑阵与主要Build，作为后续六系达到同等完整度的首份标准样板。五本为玄金百炼录／庚金真解／金煞藏锋经／玄庚剑诀／百兵御灵诀；前3本5重，后2本8重；8个法术来自两本战斗功法。该文是内容设计基线，不代表功法、法术、阵法或金系新机制已接入游戏；具体数值、CD、资源成本、状态产量、共鸣上限与部分比例仍按文内留待后续。金系后续设计先读该文，再读装备／功法总纲及21状态设计；不机械复制到其余六系，不同步Git。

- 状态生命周期与触发最新补充（2026-09-22）：当前21状态仍查docs/combat-status-design-2026-09-22.md及速查表。流血叠加共同刷新5秒；缠绕／连环每次效果结算后减1层；重伤2秒后生效、每秒减1层。雷印每次主动雷攻最多引雷一次并消耗10层，25→15余层等待下一雷攻，不转换独立标记。驱散无目标余层保留5秒；新增可驱散效果或新增驱散层都启动1秒后重结算并重置5秒窗。七属性Buff均可进入随机驱散池；新增风系Buff仍先同属性抵消并沿用生成顺序，实际存留后才可被随机驱散。独立追击率先判发动、再判命中，成功才消耗失衡。派生伤害默认不触发二次属性机制，明确来源例外另定；雷击不触发连环、灼烧不触发破甲、连环闪电不利用缠绕。灵障基础吸收50%，实际吸收min（灵障值，伤害×吸收率），法器可提高吸收率；破甲再按进入护甲阶段的剩余主动火伤拆包。全部资源支持1位小数，状态整数；须保存Debuff生成批次，舍入方式仍待统一。双Debuff共鸣上限留待数值设计。保持21状态，后续转入装备／法器／功法／法术等内容设计；本轮只改文档不实施游戏或配色，不同步Git。

- 最新七系状态设计（2026-09-22）：先读docs/combat-status-design-2026-09-22.md及docs/buff-debuff-reference-2026-09-22.md，设计现为7种Buff＋14种Debuff；新增流血／缠绕／寒霜／破甲／重伤／驱散／连环不替代旧7种。坚韧／轻灵／疲惫／失衡对应成功后消耗max(1,floor(N/2))（N≥1），失衡须追击成功，最后1层消耗完。润脉／枯脉均从溢出／不足项向另一项只做一次50%转移，允许小数；转移后无法容纳／扣除的余量作废，禁止反向递归。麻痹统一1秒、CD照转且到点跳过发动重开轮转；寒霜基础每层冻结全阵盘0.5秒，先施加后触发，一般主动攻击事件最多1层且高级来源可明确多层例外，冻结范围和排除项见水系基线／状态文档。抵消按生成时间，同刻随机逐种处理；共鸣合计同属性两类Debuff，各Debuff独立上限。下方旧14状态、成功清零及2秒暂停麻痹属于历史设计／当前旧释义，不覆盖本版。生产注册表仍原14词，完整战斗状态仅毒蚀已接入，体力／灵力仍整数；本轮仅文档，不自行实施全套机制或改词色，未定接口见新版第8节，不同步Git。

- 后续装备／功法开发核心参考（2026-09-21）：用户指定docs/equipment-cultivation-framework-2026-09-21.md为武器、防具、法器、符箓、灵根、功法／法术与高阶连携的核心参考架构，原文31节在docs/sources/equipment-cultivation-framework-input-2026-09-21.txt。相关新设计先读该纲，再读9月18战斗状态设计及后续实现补充；具体装备数值留待内容设计，示例不当正式配置。原文第五档“稀品”与现品级“灵品”差异保留待衔接，不自行迁移名称。此次只归档和维护入口，未授权一次性实现全部机制；保留当前实现及既有未决边界，不同步Git。

- Latest cultivation/item access (2026-09-21): six cultivation ranks use maximum item quality=order+2 (zero-based order), mortal→凡品 through spirit transformation→仙品. Test protagonist is元婴, max玄品; original resource stats and ten下品 map items unchanged. Explicit production quality is required; quality_level derives from the shared seven-rank mapping. Over-level names/categories become？; tooltip keeps only SummaryPanel with cultivation-insufficient copy, no effects/glossary. Map placement/swap/random equip, formation apply and battle equip/runtime enforce eligibility. Save restore returns over-level equipment to storage without losing ownership; formation application rejects atomically. Rewards remain collectible but masked; search uses visible name. Unranked creature innate attacks are exempt. See current inventory design. No Git sync.

- Latest keyword color review boundary (2026-09-21): user objects to unreviewed palette decisions. Existing14 status colors and legacy11 mint-green terms describe implemented state, not an approved final palette. Present complete keyword inventory, exact swatches and proposed changes for user review before further color changes. This review changed no game code/colors. Current shared item-tooltip scope is25 terms (20 glossary-capable plus5 body-only); sidebar labels and nested terms inside glossary explanations are separate and not covered by the previous color implementation.

- Latest status keyword icon colors (2026-09-21): all14 elemental Buff/Debuff terms use individual readable colors sampled from their own icons in both tooltip effect copy and the lower glossary term. Pairs/colors:反锋F2BF45/锋痕EEA43A,生机7FE05A/毒蚀8BD43B,润脉59CCEF/枯脉86AFEA,附炎FF9B38/灼烧FF7138,坚韧DFB35C/疲惫BCA082,轻灵75DFE2/失衡8BCFD2,雷蕴C18CFF/雷印F2CF58. Other keywords retain9DE3C3. Registry now stores icon/color; seven new debuff icons have mipmaps. Text,fonts,status logic and compound-keyword exclusions unchanged. Dedicated186 headless and OpenGL static-render checks pass; one earlier D3D12 static render passed at172 checks, while a later concurrent rerun hit engine OOM. No mouse suites or Git sync.

- Latest dialogue-space/card geometry (2026-09-21): while MapDialogue is visible, one non-echo Space press advances exactly like a completed left click, including illustration→first line and final completion. Reward modal remains explicit-click-only. Map/reward cards use3:4 width:height, and current 下品 cards crop levelbq-1.webp toRect2(14,7,327,384), display it at42 design px wide with its center on the card bottom-border center. Allow badge overflow; row gap15→18, horizontal gap15 unchanged; quantity moves up4px. Preserve card contents/gameplay. Direct dialogue input plus static/render card checks only; no unrelated mouse suites or Git sync.

- Latest rarity card artwork (2026-09-21): map/reward cards map 下品/凡品/良品/上品/灵品/玄品/仙品 to level-1..7.webp. Sample only the safe interior Rect2(48,48,416,672), aspect-cover centered, excluding all authored rims. Draw an independent inward2px rarity-colored border with radius10 and analytic AA clipping; overflow artwork is hidden. Keep six columns,733/490 card aspect and foreground geometry. Retire old level-fanpin optical/top-inset compensation; actual row gap6→15, matching horizontal15. All seven assets mipmapped; current10 item qualities stay下品. Static/render checks only, no mouse tests or Git sync.

- Latest header/button and item-tag polish (2026-09-21): shrink both map Head utility buttons to36×36. Settings stays circular at design(14,48); task list is a radius6 square at(1872,12), with its three lines thinned3→2px and6px vertical spacing. Move MapInventoryItemCard CategoryTags down4px,y181→185, affecting map storage and reused reward cards. Preserve hints,input behavior,fonts,card geometry and gameplay. Static/render validation only,no mouse suites or Git sync.

- Latest event trigger scope (2026-09-21): automatic arrival triggering applies only to story-progression events explicitly configured with trigger=arrival (current N37/N20, including intermediate-route arrival). All other events default to trigger=click: reaching or passing the point never opens them; the player must be stationary on that point and click the path node. Remote clicks and moving-state clicks do not trigger. Registry accepts only arrival/click. Preserve prerequisites,route pause/resume,reward and completion rules. Event101/dialogue36/illustrated138/story35 checks pass; no Git sync.

- Latest inventory card name font (2026-09-21): map/reward MapInventoryItemCard item names now use exactly the same inherited theme font as the category chips beneath them. Remove the previous bold SimSun FontVariation override only; keep20px gold title size/color,position,card geometry,tags and behavior. Battle StorageItemCard already uses the inherited font. Static D3D12 validation only; no mouse suites or Git sync.

- Latest map header utility buttons (2026-09-21): add a44×44 circular dark settings/gear button at design(10,44) on the left and a42×36 radius6 dark task-list button with three horizontal lines at(1866,12) on the right. Both scale with the1920 Head, use a pointing hand and simple future-feature tooltips; they intentionally have no click action yet and consume input instead of passing it to the map. Preserve all status/header geometry and content. Dedicated header headless/D3D12 checks only; no unrelated mouse suites or Git sync.

- Latest sidebar artwork opacity (2026-09-21): set only the InformationPanel inventory-sidepic.webp artwork alpha0.25→1.0, making the screenshot's 属性加成 container background fully opaque. Keep transparent StyleBox backing, existing top64px fade shader, artwork geometry/crop,18px clipping,gold rim,return button,content and interactions. Static/render validation only; no mouse cases or Git sync.

- Latest automatic story/reward flow (2026-09-21): configured unlocked story nodes trigger on arrival, including intermediate nodes, pause travel at the node and resume the route after completion. N20 after zero-based line14 opens UI-jiaohu-xiao reward modal with the storage-style warm jade card and button-queren/white SimHei 收下. Only acceptance grants1 item, saves its stable receipt atomically and plays gameplay-bag-pick. Existing seeded jade remains. Optional version1 granted_rewards persists added ownership and prevents repeat grants across story replays/restarts; failed saves leave live ownership unchanged and permit retry. Story completion itself remains run-scoped. Map/reward/battle storage cards hide quantity1 and show >1. See docs/map-interaction-design.md; legacy click-again/no-reward guidance is superseded. No Git sync.

- Latest keyword scope/status registry (2026-09-21): remove compound keywords毒蚀免疫,护甲上限,恢复上限 and everyXX上限; retain copy but highlight only base毒蚀/护甲 where applicable. Register all14 confirmed elemental statuses with buff/debuff polarity and meanings in data/ui/combat_status_keywords.json:反锋/锋痕,生机/毒蚀,润脉/枯脉,附炎/灼烧,坚韧/疲惫,轻灵/失衡,雷蕴/雷印. 麻痹 remains a control,not one of14. Base glossary7 plus statuses14 overlap on毒蚀=20 unique glossary-capable terms; plus冷却/消耗/气血/体力/灵力=25 unique highlight-capable terms. Keep current green pending a later color decision. Dedicated130 headless/D3D12 static-render checks pass without mouse cases; no Git sync.

- Latest map actor sheet replacement (2026-09-20): idle=stand16-spritesheet-4x4.webp16 frames, walk=walk26-spritesheet-6x5.webp26 frames, run=run16-spritesheet-4x4.webp16 frames. Use360×360 row-major frame_regions and sequential playback; walk's last4 transparent cells excluded. Preserve cycle durations with12/26/(16÷0.75)fps, movement200/360 and existing route/facing rules. All display heights144/scale0.4; feet offsets-136/-134/-135. Enable mipmaps and linear mipmap sampling. Dedicated animation/model/render checks only, no mouse suite or Git sync.

- Latest taxonomy naming/removal (2026-09-19): rename consumable child投掷物→掷具; remove阵图/board_recipe from blueprints. Blueprints are now丹方/器方/符方/饵方, all-tab grouped secondaries18→17, supported leaf categories20→19. Current10 items, filters,counts,search,quality,sort and six-column layout unchanged. Static JSON/editor validation only; no mouse cases or Git sync.

- Latest value-column direction correction (2026-09-19): user corrects prior+10px shift. All13 attribute values now use-10px offsets from the original column position (20px left from preceding version). Icon/name remains+5px;44px centered value slots, block geometry and behavior unchanged. Static rendering only; no Git sync.

- Latest sidebar text/cultivation row (2026-09-19): all attribute names and values use #FBF4BF. Add修为 below灵力 using player-status-level.webp, fixed+0% for future board cultivation-speed bonuses, same label hover/hand cursor/body tooltip. Keep33px rows,9px gaps and+5/+10px offsets; centered group now635px tall. Display placeholder only; no cultivation mechanic added. Static render only, no Git sync.

- Latest attribute hover targets / offsets (2026-09-19): all12 sidebar attributes use a shared icon+name HBox hover target with pointing-hand cursor and the existing body-only tooltip. Add hp/stamina/spirit descriptions. Shift icon/name contents right5px and value contents right10px inside fixed column slots; preserve centered block, row heights and9px gaps. No resource/gameplay changes or Git sync.

- Latest sidebar content centering (2026-09-19): center the complete heading/motto/resources/nine-buff block inside the208×910 panel, independent of return button. Title-to-motto18→9px; motto-to-first-rule, first-rule-to-health and second-rule-to-counter all9px layout gaps. Preserve typography, icons, values and hover. Static/render validation only; no mouse suites or Git sync.

- Latest sidebar buffs / all-category expansion (2026-09-19): all tab exposes all18 grouped secondary categories. Sidebar motto precedes its separator; below resources show armor/shield, separator, counter/vitality/moisture/flame/tenacity/agility/thunder with mipmapped buff art. User confirmed sum configured permanent cultivation/board/equipped-item buff_bonuses; unconfigured=+0, storage/timed restores excluded, armor includes equipped armor_capacity. Icon hover reuses ItemTooltip body-only dark540px panel and23px KaiTi. Definitions in data/ui/map_buffs.json; this is display/aggregation, not new elemental combat mechanics. No Git sync.

- Latest inventory taxonomy / final glossary polish (2026-09-19): primary tabs全部/收藏/器物/消耗/灵材/蓝图/关键/杂物; equipment children武器/防具/法器/阵盘/符箓, consumables丹药/投掷物/诱饵/食物, materials草木/兽材/矿材/异材, blueprints丹方/器方/符方/阵图/饵方. All/favorite/key/misc have no secondary row. Parent selection clears secondary; clicking selected child again returns to parent-wide results. Keep counters,search,quality,sort,six columns and current10 IDs. Glossary now7 terms: remove消耗, continuous copy is generic, light armor says护甲类型, physical damage copy explicitly states other armor100%. No gameplay changes or Git sync.

- Latest item-tooltip typography/wording (2026-09-19): shared ItemTooltip uses SimSun for the item name and each concrete highlighted term, KaiTi for effect copy and glossary explanations; the glossary heading remains SimSun. Weapon chips are category/subcategory/damage type/footprint, so the third chip is斩击/穿刺/钝击. All three current pills say使用后消耗一瓶 in body and consumption meaning. Armor restoration omits the redundant不超过护甲上限 phrase. Presentation only; no gameplay/hover changes. Dedicated120 headless/D3D12 checks pass without mouse cases; no Git sync.

- Latest physical damage/toxin integration (2026-09-19): live physical matrix slash vs none1.2/light1/heavy0.8/spirit1, pierce1/1.2/0.8/1, blunt1/1/1.2/1. Both former1.05 entries now1.0. Share EffectSystem matrix with tooltip; scale before armor, keep fractional HP/armor/damage totals and displays. Toxin first ticks at2s then each1s for current stacks damage followed by minus1 stack; additions never reset timing, clearing invalidates pending ticks, reapplication after zero waits2s. Existing detox clears and grants15 combat seconds immunity; same-time cleanse before tick. No current enemy toxin source added. Read docs/combat-damage-toxin-2026-09-19.md. Glossary now8 concise terms; no cooldown/armor-cap/recovery-cap/toxin-immunity definitions. No Git sync.

- Latest tooltip width reduction (2026-09-19): reduce shared ItemTooltip width by one quarter,720→540 design px. Main summary and inset keyword panel narrow together through the existing WIDTH-derived constraints; height remains content-driven. Preserve96px portrait,fonts,8px explicit-hard-break gap,default soft-wrap line spacing,wording,gameplay and hover behavior. Dedicated68 headless/D3D12 static-render checks pass without mouse cases; screenshots show no overflow; no Git sync.

- Latest tooltip hard-break spacing (2026-09-19): render each explicit newline in ItemTooltip effect copy as a separate RichTextLabel inside an8px VBox gap, matching the KeywordRows separation. Do not override RichTextLabel line_separation, so automatic wrapping retains its existing compact spacing. Text,fonts,keyword layout,gameplay and hover behavior remain unchanged; the later width rule above supersedes the then-current720px width. Dedicated68 headless/D3D12 static-render checks pass without mouse cases; test cleanup explicitly releases the two cached tooltip themes to avoid exit leaks; no Git sync.

- Latest four-panel backing removal / formation opacity (2026-09-19): raise inv-bg-1 FormationPanelBackground alpha0.4→0.5. Remove the opaque flat StyleBox fills beneath all four main panels (InformationPanel,BoardPanel,FormationPanel,StoragePanel) by setting their backing colors transparent; the global inventory background now shows wherever their artwork is translucent. Keep each artwork alpha (sidebar0.25,board0.5,formation0.5,storage0.5),radii18/13/13/13,borders,art geometry,content and interactions. Dedicated51 headless/D3D12 render checks pass without mouse cases; no Git sync.

- Latest formation-background opacity (2026-09-19): raise only inv-bg-1 FormationPanelBackground alpha0.3→0.4. Keep its display x=-1 with only the left edge extended1px, right/top/bottom fixed, centered1% content enlargement,714/718 horizontal content scale,13px clipping,cards and interactions. Board artwork remains restored at alpha0.5/x=-7/right+4/top0/bottom+5. Dedicated50 headless/D3D12 render checks pass without mouse cases; no Git sync.

- Latest formation-background correction (2026-09-19): user corrected the previous target. Revert inv-bg-2 BoardPanel artwork to its prior x=-7/right=panel+4/top0/bottom+5 and alpha0.5. Apply the requested change to inv-bg-1 FormationPanelBackground instead: extend only its display left edge1px (x=-1, right/top/bottom unchanged) and lower that artwork alpha0.75→0.3. Keep its centered1% content enlargement,714/718 horizontal content scale,13px clipping,card layout and interactions. The6px storage row layout gap and #806d48 StoragePanel rim remain. Dedicated50 headless/D3D12 render checks pass without mouse cases; no Git sync.

- Latest card-row/storage-rim/board-art polish (2026-09-19; board-art portion superseded by the correction above): keep storage card horizontal layout gap15px, but set the vertical layout separation to6px to compensate for level-fanpin.webp's approximately30-source-pixel transparent/dark top inset; at the current fixed card width the visible frame-to-frame row gap is approximately15px without changing card size, first-row placement or card internals. StoragePanel's existing1.2px analytic rim now uses #806d48 only; InformationPanel remains d9bb72. The originally recorded inv-bg-2 change was later redirected to inv-bg-1 by the user. Dedicated50 headless/D3D12 render checks pass without mouse cases; no Git sync.

- Latest storage rows/radii/border correction (2026-09-19): undo justified spreading for the collection/category rows; restore compact left-aligned HBox8px and wrapping HFlow5px layouts while retaining the shared22px row inset and title optical compensation. Set FormationPanel and StoragePanel corner radii to BoardPanel's13px. Restore only StoragePanel's procedural shader rim using the exact InformationPanel gold color,line width and AA implementation; formation remains borderless. Card22px side insets and15px horizontal/vertical gaps remain. Dedicated50 headless/render checks pass without mouse cases; no Git sync.

- Latest tooltip wording/font refinement (2026-09-19): glossary heading/terms/descriptions use SimSun at existing sizes; main name/body remain KaiTi. Format冷却：XX秒，…; hide quality text chip but retain quality mist and footprint text. Do not list hp/stamina/spirit glossary definitions; retain body values/colors. Rename持续药效 to持续 and define毒蚀免疫 as生效期间免除毒蚀效果. Dedicated58 static/render checks pass without mouse suites; no Git sync.

- Latest storage optical alignment (2026-09-19): align the visible left edge of title-chuwudai to the BoardPanel title's22px inset by compensating its42/300 source-alpha left padding inside the unchanged26x26 slot. Storage heading/search,collection,category,sorting and item-card rows use equal22px left/right panel insets. Collection/category buttons retain their widths and use expanding inter-button spacers so first/last buttons land on those boundaries. Card side insets are22px; horizontal and vertical inter-card gaps both remain15px. Dedicated49 headless/render checks pass without mouse cases; no Git sync.

- Latest item tooltip redesign (2026-09-19): shared ItemTooltip for map/battle storage and boards,720px dark summary with96x96 quality-glow portrait,30px KaiTi name,23px effect copy,and an inset brown18px keyword glossary. No top-right ID/count,footprint graphic,info badge or inline effect icons. Use冷却/敌方. User explicitly preserves weapon stamina costs; weapon bodies are not consumables. Pills alone explain bottle consumption. No gameplay or hover timing changes; use dedicated static/render checks without mouse suites.

- Latest storage background transform (2026-09-19): enlarge the current inv-bg-3 display by another2%,making cumulative scale1.0404. From the previous right3/down0 offset move left1 and down2,so final center-relative offset is right2/down2. Keep alpha0.5,transparent item well,200px search,radius14 clipping,mipmaps and no procedural border. Dedicated44 headless/render checks pass without mouse cases; no Git sync.

- Latest storage background opacity (2026-09-19): set only inv-bg-3 StoragePanelBackground alpha0.75→0.5. Keep its total scale exactly1.02 about panel center (do not compound another2%),right shift3px,transparent item well,200px search,radius14,mipmaps and no procedural border. Dedicated44 headless/render checks pass without mouse cases; no Git sync.

- Latest storage background visibility/layout (2026-09-19): narrow the top-right search LineEdit250→200 design px. Make the item display well fully transparent so inv-bg-3 remains visible behind cards and empty space. Enlarge the existing cropped inv-bg-3 display2% about the StoragePanel center,then shift it right3px; keep alpha0.75,mipmaps,radius14 clipping,no procedural rim,toolbar/card geometry and interactions. Dedicated44 headless/render checks pass without mouse cases; no Git sync.

- Latest ten-item roster/support effects (2026-09-19): remove all18 a/b/c test variants, keep six originals, add warm_jade/hemostatic_pill/sinew_pill/detox_powder, each quantity1 and1x1. Jade CD4 restores hp4/stamina6 capped independently at floor(max*2/3), never lowering an already higher resource. Three medicines use one bottle per activation: CD6.5 hp2 every2s x3; CD7.5 stamina4 every2s x3; CD5.5 clears毒蚀 and grants15 combat seconds immunity. Scheduled restoration survives bottle consumption. Existing battle no-writeback boundary retained. Only toxin stacks/application/clear/immunity added; full wood damage/cancellation rules and enemy sources remain unimplemented. Precise retired-ID migration drops equipped variants; formation references remain and missing cells stay empty. See current inventory design; historical24-item guidance below is superseded. No Git sync.

- Latest storage panel background (2026-09-19): use mipmapped inv-bg-3.webp as the910x910 StoragePanel background at alpha0.75. Crop to the authored alpha-used painted bounds Rect2(33,32,1192,1196), stretch to the panel and clip with radius14 so the artwork frame aligns to the container. Remove StoragePanelBorder and set the StyleBox border width0; retain the dark backing,content margins,toolbar,six-column cards,filtering and interactions. Dedicated60 headless/render checks pass without mouse cases; no Git sync.

- Latest square storage geometry (2026-09-19): at the1920x1080 design baseline, make StoragePanel exactly910x910. Reduce the four horizontal gaps by1px each: left/right screen margins24→23 and the two inter-column separations20→19. Keep sidebar208 wide,middle board/formation column718 wide and bottom margin24; resulting x positions are sidebar23,middle250,storage987 with storage right1897. Fixed six-column cards reflow to the wider content area. Dedicated57 headless/render checks pass without mouse cases; no Git sync.

- Latest formation horizontal scale correction (2026-09-19): “收缩2px” means proportional horizontal compression of inv-bg-1 artwork,not insetting/cropping its display rect. FormationPanelBackground again fills the complete718x174 panel; widen only its Atlas sample horizontally so visible artwork scales to714/718 while height stays unchanged. This removes the exposed #0d1c26 side strip. Keep centered1% content enlargement,alpha0.75,radius18 and no procedural border. Board geometry remains x=-7/right=panel+4. Dedicated56 headless/render checks pass without mouse cases; no Git sync.

- Latest board background position / formation edge diagnosis (2026-09-19): move inv-bg-2 right1px from the preceding geometry without resizing; final x=-7,right=panel+4,top0,bottom extension5. The thick dark line at the right of inv-bg-1 is not part of the replacement artwork: the requested2px horizontal inset exposes FormationPanel's retained #0d1c26 backing (with the image's own dark edge beside it). Do not change that formation inset until requested. Dedicated55 headless/render checks pass without mouse cases; no Git sync.

- Latest board/formation background geometry (2026-09-19): move the existing inv-bg-2 display2px left,then extend its moved left/right edges outward5px each; final x=-8,right=panel+3,while top stays0 and bottom extension stays5. Keep board content scale0.970299,alpha0.5,radius13. Inset inv-bg-1 display2px from both left/right edges while keeping top/bottom fixed; raise formation background alpha0.5→0.75,retain centered1% content enlargement,radius18 and no procedural border. Dedicated55 headless/render geometry checks pass without mouse cases; no Git sync.

- Latest inv-bg source refresh (2026-09-19): user replaced inv-bg-1.webp and inv-bg-2.webp in place with visually refined originals. Godot reimported both with the existing mipmap settings; source dimensions remain compatible with the current Atlas regions. Keep formation inv-bg-1 centered1% enlargement/alpha0.5/radius18 and board inv-bg-2 scale0.970299/alpha0.5/radius13 with its established top sample and display extensions. Dedicated54 headless/render checks pass without mouse cases; no Git sync.

- Latest board/card/formation option polish correction (2026-09-19): BoardPanel radius is13px, but restore inv-bg-2 content scale to0.970299; the requested1% enlargement belongs to the inv-bg-1 formation-panel painted region and is centered there, with alpha0.5 retained. Formation record cards remain92x138 with78x78 artwork,now at7px equal left/right/top spacing; all card button states use no frame or fill,while captions remain. The formation name LineEdit and its placeholder use17px matching the save-layout option. That CheckBox supplies matching20px checked/unchecked artwork so unchecking keeps the square and removes only the tick. Dedicated54 headless/render checks pass without mouse cases; checked/unchecked screenshots verified; no Git sync.

- Latest formation panel artwork border (2026-09-19): add mipmapped inv-bg-1.webp behind the718x174 formation-record panel at alpha0.5. Crop to authored opaque painted-frame bounds Rect2(40,99,2052,515), excluding faint outer shadow so the image border aligns to the container edge,then stretch to panel and clip with radius18. Remove the procedural FormationPanelBorder and set StyleBox border width0. Cards/scrolling unchanged. Dedicated48 headless/render static checks pass without mouse cases; screenshot confirms approximately1-2px antialiased painted edge alignment; no Git sync.

- Latest board panel corner radius (2026-09-19): reduce only the middle BoardPanel rounded radius10→8 design px. Keep sidebar18,formation18,storage14,background geometry/opacity and all content unchanged. No mouse checks per user preference; headless editor startup and diff check pass,no Git sync.

- Latest24 seeded items (2026-09-19): retain six original map item definitions/IDs and add_a/_b/_c IDs with lowercase suffix names (e.g.猎弓a). User explicitly chose identical full attributes/art/footprints/quality/enhancement,quantity1 each; acquisition order7..24 for new entries. All24 are real independent loadout/battle items,not stacked display copies. Legacy saves preserve original placements and gain18 newly seeded storage items; formations retain independent identities. Six fixed columns produce four scrollable rows. Dedicated45 data/persistence/static-render checks pass without mouse cases; no Git sync.

- Latest board background bottom-only extension (2026-09-19): extend only the inv-bg-2 display bottom edge another3 design px, changing cumulative bottom extension2→5. Keep top/right fixed,left extension1px,alpha0.5,content scale0.970299 and all foreground unchanged. No mouse checks per user preference; headless editor startup and diff check pass,no Git sync.

- Latest board background edge stretch/opacity (2026-09-19): after cumulative content scale0.970299 and top-anchored sampling, keep the BoardPanel background display top/right edges fixed, extend its left edge outward1 design px and bottom edge outward2 design px, with panel shader clipping overflow. Raise only inv-bg-2 self_modulate alpha from0.25 to0.5. Inner board/foreground unchanged. No mouse checks per user preference; headless editor startup and diff check pass,no Git sync.

- Latest board background top-anchored third scale (2026-09-19): shrink the current inv-bg-2 content by another1%, cumulative scale0.970299. Preserve the previous0.9801 region's top sampling coordinate and horizontal center; extend the Atlas region downward so the visible top position stays fixed. TextureRect still fills718x718,no black inset. No mouse checks per user preference; headless editor startup and diff check pass,no Git sync.

- Latest board background second content scale (2026-09-19): shrink the current inv-bg-2 visual content by another1%, changing the cumulative centered content scale from0.99 to0.9801. Continue expanding only the centered Atlas sampling region while the TextureRect fills the full718x718 panel, so no black inset appears. No mouse checks per user UI-validation preference; headless editor startup and diff check pass,no Git sync.

- Latest board background content scale (2026-09-19): scale inv-bg-2 visual content to exactly0.99 about its center by expanding the centered Atlas sampling region, while the TextureRect still fills the complete718x718 BoardPanel. This avoids the rejected dark/black inset border. Keep alpha0.25,radius10,no procedural rim,title/capacity,inner3x3 board and interactions unchanged. Inventory189 headless/199 render checks pass; no Git sync.

- Latest formation create layout option/actions (2026-09-19): replace the creation explanatory sentence with a visible checked CheckBox labeled按当前阵盘的道具和格位保存. Checked preserves existing current-layout capture; unchecked creates a valid empty formation layout. Edit mode reuses the control unchecked as用当前阵盘布局更新已保存的道具记录. Hide the separate message label unless an error occurs. Swap actions to新建/保存修改 on the left and取消 on the right. Extend save_formation with backward-compatible capture_new_layout default true. Inventory189 and formation39 headless checks pass; real render produced and visually verified formation_create_dialog.png before the known unrelated hover hit-test flake caused3 cascading failures,not rerun; no Git sync.

- Latest borderless board background correction (2026-09-19): revert the4px display inset because it exposed the dark backing as an unwanted black border. The cropped inv-bg-2 display again fills the complete718x718 BoardPanel from(0,0), with neither dark margin,procedural gold rim nor authored gold frame visible. Keep source crop72px,alpha0.25,radius10,title/capacity,inner3x3 board and interactions unchanged. Exact layout revert; no repeated Godot run,git diff check passes,no Git sync.

- Latest board background display inset (2026-09-19): center-shrink the already cropped inv-bg-2 display by4 design px on every side inside the718x718 BoardPanel, exposing an even dark backing edge. Do not restore the removed procedural gold rim or authored gold frame. Keep source crop72px,alpha0.25,radius10,title/capacity,inner3x3 board and interactions unchanged. Inventory187 headless/197 render checks pass; no Git sync.

- Latest formation dialog six-icon fit (2026-09-19): add mipmapped zhen-lianqi.webp as the sixth option炼器. Keep two complete rows of three:攻击/防御/平衡 then修炼/炼丹/炼器. Narrow the dialog from480 to460 design px and remove the600px minimum height; content now determines creation/edit height so only the normal22px bottom padding remains instead of an empty footer. Keep124x130 choice cards,12px gaps,48px input/actions,persistence and existing icon paths. Inventory186 headless/196 render checks pass after one retry for the known unrelated formation-hover hit-test flake; no Git sync.

- Latest board panel background (2026-09-19): replace the middle718x718 BoardPanel background with mipmapped inv-bg-2.webp at alpha0.25. Crop72 source pixels on every side so the asset's authored gold frame is not shown, remove the BoardPanel procedural gold rim, and reduce only this panel's rounded clipping radius from18 to10 design px. Keep the dark backing,22px content padding,title/capacity,inner3x3 zhenpan-bag artwork,drag mapping,formation/storage borders and all interaction unchanged. Inventory184 headless/194 render checks pass; no Git sync.

- Latest health icon optical scale (2026-09-19): after shared full-canvas normalization, enlarge only the sidebar health heart1.1x about the common canvas center. Implement with a centered source-canvas region,not alpha-used-rect trimming; stamina/spirit remain1.0x. Keep all27x27 slots,positions,labels,mipmap filtering and resource behavior. Inventory182 headless/192 render checks pass; no Git sync.

- Latest board-bonus icon scale normalization (2026-09-19): health/stamina/spirit sidebar icons keep their authored equal-size full texture canvases as the common scaling reference inside the same27x27 slots. Remove per-icon alpha-used-rect Atlas trimming, which had independently enlarged differently shaped artwork and made equal source files look inconsistent. Preserve left alignment,7px label gap,mipmap filtering,22px captions,centered44px values and resource logic. Inventory182 headless/192 render checks pass; no Git sync.

- Latest storage filter presentation (2026-09-18): first collection row follows 收藏/常用/灵材/配方/储物袋, with 储物袋 as the total/all filter and recipe as a real optional boolean tag. Second category row supplements 法器/问答/材料/招式 while retaining project-specific weapon/armor/throwable/item categories. Selected collection uses a gold two-pixel rim/glow; selected category uses a blue two-pixel rim/glow with cool-white text; idle categories retain fine dark-blue frames. Inventory179 headless/189 render checks pass; no Git sync.

- Latest formation dialog layout (2026-09-18): dialog width480/min height600,content-driven extra height for editing. Icon rows are攻击/防御/平衡 then centered修炼/炼丹; existing assets unchanged,战斗 caption renamed攻击. Keep124x130 choice cards and12px gaps; input/action minimum heights48,wrap description. Saved icon paths remain compatible. Sixth category采集 is only a suggestion,not implemented. Inventory184 render and formation38 headless pass; no Git sync.

- Latest storage count removal (2026-09-18): remove the bottom-right “显示 N / N 项” label and its entire layout row from the map inventory storage panel. Filtering, empty-state feedback, scrolling and the fixed six-column card area remain unchanged; the released height belongs to the item area. Inventory174 headless/184 render checks pass; no Git sync.

- Latest board resource bonuses (2026-09-18): base.board.bag defines resource_bonuses hp5/stamina5/spirit0. BoardLayout validates optional nonnegative integer bonuses, defaults missing keys to0. MapPlayerStatus and PartyMemberState.maximum/reset_resources add equipped-board bonuses without rewriting character base stats; same board gives the same bonus to any actor (current dog also uses bag). Map starts55/55 HP,55/55 stamina,0/0 spirit; hero battle HP75 with existing+20 companion. Sidebar shows three rows below motto, existing27px resource icons +22px KaiTi captions and aligned +5/+5/+0 column. LegacyCombatFixture clears bonuses to preserve historical baselines. Header57,loadout58,armor33,legacy1117 headless and inventory182 render pass; no Git sync.

- Latest inventory title icons/formation card lock (2026-09-18): replace the storage/bag/sidebar heading bars with26x26 mipmapped title-chuwudai/title-zhenpan/title-zpsxjc icons. Titles are储物袋,阵盘,属性加成; motto is天地灵物，纳于一盘. Formation card artwork occupies a fixed78x78 slot independent of one/two-line names; names use tighter -6 line spacing. The record action uses title-zhenxing in the artwork slot,then a bold+ line and阵型记录 line below. All four title assets use LINEAR_WITH_MIPMAPS. Inventory170 headless/180 render and formation38 headless checks pass; no Git sync.

- Latest map item exchange/fixed storage slots (2026-09-18): storage-to-board drag may swap iff its entire footprint fits and overlaps exactly one distinct equipped instance; light-blue preview (0.55,0.8,1,0.32). Empty=green, two-or-more overlaps/out-of-bounds=red. Build replacement inventory before mutation, return the whole displaced unit to storage, commit once; board-to-board and random right-click rules stay unchanged. MapInventoryGrid reserves six equal fractional-width columns regardless of visible count, preserving15px gaps and16px outer insets; wheel scrolling hides the vertical bar so overflow cannot resize cards. Loadout58 headless/render and inventory160 headless/170 render checks pass. No Git sync.

- Latest formation record presentation (2026-09-18): remove the separate vertical阵型 title. Center the formation-card strip vertically in the718x174 panel with equal top/bottom space; the add card reads阵型记录 in the same F9F8C8 as the bag heading. Card names use15px,up to2 lines,and ellipsize at the end of line2; icon holders remain square/borderless (78px in cards). The3s edit reveal starts on the first mouse-motion entry into the card and no longer depends on tooltip/GUI hovered-control state; true exit,focus loss,drag or modal opening resets it. Inventory150 headless/160 render and formation38 headless checks pass; no Git sync.

- Latest portable formations (2026-09-18): formation layout contains only placements (stable instance_id/item_id/cell), no board ID/size/skin. Normalize legacy version1 board-bound records by discarding board metadata. Keep row-major top-left coordinates and holes on larger boards; skip no-longer-owned units without substituting same-type copies or erasing saved references. Equipped units remain owned. Structural validation on record load is separate from current-board fit validation on apply; insufficient space/overlap rejects application atomically. Current top-level loadout keeps strict board validation; no new acquisition/deletion gameplay. Formation38,inventory145,loadout39 headless checks pass; no Git sync.

- Latest persistent formations (2026-09-18): title now vertical阵型,26px regular KaiTi,F9F8C8; remove all presets. Saved cards capture current owned-item IDs/cells and restore validated layouts on click. Multiple records live in optional formations array of backward-compatible version1 inventory_loadout.json; create/edit/apply save immediately through existing atomic store,failed writes roll back with visible error. Card hover3s reveals gold three-line edit button; default edits name/icon only,explicit checkbox updates saved layout. Reset hover on exit/focus loss/modal/drag; clipped cards cannot receive clicks; wheel scrolls overflow without resizing panel. Five original icons remain choices; no bonus effects or battle writeback. Formation25 render,inventory145/154,loadout39 headless pass; no Git sync.

- Latest formation configuration UI (2026-09-18): map inventory formation title is vertical; the three presets and one custom slot are fixed2:3 cards. Card icon areas are square and add no extra border. Clicking 新增阵型 opens a modal with five mipmapped zhen-*.webp choices, a required custom name, 取消 and 新建; creating replaces the fourth slot for the current runtime session. Esc/I close the modal before the inventory. This is UI/session state only: no battle effects or persistent save. Inventory149 headless/156 render and loadout39 checks pass; no Git sync.

- Latest storage spacing value (2026-09-18): STORAGE_CARD_GAP=15 design px for horizontal/vertical gaps; side insets=16 including approximately1px container gold stroke, leaving15px clear space. Keep6 columns and optical frame bounds x27..517; cards shrink uniformly to fit. Inventory129 render checks pass; no Git sync. Supersedes4px/5px values below.

- Latest optical card spacing correction (2026-09-18): level-fanpin alpha bounds include a dark halo; fit its visible metal sides using source x27..517 instead. Keep6 columns and4px grid gaps; set side insets5px to account for approximately1px of the container gold stroke, equalizing visible clear space. Preserve source pixels and composition. Inventory129 render checks and screenshot edge sampling pass; no Git sync. Supersedes alpha-bounds/4px outer inset claim below.

- Latest six-column storage (2026-09-18): storage grid now6 columns,4 design px horizontal/vertical gaps and4px left/right card-area insets. Toolbar/count retain14px horizontal margins separately. Fit cards by frame.get_image().get_used_rect(),removing source alpha padding and old CARD_SCALE0.98; expanded slots absorb pixel rounding remainder. Preserve source pixels,internal composition and other panels. Inventory129 render checks pass; no Git sync.

- Latest capacity typography (2026-09-18): bag heading capacity caption and occupied/total value share the same Microsoft YaHei/Noto Sans CJK SC font stack. Keep caption16px, value17px and8px gap; brighten 容量 from muted85989f to c5d0d2 while value stays fff0bd. Layout/counting behavior unchanged. Inventory129 render checks pass. No Git sync.

- Latest four-panel borders/corner AA (2026-09-18): bag radius restored18; inv-zhenpan grows another3% about its current artwork center after all previous transforms,alpha0.25 retained. Sidebar/bag/formation/storage share Head d9bb72 and1.2px stroke; radii18/18/18/14. Replace visibly aliased Line2D with map_panel_border.gdshader analytic rounded-distance pixel coverage and2px apron. Replace hard child masks with map_inventory_panel_art.gdshader rounded alpha clipping,Atlas UV correction and preserved sidebar64px top fade. No image overflow or layout changes. Supersedes32px bag/Line2D rules below; inventory128 render checks pass and magnified before/after verified. No Git sync.

- Latest item presentation rule (2026-09-18): every present and future map inventory item artwork must have imported mipmaps; MapInventoryCatalog rejects missing/invalid/non-mipmapped icon resources and MapItemArtwork uses LINEAR_WITH_MIPMAPS. All six current items use art_outline_px=1 with shared softened near-black #151515, visible in storage, board and drag preview. Weapon cards show three chips: 武器+剑+斩击, 武器+弓+穿刺, 武器+锤+钝击; generic category remains authoritative for filtering. Original WebP pixels, geometry, stats and saves unchanged. Inventory123 headless/128 render and loadout39 pass. No Git sync.

- Latest inventory item mipmaps (2026-09-18): enable imported mipmap generation for weapon-qsdj/lg/dbtc and armor-cby/jtk/myd; MapItemArtwork explicitly uses LINEAR_WITH_MIPMAPS for the source-art TextureRect. This fixes jagged edges when the high-resolution transparent WebP art is reduced inside storage cards and also benefits shared board/drag rendering. Original WebP pixels, geometry, outlines, stats and saves remain unchanged. Godot reimport succeeded and map inventory114/114 headless checks pass. No Git sync.

- Latest bag panel border (2026-09-18): restore BoardPanel outer rim using shared MapStatusHeader.LINE_COLOR d9bb72 and LINE_WIDTH1.2 design px. Closed antialiased Line2D above artwork,32px outer radius,0.6px center inset; retain layout,background transform/alpha0.25 and clipping. Supersedes image-only border. Render visually verified; current119 checks include3 unrelated work-in-progress mipmap failures. No Git sync.

- Latest sidebar brightness seam fix (2026-09-18): width-fit/bottom-aligned inventory-sidepic leaves76.58 design px of plain fill above its hard top edge. Fade its top64 design px with map_sidebar_fade.gdshader smoothstep into retained alpha0.25. Preserve art geometry/source, panel fill/rim and return button. This corrects the missed image-edge cause in the preceding border diagnosis. Inventory119 render checks pass; no Git sync.

- Latest hunting-bow outline width (2026-09-18): supersedes the2px/3px rules below. base.map_item.hunting_bow art_outline_px=1, using one radius with16 fractional directions and linear mipmap filtering. Preserve corrected local glow→outline→source order so static storage, equipped board and map drag preview all show it. Source pixels, geometry, other items, stats and saves unchanged. User explicitly requested no repeated tests; not run. No Git sync.

- Latest inventory alpha/bottom extension (2026-09-18): inv-zhenpan and inventory-sidepic self_modulate.a=0.25. After prior background transform add3 design px to inv-zhenpan height only,keeping top/left/right fixed and rounded clipping. Sidebar has one visible1px gold rim plus separate return-button artwork rim; no duplicate visible panel border,neither removed. Inventory119 render pass,no Git sync.

- Latest inv-zhenpan vertical stretch (2026-09-18): after existing geometry, increase height1% about vertical center; width/x unchanged. At718:746.9354x754.404754 at(-16.1654,-18.202377). Use STRETCH_SCALE; retain alpha0.75,radius32 clipping,global16:9. Inventory119 render pass,no Git sync.

- Latest global aspect lock (2026-09-18): project.godot stretch/aspect=keep with1920x1080 viewport andcanvas_items. All map/inventory/battle content stays16:9 with uniform scaling and engine black bars on mismatched windows; never expand logical canvas to window ratio. Preserve existing window mode2. Supersedes prior wider-window adaptive positions in normal runtime. Source assets/saves unchanged,no Git sync.

- Latest hunting-bow outline correction (2026-09-18): supersedes the3px rule below. base.map_item.hunting_bow now has art_outline_px=2. Keep glow, outline and source art in MapItemArtwork local behind-parent order (glow→outline→source), with NO negative z that can slip behind static card/board backgrounds. Build the smooth outline from radii1/2,16 fractional directions each, linear mipmap filtering. It must show in map storage, equipped board and map drag preview; preserve weapon-lg source pixels, icon geometry, other items, stats and saves. Inventory114/119 and loadout39/39 pass; no Git sync.

- Latest inventory background opacity (2026-09-18): inv-zhenpan BoardPanelBackground and inventory-sidepic sidebar_art both use self_modulate.a=0.75. Overrides prior0.5 for inv-zhenpan; keep geometry,clipping,foreground and backing unchanged. Inventory119 render pass,no Git sync.

- Latest bag scale correction (2026-09-18): cancel horizontal3% expansion. From previous centered1.03 size plus right2px, uniformly scale another1.01, anchoring right edge and vertical center. At718 panel:746.9354 square,origin(-16.1654,-14.4677),right730.77. KEEP_ASPECT_CENTERED,radius32 clipping,alpha0.5 retained. Inventory119 render pass,no Git sync.

- Latest bag background clipping/geometry (2026-09-18): BoardPanel uses CLIP_CHILDREN_AND_DRAW with retained opaque flat fill; radius32 design px matches approx46px source gold corner. Background retains alpha0.5 and prior1.03 height,then shifts right2 and expands current width another3% leftward with shifted right fixed (761.7262x739.54 at(-30.9562,-10.77) for718 panel). STRETCH_SCALE implements horizontal-only widening. Mask hides all overflow; foreground/margins/layout unchanged. Inventory114/119,loadout38 render,startup pass,no Git sync.

- Latest hunting-bow outline (2026-09-18): only base.map_item.hunting_bow has art_outline_px=3. MapItemArtwork draws a rounded3-design-pixel black silhouette behind the trimmed bow and above its quality glow, shared by map storage card, equipped board art and map drag preview. Do not alter weapon-lg.webp source pixels, icon size/position, footprints, stats, saves, or other items. Validate outline width as a positive integer. No Git sync.

- Latest inv-zhenpan opacity (2026-09-18): BoardPanelBackground.self_modulate.a=0.5; only artwork/painted rim fade. Retain original flat backing,no procedural border,centered1.03 scale and foreground opacity. Inventory114 render pass,no Git sync.

- Latest bag panel correction (2026-09-18): retain original BoardPanel and0d1c26 flat fill,remove only procedural border. inv-zhenpan is now a separate input-ignored TextureRect above fill/below content,centered at1.03 times container size (739.54 square at718 baseline,offset-10.77). Keep22px margins and interactions. Supersedes StyleBoxTexture replacement below. Inventory109/114 and startup pass,no Git sync.

- Latest bag panel artwork (2026-09-18): BoardPanel718x718 now uses inv-zhenpan.webp via StyleBoxTexture replacing both old flat fill and gray border. Centered square Atlas encloses full used rect, preserving source aspect/painted rim/transparent corners. Keep22px content margins,heading/capacity,inner zhenpan-bag geometry and drag mapping. Full original artwork opacity; map-head30% remains independent. Inventory109/114,loadout38 render and startup pass,no Git sync.

- Latest header background (2026-09-18): add assets/map-head.webp as HeaderBackgroundArtwork above existing fill, below ornaments/line/foreground. Trim transparent padding with Atlas, aspect-cover centered in design rect x527.36..1920,y0..CONNECTION_Y; self_modulate.a=0.3,mouse ignored. Preserve Head scaling, foreground opacity, source pixels and no backing below gold rule. Header53/56,inventory114 render and startup pass; no Git sync.

- Latest inventory middle-column geometry (2026-09-18): the outer BoardPanel, not its inner bag artwork, is now square at718x718 design px. Preserve body height910, BoardPanel height and left edgex252; narrow only its right edge by62. FormationPanel follows the same718 width. StoragePanel absorbs all released width on the left, moving fromx1052 tox990 while retaining right edgex1896 and the20px column gap. Inner zhenpan-bag artwork remains aspect-preserved/centered. Uniform fixed-canvas scaling, interactions, saves, assets and no-Git-sync remain unchanged. Read docs/map-inventory-design.md.

- Latest storage quality glow (2026-09-18): only MapInventoryItemCard enables MapItemArtwork storage_quality_glow. Centered136px circular wash, lower-quality下品=b4c6dc,alpha0.24,inner_radius0.18,falloff_power1.4; alpha reaches0 at outer rim,no outline. QUALITY_GLOW_COLORS keys quality,not enhancement; only current下品 mapped,unknown neutral fallback. Shared shader defaults preserve board/drag glow. Inventory98/103 and startup pass,source pixels/saves unchanged,no Git sync.

- Latest sidebar text/return scale (2026-09-18): sidebar bag_caption is now the complete single-line行囊属性加成,26px regular KaiTi andfff0bd like the three section titles; no board-name prefix or FangSong bold. Motto retains17px KaiTi but alsofff0bd. Return artwork grows another20% about its previous center (cumulative1.44 original), widening and heightening button inside retained56px layout slot; source art/sidebar dimensions unchanged. Inventory98/103 and startup pass; no Git sync.

- Latest inventory headings/capacity (2026-09-18): remove synthetic bold from all three section headings; retain26px KaiTi,fff0bd and markers. Replace bag3x3 label with muted16px容量 and bright17px occupied/total (8px gap). Read InventoryState.occupied_cells() and actual board dimensions, refresh on loadout.changed; count cells, not items. Inventory98/103 and loadout38 render checks pass. Existing project.godot user edits preserved; no Git sync.

- Latest inventory five-column layout (2026-09-18): 1920x1080 baseline keeps left/right/bottom24px. On wider windows move sidebar to scaled24px left margin and extend storage right, retaining middle position and uniform component scale; taller windows retain vertical spare space. Five cards per row,6px horizontal/8px vertical separation,CARD_SCALE0.98; size cards from visible content area, horizontal scroll SHOW_NEVER avoids disabled-scroll content minimum overflow. Three section headings share26px KaiTi,fff0bd,FontVariation embolden1.2 and3x23px marker; remove bag heading rule. Return artwork grows20% about its center in the existing56px slot. Inventory98 headless/103 render and loadout38 render pass. Preserve source assets, saves and no Git sync.

- Latest board item artwork scale (2026-09-18): reimport same-path weapon-qsdj/lg/dbtc and armor-cby/jtk/myd assets. ItemDragPreview.BOARD_ICON_SCALE=0.9 and artwork_rect center-scale the previous9px-inset image area by90%; both map board and battle boards/drag previews share this. Keep logical footprint, placement hints, CD/quantity markers and storage-card image sizing unchanged. Preserve source pixels, saves and no Git sync.

- Latest armor pool/items/ward/background (2026-09-18): actual runtime now consumes armor before HP, with NO fixed per-hit defense. Initial armor/base cap0; equipment cap bonuses stack immediately, increasing cap grants no current armor, removal clamps current to new cap. Three new 下品 armor records in data/maps/inventory_items.json: 粗布甲 armor-cby, 衣甲/light轻甲,1col×2rows,CD4/gain3/cap7; 旧铁盔 armor-jtk,头盔,1×1,CD5/gain1/cap2; 木圆盾 armor-myd,盾,1×2,CD5/gain3/cap2. Only衣甲 has armor_type; no armor-type damage coefficients yet. No stamina costs. Ward is now ACTIVE restore_armor2 every5s, no cap bonus. Battle insertion waits3s then full rotation; board moves retain progress. Actual F6 uses six seeded owned items with compatible version1 saves, preserving old positions; battle never writes back. Armor current/cap/type shown below hero resource bars. Legacy fixed-defense/old ward exist only via explicit tests/legacy_combat_fixture.gd; do not mistake1117 old regression checks for new combat-design coverage. New armor33 checks, inventory98 headless/103 render, loadout38 in both modes pass with remaining full runner checks. InventoryBackground uses inv-background.webp alpha0.5, aspect-preserved cover BELOW Head behind panels, input ignored. Preserve source assets/history; no Git sync. Read docs/map-inventory-design.md and combat-system-design-2026-09-18.md section12 for actual scope.

- Latest inventory sidebar/uniform fit (2026-09-18): clear faction_caption but retain40px FactionReserved. BoardLayout.display_name reads board JSON name; base.board.bag now named普通行囊, stable ID/save unchanged. Sidebar renders board name then属性加成 on separate lines in24px FangSong with0.6 synthetic embolden; motto17px KaiTi in dec995 with6px rule gap. Draw1px rounded gold rim c9ac70 alpha0.35 above sidebar art, remove gray border; preserve original art width-fit/bottom anchor. Fix resize drift by a fixed1920x1080 inventory canvas/body1872x910, uniformly fit/center below width-scaled Head; nonmatching aspect leaves spare background space instead of resizing panel height. Header/map scaling unchanged. Regression92 headless/97 render inventory checks across16:9,16:10,ultrawide and35 loadout checks pass. No Git sync.

- Latest map loadout persistence/battle handoff (2026-09-18) supersedes pending/no-drag/reset-on-restart statements below: user confirmed bidirectional storage↔3x3行囊 drag, board moves, right-click random equip/return, and persistent last-closed layout used by battle. MapLoadoutState owns InventoryState/SharedStorage, canonical three weapons from data/maps/inventory_items.json adapt into ordinary ItemData without duplicating stats or migrating target combat mechanics. UI projects only unequipped records; previews show full aspect-preserved icon/glow at board size centered on pointer, nearest complete footprint green/red, exact drop or reject. No map CD; only successful moves mutate ownership. Close/focus-loss cancels uncommitted drag. MapLoadoutStore saves version1 stable-unit/item/board IDs and cells to user://inventory_loadout.json using.tmp/.bak; I/Esc/return and orderly exit save, restart restores, empty board is valid, corrupt saves report without overwrite. Ownership is currently fixed three seeded weapons; future acquisition/deletion/enhancement requires schema expansion. Actual scenes/main/main.tscn enables use_saved_loadout, replacing old hero gear/storage with snapshot and remainder; retain dog/companions and ordinary battle rules, never write temporary battle changes back. No encounter transition, resource transfer, full saves or rewards. Tests explicitly isolate paths/disable persistence; legacy combat tests opt out of snapshot mode. See docs/map-inventory-design.md. Preserve historical records and no Git sync.

- Latest map sidebar art (2026-09-18): inventory-sidepic.webp438x1755 uniformly fits sidebar width and anchors bottom; at1080p208x833.42 with76.58px unfilled top, original panel color retained. Rounded panel clips art, art ignores input. ReturnToMap now uses button-return.webp via existing BattleActionButton aspect/press feedback,56px slot, no overlaid caption; original close signal preserved. Source art unchanged; removed bottom verse stays removed. Map equipment scope still pending. No Git sync.

- Latest map bag caption (2026-09-18): map I-screen 3x3 panel title changes 阵盘→行囊. Remove the sidebar bottom verse 大道藏于万物 / 修行自有机缘; retain other labels and battle terminology. Previous map equipment interaction scope question is still unanswered, drag remains unimplemented. No Git sync.

- Latest card styling / pending map equipment (2026-09-18): map cards scale artwork uniformly0.92 within four-column slots; weapon image center down10 design px, bold Song name y151/h28 and category tags y181 reduce gap to2px. MapItemArtwork adds faint radial gray-blue glow fading to transparent at all edges. Preserve source art. Sidebar208x910 at1080p (~1:4.38). Four-card render fixture is test-only, actual inventory remains three. Map equipment interaction requested but NOT implemented yet: awaiting user's scope confirmation (map-only bidirectional drag/right-click, no cooldown, session layout, restart reset). Do not report task fully complete. No Git sync.

- Latest item quality/footprint correction (2026-09-18): remove the extra upper-left 凡品 label from map cards. Three current weapons now quality 下品 (filters/tooltips follow), retaining level-fanpin.webp art, weapon/subtype tags and enhancement0. User defines footprint notation as COLUMNS × ROWS, superseding all earlier row-first notes: 1×2 means columns1/rows2, vertical mini-cells at upper right. Store actual footprint_columns=1, footprint_rows=2 and show column-first tooltip. Keep legacy battle definitions and source art unchanged. No Git sync.

- Latest compact storage toolbar and enhancement sorting (2026-09-18): map storage uses four compact rows (title/search, collections, categories, quality/level/acquisition), 26px title, 14–15px controls, 28–30px button heights, no button outlines. Preserve Head/left/board layout. User defines level as future enhancement level, currently0 for all three weapons. Optional enhancement_level is a validated nonnegative integer, absent means0. Level and acquisition buttons select exclusive primary sort and toggle directions; level ties use current acquisition direction then stable ID. Reset defaults to acquisition newest first/level descending; tooltip shows enhancement separately from quality. No strengthening gameplay or stat bonuses implemented. Focused map inventory checks81 headless/84 render and startup pass. No Git sync.

- Latest map weapon categories (2026-09-18): three map display records share category weapon, now labeled 武器 in filters and cards; optional nonempty subcategory is 剑 for 青石短剑, 弓 for 猎弓, 锤 for 短柄铁锤. Cards show rounded main-category/subcategory chips and tooltips include both. Keep 斩击/穿刺/钝击 separately as 伤害类型 in tooltip, not category chips. These records have no elemental attribute and display no element/无属性 chip. Preserve stats, artwork, footprints and map/battle separation. No Git sync.

- Latest bag board and map weapons (2026-09-18): user confirmed map AND both current battle arrays use 3 rows x 3 columns with zhenpan-bag.webp. base.board.bag uses source1230x1278, x lines184/470/766/1052 and y280/548/826/1104. BoardLayout exposes grid_size; PartyMemberState passes it into InventoryState, InventoryView and random placement use instance dimensions, no invisible fourth row/column. Old 4x4 skins and historical mechanics fixtures retained explicitly. Current sword/armor/claw placements still fit; two vertical swords plus2x2 armor do not. Map inventory now seeds three display records from data/maps/inventory_items.json, each quantity1/common quality level-fanpin frame: 青石短剑 slash7/CD3.5/stamina3; 猎弓 pierce9/CD4.5/stamina5; 短柄铁锤 blunt8/CD4/stamina4. New footprint rows1/columns2 are horizontal mini-cells at card upper right; do not reinterpret legacy ItemData width/height or rotate old sword. Newly supplied weapon-qsdj/lg/dbtc art is attached to sword/bow/hammer using trimmed aspect-preserved Atlas images, tags follow reference, all numeric attributes in hover text. Map records do not yet equip into battle; no new damage pipeline or balance migration. Preserve art, historical progress, and no Git sync.

- Latest map inventory framework (2026-09-18): map-only I opens MapInventoryScreen; I/Esc/返回游历 closes, except typing I in search remains text. Preserve the same Head/state, hide only map location plaque while open, pause travel/sprite and block map click/pan/zoom, restore original journey/camera on close. Active dialogue/illustration blocks opening. Four sections in three columns: left text/property reserve, middle empty hero bag-bg-1 board above same-width horizontal disabled formation placeholders, right empty 储物袋 with search/collection/category/quality/acquisition sorting. No level filter, no training button, no sample items in normal game. MapInventoryCatalog is a separate validated display/filter model; no battle inventory, equipment transfer, formation mechanics, rewards or saves wired. Left 青鹤宗/元婴期储物袋 captions are reference placeholders, not player rank/faction. See docs/map-inventory-design.md and data/ui/map_inventory.json. Run map_inventory_smoke (headless/render) for actual keys, modal isolation, movement continuation, filtering and layout. No Git sync.

- Latest combat design documentation (2026-09-18): current entry is docs/combat-system-design-2026-09-18.md; verbatim source docs/sources/combat-design-input-2026-09-18.md. Documentation ONLY; no runtime migration. Supersedes September17 uniform decay: every status has its own lifecycle. Metal反锋/锋痕 store one-shot damage (反锋 requires physical damage past shields); wood生机/毒蚀 and water润脉/枯脉 wait2s then tick current stacks and lose1/tick, active-pool additions do not restart; 毒蚀 bypasses thunder shield/armor. Fire附炎 spent at attack launch even on miss,灼烧 deals stacks/s for10s, reapplies add stacks/refresh10s, goes through thunder shield→armor→HP. Earth坚韧/疲惫 are crit resistance/vulnerability, wind轻灵 clears on dodge and失衡 grants one independently hit-tested50% pure-weapon follow-up, clearing on opportunity even if miss. Thunder雷蕴 consumes10 stacks to add to ONE persistent shield pool;雷印10 arms for thunder weapon/spell,雷击=1.5x highest-attack weapon in current board plus麻痹(board halt2s), shield break also paralyzes breaker. Pool gain/cap,雷印 consumption, weapon reference ownership/basis and no-weapon case remain open. Resonance is active elemental damage only, snapshot before launch; derived damage does not re-read resonance or recursively trigger derived damage. Weapon-shaped spells remain spell sources unless explicitly tagged as weapon attacks. 连携 is post-normal-settlement effective second-spell damage, no elemental/racial/resonance modifiers on bonus; window2s is TENTATIVE. Source44-section pipeline is illustrative, not final. Keep pending boundaries in section11, actual implementation in section12, all historical reports and no-Git-sync policy.

- Latest combat design documentation (2026-09-17): read docs/combat-system-design-2026-09-17.md before future combat planning; source archived at docs/sources/combat-design-input-2026-09-17.txt. This is documentation-only target design, NOT implemented or authorization to migrate runtime. Covers seven elements (thunder→water→fire→metal→wood→wind→earth→thunder), consumable armor, spell-priority thunder shield, cancellable/decaying status stacks, manual main spells, realm-gated 连携, roots and battle-only nonspendable experience. New insertion waits3/2.5/2/1.5/1/0 replace old DESIGN values; runtime still fixed3. New row-major simultaneous item rule and manual spells conflict with older rules; unresolved interaction details stay explicit. Latest text reopens final damage/armor matrix despite approved historical September13 trial coefficients; preserve all old reports/data. Do not promote suggested body-armor type authority, tentative root efficiencies, thunder damage or sample 连携 multiplier to final rules. Shield terminology, pause/reequip, consumables, postbattle clearing and failure conflicts are listed in sections12–13 for later clarification. Current Godot behavior, player defaults and no-Git-sync remain unchanged.

- Latest actual player defaults (2026-09-16): chen_yu.json now mortal, max/initial HP50, stamina50, spirit0. MapPlayerStatus reads rank and optional stage only from the character definition; remove map header's qi_refining/一层 overrides. Header shows凡人,50/50,50/50,0/0 with empty spirit track (visual Range max at least1; authoritative spirit cap stays0). Map cultivation remains0/100 for mortal, experience0, spirit_stones0, age15/lifespan80 retained. Future victory cultivation/experience rewards and progression are planned, not implemented; no map/battle runtime transfer or saves yet. Standalone battle shares new base defaults but keeps companion effects (existing+20 HP yields70 in that roster). Map player-level-1 frame unchanged; battle uses actual mortal pt00. No Git sync.

- Latest map portrait frame (2026-09-16): replace procedural gold Line2D rim with player-level-1.webp, keeping portrait position/size and PortraitGroup transform. Source543x610 has ring center(271,300); centered Atlas[16,45,510,510] aligns actual ring and portrait centers, uniform displayed size113.333 before group scale1.05. Portrait radius48 overlaps opaque rim (source radius216 calibration), circular mask retained. Frame renders above portrait, mipmaps enabled and LINEAR_WITH_MIPMAPS. Only map header frame changes; source images/battle rank frames unchanged. No Git sync.

- Latest counter/plaque offsets (2026-09-16): currency/experience groups move right10 to iconsx1735/captionsx1767; numbers move an additional10 (caption prefix advance +15 total), so values move20 overall. MapTitle moves down20 to(18,128), size/aspect unchanged. Overrides previous positions only; no Git sync.

- Latest hanging map title (2026-09-16): remove the map SettingsPlaceholder entirely, including its remaining three-line glyph; battle settings unchanged. Add MapTitle using original map-title-qshw.webp (362x1086), Atlas[46,0,236,1031], position(18,108), uniform scale0.36 (84.96x371.16 at1920 design width). Draw before head3 so the preserved top cord tucks behind the lower frame. Enable asset mipmaps and LINEAR_WITH_MIPMAPS; fixed under header content, ignores mouse, persists during dialogue and does not follow map pan/zoom. No new backing below the gold rule, source art unchanged. No Git sync.

- Latest header spacing (2026-09-16): head3 and contained foreground shift left2: origin(-35.32,-19.12), PortraitGroup=(-12.805,-5.805), identity rect(178,22,320,62), age rect(186,92,244,22). Four resource groups shift left10 to rowx560, width1140 unchanged. Currency stack shifts right5 (iconsx1725/captionsx1757); amounts are separate Labels positioned another5px beyond former prefix advance, while state refreshes names and numeric values separately. Settings placeholder uses StyleBoxEmpty, keeping only three-line gold glyph without rounded backing; disabled behavior unchanged. No Git sync.

- Latest identity centering/right rule (2026-09-16): PortraitGroup moves another2px left (position=-10.805,-5.805; scale1.05 retained). Name/rank now share HBoxContainer gap18 inside transparent CenterContainer rect(180,22,320,62), centered as a group both axes; age/lifespan centered in separate transparent CenterContainer rect(188,92,244,22). No drawn container borders/backgrounds. Preserve fonts/content. Draw connection after both head3 and head2 (before foreground content) so head2 alpha/color cannot interrupt gold rule; retain left fade and endpoint1920. No Git sync.

- Latest protagonist identity (2026-09-16): base.character.chen_yu stable ID retained, display name now张辰宇 (also inherited by battle UI). Map status config rank base.cultivation.qi_refining plus cultivation_stage="一层" displays炼气 · 一层; lifespan80, age15 unchanged. Stage is display metadata, no progression mechanics or global rank-name rename; battle retains its existing rank-only display. Map name rect(190,25,156,58), rank rect(352,31,148,34), retaining48/25px fonts to fit longer identity. No Git sync.

- Latest portrait/counter stack (2026-09-16): map portrait+rim grouped and scaled1.05 around previous outer-rim bottom-right(176.1,116.1); PortraitGroup position=(-8.805,-5.805), scale1.05, preserving bottom/right anchor. First3 resource columns remain232px each atx570/826/1082; resource row width1140 makes cultivation bar372px endingx1710 (old spirit-stone number edge). Cultivation caption is修为 + one space + rounded integer percent, refreshed from state with raw current/max and bar; default修为 0%. Currency stack at old experience column x1720:25px icons,20px text x1752, rows separated32px, total57px centered above gold rule. Preserve zero values and no accumulation rules. No Git sync.

- Latest header join gradient (2026-09-16): head3 moves another4px left to(-33.32,-19.12), retaining size/fonts/portrait and gold-rule y. Header fill blends to#142230 at head3 right edge x529.36 and smoothly returns to preceding center gradient over360px; preserve right-side gradient. Connection Line2D has short64px alpha fade from its left start (first12px transparent) then full gold through screen right edge. Use explicit collinear vertices at0/12/64/end: Line2D gradient otherwise interpolates only endpoints across the entire line. No fill below rule; map boundary unchanged. No Git sync.

- Latest gold-rule boundary correction (2026-09-16) overrides128px/deepest-ornament reservation: map/header boundary is the horizontal gold rule, CONNECTION_Y≈90.942px at1920 width. height_for_width uses exact proportional rule height. MapViewport stays clipped below that line. Remove full-width underlay and all generated fill below rule, including head3 lower-left patch; fill is a rectangle ending at rule. Original head3 art/portrait/name strip may protrude over map below rule without added backing; preserve source images and their placement. Retain map coordinate/zoom/input adaptation. No Git sync.

- Latest separate map viewport (2026-09-16): header now reserves a full-width strip down to its deepest head3 edge (ceil127.418=128px at1920 width). MapContent is under Control MapViewport with clip_contents=true; viewport starts exactly at header bottom and fills remaining screen. Header underlay extends navy across its full reserved height. MapScreen fitting/clamping/initial framing/resize focus use viewport size, map_to_screen adds viewport offset for hover/node clicks, zoom subtracts it. Block all map input above viewport; event/dialogue overlays remain full-screen and header stays visible. Keep width-based100%-175% zoom/default125%, source aspect, manual camera and movement rules. For short vertical artwork, align top to viewport rather than center vertically. Editor layout preview still uses full view without runtime header. Updated map UI tests use coordinate helper/local viewport sizes. No Git sync.

- Latest header pixel polish (2026-09-16): head3 origin now(-29.32,-19.12), left5/down2 from prior; width562.68 (5px narrower) via uniform scale562.68/887, preserving aspect. Its internal rule/connecting baseline is~90.942px. Extend gold line to x1920 and fill navy continuously to this straight baseline under head2, no silhouette gaps. head2 right edge1921 and tip y90.92 give exactly right1/down1 from prior layout. Portrait/rim move up4 (portrait78,18; rim center126,66), age/lifespan15px and rank25px. Currency icons player-status-coin/exp appear in27px slots at x1598/1720, names+zero labels shifted34px right; both enable mipmaps and linear mipmap sampling. All positions at1920 reference width. No Git sync.

- Latest map header expansion/counters (2026-09-16) supersedes460px head3 and22px age rules: keep head3 Atlas[7,45,887,231], scale0.64, origin(-24.32,-21.12), putting source inner rules x45/y78 on viewport edges and cropping outer first rules. Visible right reaches543.36; bottom126.72. Internal rule/connecting line is y89.92; head2 and resource groups track it. Age/lifespan16px in bottom strip, rect(204,92,258,22), centered; name/rank remain48/24. Four resource columns now x570,width1000,separation24 (~232px tracks). Cultivation caption is修为 with player-status-level.webp, also mipmapped. Add state/config spirit_stones=0,experience=0 shown as灵石/历练 alongside caption rows at x1598/1720; no icons/add buttons yet and no earnings rules. Far-right60x44 rounded menu glyph is disabled settings placeholder, no settings UI implemented. Preserve source images, modal/input rules, and no Git sync.

- Latest head3/mipmap update (2026-09-16): user chose full map-head3 frame around portrait+identity,460px wide (not old230px ornament). Source900x350, cropped Atlas[7,45,887,231], placed(0,6); internal rule source y218.5 maps to CONNECTION_Y=6+173.5*460/887 (~95.98). LEFT_TIP=(803,173.5) joins line over frame fill; head2 remains43px high and tracks line height. Preserve aspect and source art. Portrait96px at(78,22), rim center(126,70)/radius49; identity name/rank/age sizes now48/24/22, positions(202,25)/(332,31)/(204,64), previous colors/fonts retained. Status groups center within new header line height. All3 player-status textures generate mipmaps and their TextureRects use LINEAR_WITH_MIPMAPS, leaving global filtering untouched. No Git sync.

- Latest map identity typography (2026-09-16): follow the user's cropped identity reference. At1920 design width, name uses50px KaiTi with0.4 embolden and f9f6d3 at(202,6); rank uses26px bold700 Noto Serif SC/Song fallback, ece6b5 at(332,12); age/lifespan uses24px weight600 serif, d6d7d3 at(204,52), separated by a spaced vertical bar. Retain actual prologue values (mortal,15,60), not reference sample values. Font colors sampled from reference bright text; never bundle system font files. Other header geometry/resources stay unchanged. No Git sync.

- Latest map header polish (2026-09-16) supersedes preceding ornament sizes and no-age rule: left art width230/right art height43 (50% of previous), flush to respective screen sides; keep horizontal connection y85.326 at1920 reference. Gold line width1.2 extends3px under both tips. Fill is opaque #0b1320 around center with stronger navy-blue toward side edges; preserve alpha-traced bottom silhouette. New icons player-status-health/stamina/spirit use trimmed aspect-preserved art. All four caption rows are27px tall, tracks aligned, full45px groups vertically centered within the header above the line. Identity shows name/rank together, age/lifespan beneath. Map config overrides rank to base.cultivation.mortal, age15, lifespan60; standalone battle test remains qi_refining. Age/lifespan have no automatic ticking/death mechanics; cultivation remains0/100. Preserve header input/modal rules and source art. No Git sync.

- Latest map status header (2026-09-16): map-only MapStatusHeader stays fixed above MapContent and visible during events/dialogue (z30), battle UI unchanged. Use original map-head1/2 with aspect-preserved cropped art, horizontal tips joined by gold line; fill deep navy gradient along sampled source alpha lower silhouettes. At1920 reference left art width460/right art height86; resize uniformly by viewport width. Show player-1-1 portrait, character name/rank and HP/stamina/spirit from ContentRegistry via pure MapPlayerStatus; cultivation preview0/100, no growth/upgrades. No spirit-awareness, essence, currencies, age, lifespan or new menu buttons. data/maps/status_header.json selects stable character ID and portrait; map resource state initializes separately from battle and currently has no gameplay costs/save/transition integration. Header controls ignore mouse; MapScreen guards its silhouette during normal browsing, while modal dialogue retains full-screen clicks. Run map_header_smoke in headless/render modes with retained suites. Design: docs/map-status-header-design.md. No Git sync.

- Latest round dialogue portraits (2026-09-16): map-dialogue.webp now1835x470, cropped[10,33,1807,409]. Replace event portrait paths with player-1-1.webp (490x490,Atlas[22,18,448,448]) and npc-qlzdz-1.webp (715x715,Atlas[27,27,664,664]); stable speaker IDs unchanged. Round aperture center in cropped art is(246,204.5), configure slot[62,20.5,368,368],max_size368,text_rect[475,95,1250,220]. Portraits use equal-scale calibrated crops, circular portrait_mask shader, drawn beneath the background rim with radius184 covering aperture radius~180.1; no transparent gap or spill outside rim. Keep31px KaiTi by computing font against prior1930x371 reference independently of new art. At1080p dialogue1126.4x254.95 centered(960,810); event picture automatically fits above it with16px gap (about918.47x570.87). Preserve source images and all event rules. See docs/map-interaction-design.md. No Git sync.

- Latest event art/type (2026-09-16): enlarge the current event frame and illustration another25%, preserving the prior bottom center and gap; total scale is5/3 of the initial fitted baseline (4/3 *1.25). At1080p about970x602.90, top82.84, bottom685.74. Dialogue text now uses SystemFont KaiTi/楷体, with STKaiti/Kaiti SC/serif fallbacks; use preceding responsive font size minus1 (31px at1080p). Keep dialogue center75%, background/portrait sizes and all event semantics. Use installed system font rather than bundling Windows font files. No Git sync.

- Latest run tone/picture scale (2026-09-15): updated fr-run.webp retains2600x724 and identical alpha/pose locations; reimport without changing slices/order/display/feet. Run playback now16fps (12-step loop0.75s), movement speed remains360. Event picture/frame scale is now4/3 of the preceding fitted size, anchored at its previous bottom center, expanding upward and sideways uniformly. Preserve the16px1080p dialogue gap and75% dialogue center; old30% picture top is only the baseline calculation, not the new top. At1080p picture is about776x482.32, top203.42, bottom685.74. No Git sync.

- Latest run sheet (2026-09-15) supersedes older six-frame run rules: fr-run.webp is2600x724 with7 unevenly spaced poses. Loop4→5→6→7→6→5→4→3→2→1→2→3 at12fps (12 playback frames,1s). Use existing frame_regions:360x724 slices at x37/419/766/1086/1422/1787/2141,y0. display_frame_height170 and feet_offset(0,-224) align source pose4 ground y586 with idle/walk while preserving airborne stride poses. Source unchanged; Godot cache reimported and runtime size verified. Keep movement speed/threshold, walk and idle unchanged. See artifacts/run7_size_comparison.png and docs/map-player-design.md. No Git sync.

- Latest illustrated event (2026-09-15) supersedes the N37-only scope and70% dialogue position below: N20 at(2082,954) hosts base.map_event.qingshihewan.wounded_disciple, requiring completed bridge_traces. After arrival/idle, a second node click opens map-event-frame.webp plus map-event-1-1.webp; the next click starts20 authored paragraphs with npc-xuetu-qlz/zhujue-m1 portraits selected by line_speakers. Keep illustration visible during dialogue, top at30% screen height, uniformly shrink to fit above the dialogue with16px gap at1080p. All dialogue centers now at(50%,75%), retaining1126.4x216.53 artwork and32px font at1080p. Preserve source aspect ratios; illustration fills/clips its configured inner window. MapEventRegistry validates optional speakers/illustration and prerequisite existence/cycles; pure MapEventState owns illustration/dialogue phases and shared run-only completion. Final extra click completes and hides only that node's Sign subtree; restart resets. Preserve modal input locks. No item rewards, healing, gathering, saves or battle transitions are implied by dialogue. Run map_illustrated_dialogue_smoke alongside existing checks. See docs/map-interaction-design.md and map-editing-guide.md. No Git synchronization.

- Latest dialogue sizing (2026-09-15): shrink dialogue background and portrait uniformly to two thirds of their previous size; center at screen(50%,70%), overriding80% below. Keep the preceding responsive font size unchanged (32px at1920x1080), reflow text within the smaller right panel. At1080p artwork is1126.4x216.53, centered(960,756). Preserve interaction/completion semantics and original source aspect ratios.

- First map interaction (2026-09-15) supersedes older all-signs-display-only restrictions only for N37: after arriving and standing idle at the west bridge endpoint, a second node click starts base.map_event.qingshihewan.bridge_traces. No arrival/pass-through/remote auto-trigger. Data in data/map_events; MapEventRegistry validates content, pure MapEventState controls paragraphs and run-scoped completed IDs, MapDialogue renders the modal. Use map-dialogue.webp centered at screen(50%,80%) and zhujue-m1.webp centered/aspect-preserved in its left frame. Full-screen left click advances the five authored lines; one more click after the fifth completes. Block movement, pan and zoom while active; never leak opening/closing clicks, right click, wheel, drag or focus-loss release. Completion hides only N37 Sign and its StoryIcon, preserving node/routes/other signs. Shared completion lasts until process restart; no disk saves. Other signs remain placeholders, combat unchanged. Run map_event_tests and map_dialogue_smoke alongside existing tests. No Git synchronization.

- Latest walk frame1 replacement/import fix (2026-09-15): source is now1683x600. Retain310x561 explicit slices but move origins to x59/379/687/994/1310,y35 to compensate the new artwork placement. Keep display height133.3, foot offset(0,-217.5),8fps and3→4→5→4→3→2→1→2. Old1644x561 imported cache was positively confirmed and refreshed through Godot reimport; source-only overwrite does not refresh already-running instances. Preserve source pixels, no Git sync.

- Latest walk sheet (2026-09-15) supersedes all older7-column/4-step walk rules: fr-walk.webp is1644x561 with5 poses, playback3→4→5→4→3→2→1→2 at8fps (8 playback frames). Source poses are not equally spaced: player.json frame_regions lists310x561 rectangles at x46/366/674/980/1296,y0. MapPlayer supports optional validated equal-sized source rectangles indexed by frame_order; keep ordinary sequential/auto slicing fallback. Walk display_frame_height133.3 and feet_offset(0,-217.5) match idle's visible104–106px body height and ground anchor. Preserve source images, initial left facing, movement speed/rules, and no Git synchronization.

- Latest map facing/junction adjustment (2026-09-15): player.json initial_facing="left" sets initial idle facing; subsequent movement controls horizontal facing and stationary updates preserve it. N09 is now at source position(1856,387), with Curve_7/9/20 endpoints and nearby bends updated to meet there. Preserve stable IDs, all38 nodes/41 edges, source art and unrelated layout. No Git synchronization.

- Latest plant/ore icon reduction (2026-09-15): map-zhiwu EventIcon at N04/N31 now uses uniform scale0.714051873; map-kuangshi EventIcon at N08/N19/N36 now uses0.628771324. Both are90% of their preceding displayed sizes. Keep centered positions, Atlas regions, all outer frames, source art and other icons unchanged.

- Latest story/outdoor icon reduction (2026-09-15): all4 StoryIcon sprites now use uniform scale0.715186134 (another10% reduction); N05 map-xiux-shiwai RestIcon now uses0.688291799 (10% reduction). Preserve centered positions, Atlas regions, all frames, and other icon sizes. Overrides previous scale values for these two icon types.

- Story size/new circle asset (2026-09-15): reduce all4 map-juqing StoryIcon scales by10% to0.794651260. User replaced mapdot3.webp with370x375 artwork; its new Atlas region is(28,32,316,311). All3 circle Sign scales are now0.861276987 to keep60 source-pixel height and existing tangent offset. Compensate child RestIcon scales to retain their on-map sizes: N05=0.764768665, N13=0.773024277, N32=0.739284467. These are uniform X/Y scales, retaining source aspect. Same-path asset replacements need Godot reimport; Atlas regions don't auto-adapt when artwork bounds change. Restart preview after import; exported builds require reexport.

- Icon size/aspect refinement (2026-09-15): reduce only the5 map-baoxiang EventIcon sprites to local scale0.695492023 and N13 map-xiuxi-shinei RestIcon to0.765567450 (90% of previous sizes). Keep centering, Atlas regions and outer signs unchanged. Always preserve source artwork aspect ratios: X/Y scale must match for all sign frames and icons; transparent-margin trimming must not stretch artwork. N13's outer frame remains uniformly scaled0.86966605 on both axes.

- Story icons on reference-matching frames (2026-09-15): user calls these hexagons but the supplied screenshot matches mapdot4 (octagonal) frames. Add centered StoryIcon using map-juqing.webp only to N03/N20/N37/N16; Atlas(62,8,178,284), local scale0.882945844, position(0,0). Keep the10 mapdot2 plant/ore/chest icons intact. All30 signboards now contain icons. This supersedes older four-octagons-blank statements; display only, no story triggers.

- Latest hexagon content (2026-09-15) supersedes the all-story rule: replace the10 StoryIcon children with centered EventIcon sprites, green reference boxes N04/N31 use map-zhiwu.webp; blue N08/N19/N36 use map-kuangshi.webp; red N06/N14/N22/N34/N27 use map-baoxiang.webp. Trim transparent margins via AtlasTexture and fit silhouettes within local radius125, preserving frame geometry and all other icons. map-juqing source remains available but is no longer displayed on these10 nodes. Presentation only; no harvesting, mining, loot or event mechanics.

- Story/rest sign icons (2026-09-15): all10 mapdot2 hexagonal signs contain centered StoryIcon children using map-juqing.webp. Circular signs contain centered RestIcon children: N05 (blue reference box) uses map-xiux-shiwai.webp, N13 (green) map-xiuxi-shinei.webp, N32 (red) map-xiuxi-water.webp. Atlas regions exclude transparent padding/nearly invisible specks; fit visible silhouettes into radius125 in Sign local space for rim clearance. Preserve current sign sizes/positions and source artwork. These icons are display only, no story/rest effects or map events implemented. Four octagonal signs remain blank.

- Latest sign polish (2026-09-15) overrides the older60px diamond/centered enemy descriptions: all11 diamond Sign sprites are enlarged20% to72 source-pixel height, scale0.942605784 and center y=-56.16 (local y=-250.714286), keeping bottom tangent to the node. EnemyIcon inherits the same20% enlargement, retains its own scale, and uses local position(0,10) for a slight downward shift. Square signs retain60px size: EastExit contains centered map-cunzhen.webp, N02 contains centered map-yiji.webp as LocationIcon children, fit to220px width in Sign local space with trimmed Atlas regions. These are display only; no location entry or encounters. Source art and graph remain unchanged.

- Diamond sign content (2026-09-15): all11 mapdot1 signs now contain centered EnemyIcon Sprite2D children using map-enemy-1.webp. Atlas region(23,15,254,283) trims transparent margins, child scale0.61176471 fits the horn silhouette within the diamond interior with rim clearance. Keep source assets unchanged and icons attached to their signs. This is presentation only, with no encounters or combat triggers; other sign interiors remain blank.

- Walk playback refinement (2026-09-15): fr-walk.webp remains a 7-column source sheet; playback now loops source frames3→2→3→4 at8fps. player.json sets frames=4, sheet_frames=7, frame_order=[3,2,3,4] (one-based, left to right). Slice using source column count. Optional frame_order supports repeats/reordering; absent means sequential frames starting at1. Reject invalid length or out-of-range/noninteger indices. Feet offset/display size and movement rules remain unchanged.

- Map signboards (2026-09-15): qingshihewan_layout has 30 decorative Sign Sprite2D children of annotated Points, using mapdot1 through mapdot5 (cross/question/check/number/flag respectively). Atlas regions trim transparent margins without editing source art; visible height60 source pixels (reduced by one third), center offset(0,-50.16), bottom20.16px above marker center, tangent to the marker artwork top (source y10 at scale0.224); no overlap. Keep 8 unannotated nodes bare, all38 graph nodes/41 routes and existing node input/movement unchanged. Signs follow parent markers, remain below the player, and are editable through standard Sprite2D transforms. No interior icons, event behavior or chest content yet. Mapping and editing instructions: docs/map-editing-guide.md. No Git synchronization.

Godot 4.7.2 · GDScript 2.x · Mobile renderer · Windows-first 2D cultivation/build game.

- Latest movement threshold/walk sheet (2026-09-15): user clarified that excluding the start, 1-2 nodes use walk and 3+ nodes use run for the entire journey. This overrides older1-3/4+ rules; do not switch a running journey back to walk as its remainder shrinks. New fr-walk.webp is2172x724 with7 horizontal frames at8fps. auto_slice=true divides the sheet using shared rounded integer boundaries (310/311px wide), with feet_offset(0,-214) and display_frame_height212 to preserve actor size despite large transparent padding. Keep idle4/3fps, run6/12fps, speed200/360, common scale1.0, source artwork, cursor behavior and deferred rerouting unchanged. No Git sync.

- Latest map animation/cursor polish (2026-09-15): idle is4 frames at3fps (slower than6fps). New fr-run.webp is2172x724, six362x724 frames at12fps, feet offset(0,-217). MapPlayer supports per-action display_frame_height; run uses164 to compensate transparent padding and preserve perceived actor size, idle/walk use128. Speeds200/360 and common display_scale1.0 remain. Map cursor defaults to ARROW, uses POINTING_HAND over a selectable route node, and MOVE only while actually dragging past6px. Hover and click share _node_at hit-testing; update after pan/zoom/release and clear on mouse exit/focus loss. No source image edits or Git sync.

- Map player tuning (2026-09-15) overrides previous sheet/speed/size values: idle source is now2172x724, four543x724 frames at6fps; walk/run stay eight128x128 frames at8/12fps. player.json uses walk_speed200, run_speed360 and display_scale1.0 (twice previous0.5). MapPlayer supports per-animation frame_size and feet_offset, normalizes frame height to base128*display_scale, and updates layout on animation_changed; idle feet offset(0,-300), other animations(0,-56), preserving ground anchor across different sheet resolutions. This is map presentation only; battle portraits and node markers remain unchanged. No source artwork edits or Git sync.

- Map player movement (2026-09-15) supersedes older no-movement scope: left-click a node to travel, left-drag (>6 viewport px) still pans; wheel/focus loss cancels click intent. Start at EastExit and initially frame it, then keep camera manual. Dijkstra selects shortest total Curve2D baked length using stable ID tie ordering; exclude starting node, 1-3 destinations use walk100 source px/s, >3 run180, keeping mode for entire journey. A new destination during motion replaces pending intent and applies at the next node, then recounts that new route. MapTravelState is a detached RefCounted graph/travel model advanced only by supplied delta; map zoom does not affect speed. MapPlayer AnimatedSprite2D slices original 128px sheets: idle6 frames/6fps, walk8/8fps, run8/12fps; loop, preserve foot anchor, flip horizontally with travel. Configuration in data/maps/player.json; source assets unchanged. No encounters, battle transitions, movement costs or saves yet; battle F6 remains. See docs/map-player-design.md, and run map-travel/map-player checks alongside existing checks. No Git sync.

- Map zoom revision (2026-09-15) supersedes older fit/250%/wheel direction rules: 100% is source image width exactly filling viewport width (scale=viewport_width/3220); zoom range100%-175%, default125%, wheel up enlarges and down shrinks. Keep pointer anchoring, pan limits and existing marker Scale0.224. Minimum zoom must have no left/right borders at all supported aspect ratios; width-fit may crop vertically or center a shorter vertical axis. Remove only the broad outer dark route-shadow stroke; retain inner shadow alpha0.42, width+5 and down2 offset, plus gold glow. Source artwork, map geometry and combat remain unchanged.

- Map texture import workaround (2026-09-14): keep assets/gamemap-qingshihewan.webp unchanged, but its .import uses compress/mode=3 (VRAM Uncompressed) to avoid embedded WebP decode failures reported by the editor. Reimport through Godot; never replace the source image or delete map scenes to fix this error. Current source and old cache both decoded successfully during investigation, so the original failure is not conclusively attributed to corruption. map_smoke now verifies texture.get_image(), and tests/run.ps1 -Render includes a real renderer editor startup check. Do not commit generated .godot cache files.

- Latest map polish (2026-09-14): shadow alpha is now0.42, overriding0.60 below; keep width/offset. EastExit is now a visible MapRoutePoint Sprite2D with mapdot Scale0.224, retaining its original position, stable east_exit ID and endpoint connection. There are38 visible markers and41 routes. F5 is used inside the project editor, not the project manager; project main_scene remains scenes/maps/qingshihewan.tscn. See map-editing-guide for entering editor and refreshing a running preview.

- Map browsing refinement (2026-09-14) overrides the initial full-map view below: start at 125% of fit scale, with authored mapdot Scale=0.224 (80% of previous 0.28). Hold left mouse to pan, release/focus loss cancels; wheel down zooms in and wheel up out, anchored at pointer, bounded to 100%-250% with factor1.1 per notch. Clamp panning to map edges and center axes smaller than viewport; preserve center location on resize. Browse transforms affect MapContent only, not authored marker/curve coordinates. MapRoute adds editable dark brown shadow_color (alpha0.60) and shadow_width=5 beneath gold glow, offset down2 source pixels. No source artwork or battle changes, no Git synchronization.

- Map route preview (2026-09-14) supersedes older maps-out-of-scope rules only for this first map step: F5 starts scenes/maps/qingshihewan.tscn; scenes/main/main.tscn remains the independently runnable battle (F6). Preserve source gamemap-qingshihewan.webp and mapdot.webp. Author geometry in scenes/maps/qingshihewan_layout.tscn: 37 visible Points plus hidden EastExit, 41 Routes using independent Curve2D resources and explicit endpoint NodePaths. MapRoute @tool follows marker endpoints while preserving interior bends; gold glow/dashes are drawn overlays. Keep stable map/point/route IDs and original-image coordinates, uniformly fit the full map without cropping. Metadata is in data/maps/qingshihewan.json; scene layout is authoritative for visual editing. No movement, monsters, encounters, map/battle transitions, saves or balance migration implemented/authorized in this step. See docs/map-editing-guide.md and docs/map-route-design.md. Run map_smoke along with retained battle checks; no Git synchronization.

- At the start of every task, read `PROJECT_PROGRESS.md` for current scope, completed work, validation, limitations and next steps. Check actual code and Git state before relying on its status.
- Before implementing a new user request, think through missing gameplay rules and material ambiguities. If any exist, ask and resolve them before editing; the user explicitly requested this workflow. Do not re-ask already confirmed rules.
- Before finishing or handing off a task, update `PROJECT_PROGRESS.md` with the date, changes, validation performed (or not run), unfinished work and next steps. Keep recommendations distinct from user-approved scope; do not leave essential handoff information only in chat.
- After implementation, validation and progress documentation are complete, deliver the local result. Do not automatically push or synchronize Git remotes; wait for an explicit user request. This preference supersedes the earlier ongoing synchronization authorization (updated 2026-09-07).
- V0.8 presentation: hide permanent item names in all battle arrays, retaining quantity/CD feedback and hover details. Chen Yu uses assets/pt-a-1.webp; other portrait images remain configured. Each character references a cultivation rank from data/cultivation: mortal, qi_refining, foundation, golden_core, nascent_soul, spirit_transformation map to pt00 through pt05. Initially Chen Yu is mortal, role 2 qi_refining, role 3 foundation. Fit and elliptically clip portraits within each rank's configured frame window; refresh name and frame together on rank changes. No progression or rank attribute bonuses yet. Play pick/place/invalid bag sounds for corresponding interactions. Consume ordinary damage events in UI for synchronized sword_slashing audio, slash VFX and downward-fading actual HP loss text in af3549, on the actual target on either side; separate simultaneous numbers. Counter/heal events do not trigger sword feedback. Keep audio and VFX outside simulation.
- Use Godot 4 APIs. Do not add C#, Node.js, Electron or unnecessary dependencies.
- V0.9 supersedes older visible-name and no-playback-button presentation rules: hide portrait-side character names/ranks, keep rank frames and board captions; vertically center resources with portraits, hide companion resource labels and narrow their bars, and align the hero's resource right edge with visible board art excluding transparent margins. Timer is 240x50; horizontal 25x25 controls in order pause / 0.5x / play / double, background 2d261f, no hotkey captions; retain keyboard controls. Play starts/resumes at 1x, pause is idempotent, speed-only controls do not resume. Bottom actions are log then retreat. Retreat is allowed only in BATTLE and takes three simulation seconds; pause and speed apply, combat continues, repeated requests cannot reset the deadline, and normal victory/defeat/draw before or at the deadline overrides retreat. Simulation schedules retreat after same-time combat events and emits finished result retreat, with future work cleared. UI shows centered 3s/2s/1s scaling/blurring/fading by simulation progress, then a success modal with review placeholder and exit. Reserve battle_review_requested for future review and battle_exit_requested for future map return; currently exit stops audio and quits the app. Do not implement review, maps or saves yet. Run the independent retreat exit smoke check alongside existing checks.
- V0.10 supersedes the older multi-array/formation/companion-resource/rotation-line/25px layout rules: each side has exactly one main with one 4x4 array and zero to two pure companions. Companions have no inventory/resources, cannot be attacked, spend no resources; main HP zero loses immediately. Remove active formation APIs. Each companion has up to two trait slots allowing any active/passive mix, defined in data/traits and referenced by character JSON. Role 2 has wood healing 5 HP every 6s plus defense +1; role 3 has fire spell damage 5 every 4s plus max/initial HP +20; dog has no companions. Support spell damage subtracts armor but does not trigger counters. Passive source contributions rebuild without accumulation; restores respect adjusted resource caps. Schedule active traits deterministically by side/companion/slot after same-time items and before retreat, using simulation time only. A healing-only roster must not indefinitely prevent an otherwise idle draw; effective damage traits can continue combat. Item and active-trait rotation uses a small colored circular remaining-seconds indicator: metal gold, wood green, water blue, fire orange-red, earth ochre, none white, with data/elements colors; no elemental matchup rules. Preserve entry cooldown, medicines, moving/returning rules. Main and its sole array centered per side, companions show only portraits and two trait slots. Controls are now 36x36 at top right, same order and shortcuts. No companion editing/recruitment UI requested. See docs/v0.10-design.md and PROJECT_PROGRESS.md for current scope; older conflicting descriptions below are historical.
- Latest presentation (2026-09-08): companions use assets/pt000.webp through CompanionCard.FRAME_STYLE regardless of cultivation, preserving portrait images and rank state. Main characters still use cultivation frames. The companion source window is [137,176,978,978] in the 1254x1314 frame, elliptically clipped with overlap beneath its rim. The centered 240x50 timer uses battle-time.webp, with no old accent lines; the central duel text is replaced by a 150x150 battle-start.webp image. Display these decorative images with trimmed transparent margins and preserved aspect ratio; do not alter source assets.
- Latest layout update (2026-09-08) supersedes prior companion row/timer sizes/resource labels: companions form a vertical column to the left of the main, 128px portraits with two 76x88 trait slots to their right. Trait slots reserve icon_texture and temporarily show authored names; active traits show only decimal seconds on a small black badge, without a progress ring, while passive traits show no CD. Item rings remain. Use title.webp top left; enlarge centered timer to 290x58 with 26px text. Add a disabled 36px gear placeholder after the four playback controls; do not implement settings yet. Replace main resource text with zhuangtai-qx.webp / zhuangtai-tl.webp / zhuangtai-ll.webp for HP/stamina/spirit, 32px icons overlapping the left end of rounded bars with fine metal rims, dark tracks and soft vertical gloss. Preserve gameplay, tooltips and resource colors.
- Latest presentation (2026-09-09) supersedes the sizes above: companion portrait/frame height is 236 * 2/3; companion 1 uses skill01.webp then skill02.webp, companion 2 uses skill03.webp then skill04.webp, preserving their trait definitions and numeric CD. Main resource tracks are approximately 149px (previously 223px), with main composite width 410. Move title.webp right by 36px with title-bg.webp behind it. Battle emblem is 180px with slightly increased top spacing. Header controls are all 72x72, centered timer is 580x116 with 39px numbers. The separator is split around left motto 逆天而行 · 道在心中, centered dynamic battle status, and right motto 山河万物 · 皆为道场; lines must not overlap text. Array size remains 548px. No Git synchronization.
- Latest refinement (2026-09-09): controls are 48x48, timer art/container is 580*2/3 by 116*2/3 with 39px digits and unchanged center. Title ink backdrop is 520x154.67 around its previous center. Left motto is centered under title.webp (x=170 relative to the shared margin); add a short separator before it, preserving gaps around all text. Align skill circle centers to companion portrait aperture centers, including pt000 source vertical offset 665/1314; CD badges follow the circle.
- Keep Data → Registry → Simulation → Game State → UI boundaries clear.
- Latest trait shadow tuning (2026-09-09): rim shadow width is now 10px (outer radius44 around radius34), overriding 12px below. Darken near-rim layers while keeping the outer edge faint; retain 1.5px downward offset and independent circles.
- Latest shadow refinement (2026-09-09) overrides the dimensions below: resource strip is y=64, height=108, retaining its vertical center; resource_strip.tres fades only the rightmost 18% horizontally to transparent, with clear top/bottom edges. Widen each trait's faint rim shadow from 6px to 12px (outer radius46 around radius34), retaining the 1.5px downward offset and independent circular shape.
- Latest inventory presentation (2026-09-09): storage panel is opaque at z=10 to cover enemy resource icons; drag previews use z=30. Remove yellow array/selected-item rectangles. ItemDragPreview uses actual target board footprint and the same 9px-inset icon fit, rotation/entry-CD and quantity style. Storage left press starts a deferred native drag immediately after input dispatch, carrying one unit; inventory changes only on successful drop. Both sources center the preview on the pointer. Pick nearest complete footprint by its center, show green/red per-cell occupancy, and drop at that exact cell; do not search farther to evade collisions. Stacks highlight the existing stack cell. Preserve all authoritative inventory/CD rules and refresh storage's target board reference on restart.
- Bottom battle/adjustment/retreat buttons have no hover tooltips (2026-09-09); do not assign tooltip_text when building or refreshing these three buttons.
- Latest button feedback fix (2026-09-09): main action prep caption is 开始战斗. Center icon+text as one group (36px max icon edge, 17px gap, 25px pale text) in the existing 260x68 art. BattleActionButton uses continuous press depth, at most 3% shrink and direct 0.16s release; remove brightness flicker and the old release sine pulse. Preserve button commands, sound, disabled states and sizes.
- Latest bottom controls (2026-09-09) supersede the old bag/log/retreat row: bt-button.webp is a 260x68 main action, centered caption 战斗开始 / 战斗暂停 / 战斗继续, with icon-battle for start/resume and icon-wait for pause. It uses existing start/toggle commands, preserves speed on resume, follows keyboard/top controls, and disables on finished state. Below it, bt-zhihuan.webp and bt-chetui.webp are 126x44 image buttons. Adjustment opens/closes storage only before battle or while paused; never auto-pause running combat. Retreat retains existing semantics. Remove visible log entrance but keep log data/internal panel. BattleActionButton supplies hover/press/rebound feedback; GameAudio synthesizes an 80ms soft click once and plays it for the three enabled actions, independent of simulation. Keep user source artwork unchanged.
- Latest shadow revision (2026-09-09) supersedes all previous shared/faded backdrops: main resources use a flat translucent strip (1e272e alpha 0.48, local y=59, height=118) tucked beneath the portrait, with clear top/bottom edges and no foggy transition or outline. Remove broad companion backdrops; each trait circle has only a faint ~6px rim shadow, offset down 1.5px. Old portrait_backdrop.tres is removed. Reference white boxes are annotations only. Preserve foreground geometry and interactions.
- Latest polish (2026-09-09): divider row is 10px higher (header minimum 106); settings gear uses solid vector teeth and f1dca6 like play controls, remaining a disabled placeholder. Unframed enemy art is 272x272 centered in its 236px slot. Main nameplates share local y=200 for allied/enemy horizontal alignment. Companion portrait layout width is 147 while rendered height/scale stays unchanged, matching approximately 14px visible circular gaps.
- Latest nameplate update (2026-09-09) supersedes hidden portrait names and initial mortal rank: Chen Yu starts at qi_refining (pt01); role_2 is named 青璃, role_3 玄川, without changing stable IDs or traits. Use trimmed name-bg.webp beneath portraits with slight bottom-rim overlap. Labels are 辰宇 · actual cultivation name and 队友 · companion name; the dog uses its name only. Refresh nameplates with authoritative rank changes. Remove old main-column array captions. BattleEmblem is 240x240, anchored to full-screen center offset (0,-20), beneath modal overlays. No progression bonuses or Git synchronization.
- Battle scene music: GameAudio starts assets/battle.mp3 immediately on entering the scene, including preparation, and loops the full track from zero. Keep music independent of battle pause/speed and storage adjustment; do not restart it on phase, inventory or selection changes. Stop it on scene exit/application exit through the existing audio cleanup. No music settings UI is currently requested.
- Gameplay simulation must run without a scene tree, UI, animations or wall-clock queries.
- UI reads authoritative state and sends commands; never infer gameplay state from controls.
- Load ordinary content from JSON and resolve it through stable, namespaced IDs. Never rename published IDs or create one script per ordinary item.
- Use reusable effects and scheduled events; do not poll every item every frame.
- Speed scales simulation time only. Never change Engine.time_scale for battle speed.
- Use Containers, anchors and size flags for layouts. Keep presentation independent of simulation.
- Keep event ordering deterministic, including simultaneous events. Reject invalid content before starting simulation.
- Current authorized scope is V0.7: retain V0.4 formations and two-sided combat, but remove formation/start/pause/reprepare buttons from the battle scene. Formation API is reserved for a future world map. Show status below the centered timer and expose a hidden log through the bottom middle button. Shared storage opens on the right during preparation or paused combat; prebattle arrays can be edited without this panel, paused battle editing requires it, running combat locks it and resume closes it. Escape closes storage without changing battle phase or pause state. Drag preview images from both storage and arrays are centered on the pointer. Select allies only as placement targets; fixed portrait/array sizes remain. Right-click storage items to equip at a random legal cell, drag to choose a cell, and return items to shared storage. Medicine stacks by definition and tracks unique bottles with two uses each; three types restore HP/spirit/stamina, ten bottles each, five points over three seconds per use. Finish opened bottles, wait before opening the next if the resource is full. Opened bottles cannot be returned. Right-click an array returns one unopened bottle; dragging to storage returns all unopened bottles and leaves opened ones. Partial returns preserve the remaining stack identity and schedule. Insertion cooldown is three simulation seconds whenever an item is equipped from storage during battle, including return/reentry while still cooling; prebattle items have none. In-array moves preserve all cooldowns, defense and entry state. Newly topped-up bottles have independent entry deadlines without resetting the current bottle. Rotation cooldown is the existing JSON cooldown (three seconds for test active items). Stack top-ups and same-cell moves preserve progress. Keep deterministic event scheduling, 1920x1080, Space start/pause/resume, F1/F2/F3 for 0.5/1/2x. New weapon/armor names reuse old art. Add stacked defense (iron 1, xuantie 2 plus 2 on entry, qinglin 3); ordinary damage floors at zero. Armor activates on start or insertion cooldown expiry, before same-time attacks. Remove all source contributions when unequipped; reentry must not accumulate. Qinglin counters normal attacks for 1 true damage, including blocked hits, excluding lethal hits; counters cost no stamina and never chain. JSON effects use on_activate, on_enter and on_attacked. Shared storage uses five-column cards with centered images, image-relative quantity badges, right-aligned footprint diagrams, rounded tags and shared detail tooltips. Rotation uses a translucent solid white line without a gray reveal mask; entry cooldown uses a faint gray overlay. Actual activations emit presentation events for a synchronized 0.24-second white flash and small scale pulse, including the last consumed bottle. Resource bars have rounded ends and diagonal shading. The centered brown timer shows SS.hh before one minute and MM:SS.hh afterwards. Tooltips retain only their custom panel, with an empty default TooltipPanel style. The dog uses base.board.roots (bag-bg-2.webp). Three allied board skins use per-skin JSON source dimensions/grid lines for consistent art, hit-testing and placement; remove old cell backgrounds/borders. World map, saves and unrelated progression remain out of scope.
- Reserve future save data for stable IDs and instance/progress state. Do not implement full saves, MOD scripting, Lua, PCK loading or Steam integration yet.
- Document major architecture changes before implementation. Prefer simple solutions suitable for a solo developer.
- Run the headless tests and a project startup check before completing relevant changes. See README.md for commands.
- Do not commit Godot caches, local logs, temporary files or exported binaries. Do not invent a Git author identity.
