# 项目开发进度 · 修仙之路

> 本文件是跨任务交接入口。新任务先读本文件和 `AGENTS.md`，再按需查看代码与详细文档。每次工作完成或中断交接时更新本文件，不能只把最新情况留在聊天记录中。

最后更新：2026-09-06（Asia/Shanghai）。

## 当前结论

**V0.1 最小可玩原型已完成并通过验证，已同步 GitHub。第二阶段尚未开始，也尚未获得实施授权。**

当前可以在 Godot 中按 F5 运行：一柄玄火剑自动攻击一个木桩，支持四档倍速、暂停/继续和重新开始。现阶段目标是验证底层运行方式，尚不是具备完整成长与构筑循环的游戏。

当前没有进行中的功能开发，没有已记录的阻塞项。最近一次工作是建立本进度文档和后续维护规则。

## 项目与环境

| 项目 | 当前信息 |
| --- | --- |
| 本地目录 | `E:\games\path-of-immortals` |
| 仓库 | [SimonZhangM/path-of-immortals](https://github.com/SimonZhangM/path-of-immortals) |
| 分支与远程 | `main`，跟踪 `origin/main` |
| 已验证的原型代码基线 | `7487d0f` — `V0.1 minimal simulation prototype` |
| 引擎 | Godot `4.7.2.stable.official.ed1daf0bf` |
| 本机引擎路径 | `E:\games\Godot_v4.7.2-stable_win64.exe` |
| 技术栈 | GDScript 2.x，2D / 原生 Control UI，Mobile / D3D12 |
| 当前平台 | Windows PC；尚未验证移动设备 |
| 入口 | `project.godot` → `scenes/main/main.tscn` |

Git 已配置本仓库作者身份，使用已连接的 GitHub 账户及隐私邮箱，不涉及全局作者配置。首次同步已确认本地与远程提交一致。上表中的代码基线不代表后续文档提交的 HEAD；接手时用 `git status --short --branch`、`git log -5 --oneline` 检查实时状态，需要核对远程时先 fetch，不要仅凭本文件判断同步状态。

## 已完成内容

- **数据驱动内容**：法器和敌人从 JSON 加载，通过 `ContentRegistry` 按稳定 ID 注册及查询；校验必填字段、重复 ID、冷却、生命、伤害及已支持的效果。
- **正式测试内容**：`base.test.fire_sword`（测试玄火剑）每 3 游戏秒造成 10 伤害；`base.test.dummy`（木桩）初始 100 HP。
- **独立模拟**：`SimulationClock` 支持 1× / 2× / 4× / 8× 和暂停；整数微秒加余数，避免每帧取整累计漂移。
- **事件调度**：稳定最小堆按到期时间及插入顺序出队；循环攻击从上一次到期时间重新排程，长帧补算到期事件。
- **效果与状态**：通用 `EffectSystem` 当前只支持 `on_activate / damage`；权威状态保存在 `GameState`，死亡后清队列，过量伤害截断至实际剩余生命。
- **界面与表现**：中文面板显示血量、冷却、模拟时间、攻击次数、累计伤害；提供倍速、暂停/继续、重开按钮；战斗记录最多保留 12 行，支持滚动。
- **工程与验证**：`AGENTS.md`、架构审查、README、测试脚本及验证记录已建立；Godot 缓存、日志、截图、构建产物不入 Git。
- **版本管理**：原目录实际没有 `.git`，已初始化并完成首个原型提交和 GitHub 推送。

## 继续开发时必须保持的行为与边界

数据链路：`JSON → ContentRegistry → BattleSimulation → GameState → MainUI`；表现事件独立交付给日志层。

1. 模拟类为 `RefCounted`，不依赖节点树、UI、动画或系统时钟；`GameManager` 是现实帧时间到模拟的桥梁。
2. 游戏倍速只改变模拟推进，不改 `Engine.time_scale`；不能让每件道具每帧轮询冷却。
3. UI 只读权威状态并发送操作。生命与统计按 revision 更新，时钟与冷却可每帧更新，表现事件按帧批量交付。
4. 木桩第 30 游戏秒被击破，共 10 次攻击，随后不再受伤；**时钟继续推进**，击破耗时单独保存。这是当前设计，不是死亡计时故障。
5. 重新开始创建新模拟，重置生命、次数、时间、记录，并恢复 1×、非暂停状态；不重新加载 JSON。
6. 普通法器用数据定义，不采用每件道具一个脚本。已有稳定 ID 不重命名。
7. 当前有意合并 ItemSystem/CombatSystem 的编排职责到 `BattleSimulation`，仅单独保留效果接口；没有建立空的 Character/Buff Registry。

## 验证状态

最近一次游戏验证：2026-09-06，V0.1 原型代码；本次交接文档变更未修改游戏逻辑，未重新执行游戏测试。

| 检查 | 已记录结果 |
| --- | --- |
| 逻辑测试 | 525 checks，0 failures |
| 倍速与帧率 | 1/2/4/8× × 30/60/144 FPS 结果一致 |
| 60 秒持续攻击 | 10000 HP 测试实例受击 20 次、损血 200；不修改正式木桩 JSON |
| 边界行为 | 精确到期时刻、同时间事件顺序、长帧、不均匀帧、暂停切速恢复、死亡与过量伤害均通过 |
| 场景联动 | 通过按钮 pressed 信号驱动真实 Manager，验证状态、标签和战斗记录；0 failures |
| 主场景启动 | 无脚本解析、资源加载或运行错误 |
| 实际渲染 | Mobile / D3D12，RTX 2080 Ti；1280×720、1280×800、1720×720、960×540 截图已检查 |

运行：Godot 打开 `project.godot` 后按 **F5**。

复验（在项目根目录的 PowerShell 执行）：

```powershell
powershell -ExecutionPolicy Bypass -File .\tests\run.ps1
powershell -ExecutionPolicy Bypass -File .\tests\run.ps1 -Render
```

引擎路径不同时传入 `-GodotPath '实际路径'`。脚本先导入，再运行逻辑、UI 联动和启动检查；`-Render` 额外使用真实渲染器生成截图。日志和截图位于本地 `artifacts/`，克隆仓库后需要自行生成。

## 已知限制与未实施内容

- 仅一个法器实例、一个敌人、一个触发类型和一个效果类型；尚未实现多实例编排、完整 Trigger/Condition 框架或连锁触发。
- 法器定义按约定只读，尚未强制封装不可变性；多实例扩展时需保护静态定义与实例状态的隔离。
- 当前没有极端事件数量的预算机制或连锁循环保护；未来扩展连锁效果时需要设计，不能简单丢弃事件。
- 未做背包拖拽、相邻关系、人物/境界成长、Buff、资源循环、商店、地图、剧情或掉落。
- 未做完整存档、MOD SDK、Lua、PCK MOD、Steam/Workshop、联网或账号系统。
- 未制作正式美术、动画、音乐与音效。中文使用系统字体，跨平台发行时需选择可分发字体。
- 未导出 Windows 发行包或安装包；将来导出时必须包含 `data/**/*.json` 并在导出产物中验证加载。

## 下一步建议（待用户确定）

建议以“第二件法器与多个实例”为下一小阶段，先确认范围，再实施：

1. 加入第二件 JSON 法器，验证不同冷却下的自动攻击。
2. 将单实例状态和调度扩展为多个实例，验证相同内容 ID 的多个实例互不影响。
3. 补充同一时刻触发、各倍速等价性与多实例重开的测试。
4. 通过后再讨论格子背包和相邻触发，不在当前交接中提前实施。

以上是建议，不代表用户已经批准第二阶段玩法或具体数值。

## 资料索引

- [工程规则](AGENTS.md)：后续开发约束。
- [运行与结构说明](README.md)：数据配置、目录和测试入口。
- [第一阶段设计审查](docs/v0.1-design-review.md)：架构简化理由与验收边界。
- [第一阶段验证记录](docs/v0.1-validation.md)：已执行检查的范围和限制。
- [基础技术立项说明 + 第一阶段实施规范](<基础技术立项说明 + 第一阶段实施规范.md>)：用户提供的技术与范围要求。
- [口袋修仙-Mod制作与SDK分析总结](口袋修仙-Mod制作与SDK分析总结.md)：实际正文是玩法分析，不包含 SDK 接口定义。

历史文档中“尚未配置 Git 身份、尚未提交”的描述属于初始实施时的状态；现状以本文件及实时 Git 查询为准。

## 里程碑与维护规则

| 日期 | 里程碑 |
| --- | --- |
| 2026-09-06 | 阅读两份说明，完成 V0.1 最小原型、逻辑测试与界面渲染验证 |
| 2026-09-06 | 提交 `7487d0f` 并推送 GitHub `main`，确认本地与远程一致 |
| 2026-09-06 | 建立本交接文档，在 AGENTS 和 README 中加入入口与维护要求 |

后续任务维护本文件时：更新日期、当前阶段、完成项、验证结果及对应代码范围、已知限制、进行中的工作和下一步。任务未完成也应记录改动位置、失败信息与待办。明确区分“已完成”“未验证”“建议”和“已批准”，不要把计划写成结果。重要细节放到 `docs/` 并在这里链接，保持这份文档便于快速接手。提交状态以实际命令结果为准，避免写入文档自身未来提交的哈希。
