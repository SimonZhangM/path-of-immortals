# 项目开发进度 · 修仙之路

> 跨任务交接入口。开始工作先读本文件与 `AGENTS.md`，按需查看代码和详细文档；完成或中断时更新。计划、建议、已批准和已完成要明确区分。

最后更新：2026-09-06（Asia/Shanghai）。

## 当前结论

**V0.2「战前背包布阵与左右对阵界面」已完成并通过验证。** 用户已授权本阶段，不再适用 V0.1 文档中“第二阶段尚未获批”的历史状态。

现在启动进入布阵：左侧玩家及 4×4 背包，右侧敌人；玄火剑固定垂直 1×2，铁甲 2×2，可以拖动。点击开始后自动战斗，空格暂停/恢复，F1/F2/F3 = 0.5/1/2×，默认 1×；设计及默认窗口为 2560×1440。

本轮实现、运行验证和文档交接已完成，没有已记录的阻塞项。后续攻防规则尚未获批。

## 项目与环境

| 项目 | 信息 |
| --- | --- |
| 本地目录 | `E:\games\path-of-immortals` |
| 仓库 | [SimonZhangM/path-of-immortals](https://github.com/SimonZhangM/path-of-immortals) |
| 分支 | `main`，跟踪 `origin/main` |
| 历史基线 | `7487d0f`：V0.1；`176648a`：首次进度交接文档 |
| 当前实现范围 | V0.2：背包、布阵阶段、对阵 UI、输入与半速支持 |
| 引擎 | Godot `4.7.2.stable.official.ed1daf0bf` |
| 引擎路径 | `E:\games\Godot_v4.7.2-stable_win64.exe` |
| 技术栈 | GDScript 2.x / 原生 2D Control UI / Mobile / D3D12 |
| 入口 | `project.godot` → `scenes/main/main.tscn` |

Git 使用本仓库配置的 GitHub 身份及隐私邮箱。实时提交及同步状态以 `git status --short --branch`、`git log -5 --oneline` 为准，需要核对远程先 fetch；不要把历史基线当作 HEAD。

## 已完成内容

| 模块 | 现状与入口 |
| --- | --- |
| 内容注册 | `scripts/registries/content_registry.gd`，加载及校验 JSON、稳定 ID、尺寸和效果 |
| 背包状态 | `scripts/core/inventory_state.gd`，4×4，矩形占格、实例隔离、边界与碰撞检查、锁定 |
| 背包交互 | `scripts/ui/inventory_view.gd`，原生拖放、抓取格偏移、绿/红落点预览、点击摆放、非法落点回退 |
| 阶段管理 | `GameState.Phase` + `GameManager`，布阵 → 战斗 → 完成，可重新布阵 |
| 模拟 | `SimulationClock`、`EventQueue`、`BattleSimulation`，独立模拟时间、稳定到期顺序 |
| 效果 | `EffectSystem` 当前仅 `on_activate / damage`；无主动效果装备不排程 |
| 对阵 UI | 左玩家和背包，中央计时与操作，右木桩、血条和记录；2K 容器布局 |
| 占位素材 | `assets/images/`：自制 SVG 修士、木桩、剑、甲；代码绘制山形背景 |
| 工程文档 | AGENTS、README、各阶段设计审查/验证记录、本进度文件 |

正式内容：

- `base.test.fire_sword`：显示名“玄火剑”，1×2，每 3 游戏秒伤害 10；保留 V0.1 稳定 ID。
- `base.armor.iron_armor`：铁甲，2×2，`effects: []`、`cooldown: 0`；当前仅用于摆放，没有防御属性。
- `base.test.dummy`：木桩，100 HP，无反击。玩家暂时为 100 HP 展示状态。

## 必须保持的行为与架构

1. 背包布局是独立权威状态，UI 只读并发命令；保存实例 ID、内容 ID、格坐标，尺寸从静态配置读取。
2. 背包不旋转、不交换、不堆叠、不丢弃到外部。非法移动不修改原位置；拖动装备任意一格时保留抓取偏移。
3. 布阵阶段时间始终为零；点击开始才创建攻击事件。重复开始不得重复排程。
4. 战斗及暂停期间背包都锁定。重新布阵保留布局，重置生命、时间、记录和速度为 1×，等待再次开战。
5. 0.5× = 2 真实秒推进 1 游戏秒；1× = 1:1；2× = 1 真实秒推进 2 游戏秒。UI 帧率不加速，不改 `Engine.time_scale`。
6. 空格按下一次切换暂停，忽略按键自动重复；按钮不抢键盘焦点，防止空格误按“开始”或“重开”。暂停中切速不解除暂停。
7. 木桩第 30 游戏秒击破，共 10 次攻击；V0.2 在完成时冻结时间，取代 V0.1 的完成后继续走时行为。
8. 模拟与背包为 `RefCounted`，不依赖节点树、动画或系统时钟。循环攻击从上次到期时刻重新排程，长帧补算。
9. 当前有两件可摆放装备，但只有一件主动攻击法器；尚未实现多个主动实例的战斗编排，不应把背包多实例等同于多武器模拟已完成。

## 最近验证

日期：2026-09-06；范围：本轮 V0.2 游戏代码与主场景。运行 `tests/run.ps1 -Render` 全部通过。

| 检查 | 结果 |
| --- | --- |
| 纯逻辑 | 484 checks，0 failures |
| 无窗口 UI | 34 checks，0 failures |
| 真实渲染场景 | 39 checks，0 failures |
| 速度等价性 | 0.5/1/2× × 30/60/144 FPS；高血量实例 60 游戏秒攻击 20 次、伤害 200 |
| 布阵与背包 | 尺寸校验、边界、重叠、自占格移动、锁定、实例隔离、抓取偏移、非法落点、重开、过期拖放数据 |
| 原生输入 | 无窗口 Godot 的鼠标按下/移动/释放触发真实拖放；Viewport 分发空格/F1/F2/F3，验证重复按键、暂停中切速 |
| 渲染 | RTX 2080 Ti，Mobile / D3D12；2560×1440、1280×720、1280×800、1720×720 与暂停画面已检查 |

原生拖放用 `Input.parse_input_event` 在无窗口模式测试虚拟鼠标。隐藏渲染窗口会读取真实系统鼠标，因此渲染检查只测拖放回调、键盘及画面，不移动用户的系统鼠标。验证详情见 [V0.2 验证记录](docs/v0.2-validation.md)。

运行：Godot 打开 `project.godot` 后 F5。复验：

```powershell
powershell -ExecutionPolicy Bypass -File .\tests\run.ps1
powershell -ExecutionPolicy Bypass -File .\tests\run.ps1 -Render
```

引擎不在默认路径时传 `-GodotPath`。截图及日志在 `artifacts/`，不进入 Git，克隆后自行生成。

## 已知限制及下一步

- 铁甲没有防御效果，木桩没有攻击；布局尚不改变战斗收益。用户本轮只明确了两件装备的占格与交互，没有规定防御公式。
- 未做多个主动法器的战斗编排、旋转、相邻触发、完整 Trigger/Condition/Buff、成长、资源、商店、地图和掉落。
- 未做存档、MOD SDK、Lua、PCK MOD、Steam、联网。重新退出游戏不会保留布局。
- ItemData 按约定只读，未强制封装不可变；极端连锁事件的预算和循环保护尚未实现。
- 自制矢量图属于原型占位素材，未制作正式角色美术、动画、音乐；中文依赖系统字体。
- 未导出发行包或验证移动设备。以后打包必须包含 `data/**/*.json` 并在导出产物中验证加载。

**下一步建议，尚待用户确定：**设计最小敌方攻击与铁甲防御规则，使装备参与攻防；或者继续扩展背包装备。不要自行确定防御数值、相邻加成或成长玩法。

## 资料与里程碑

- [工程规则](AGENTS.md)、[运行/数据/目录说明](README.md)。
- [V0.2 设计审查](docs/v0.2-design-review.md)、[V0.2 验证记录](docs/v0.2-validation.md)。
- [V0.1 设计审查](docs/v0.1-design-review.md)、[V0.1 验证记录](docs/v0.1-validation.md) 为历史记录，其中自动开战、4/8×、完成后继续走时等已被 V0.2 取代。
- [基础技术立项说明 + 第一阶段实施规范](<基础技术立项说明 + 第一阶段实施规范.md>) 是原始要求，本轮明确要求优先于旧阶段范围。
- [口袋修仙-Mod制作与SDK分析总结](口袋修仙-Mod制作与SDK分析总结.md) 实际为玩法分析，不含 SDK 接口。

| 日期 | 里程碑 |
| --- | --- |
| 2026-09-06 | V0.1 最小模拟原型完成，通过验证，提交 `7487d0f` 并推送 GitHub |
| 2026-09-06 | 进度交接文档与维护规则完成，提交 `176648a` |
| 2026-09-06 | 用户授权背包阶段；V0.2 实现并通过逻辑、输入与 2K 渲染验证 |

后续维护：更新日期、阶段、完成项、验证范围、限制、进行中的工作和下一步。中断时记录待办与错误。重要细节另存 `docs/` 并链接；Git 状态以实际命令为准，不预填本文自身未来提交的哈希。
