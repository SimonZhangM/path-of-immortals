# 项目开发进度 · 修仙之路

> 跨任务交接入口。开始先读本文与 AGENTS.md，再核对实际代码和 Git；结束时更新。用户要求遇到新玩法歧义先确认再编辑，已确认规则不重复询问。

最后更新：2026-09-07（Asia/Shanghai）。

## 当前结论

**V0.7「物品流转与战斗反馈」已实现并通过验证。** 本轮十项需求及后续确认已落实：入场冷却改3秒、阵盘内移动保留进度、未开瓶逐瓶收回、五列居中物品卡、浅入场灰层/半透明白色轮转实线、白闪与轻微缩放、圆润斜向层次状态条、棕色计时框及按需显示分钟。

默认我方左后排角色2/3、右前排辰宇；野狗单人头像组合及阵盘各自居中。主角与队友大小固定。点击我方人物或阵盘只选择物品放入目标，不改变大小或阵型。

进入战前准备，时间停止，无需打开储物袋即可整理我方阵盘。准备或战斗暂停时点击中列底部“背包调整”，右侧打开共享储物袋；战斗暂停期间仍须打开此面板才允许整理。Esc关闭储物袋，不改变战斗阶段、暂停状态或时间；关闭后战前仍可整理、战斗暂停时锁定整理。空格开战/暂停/恢复；开战或恢复自动关闭储物袋并锁定编辑。战斗结束不可调整。F1/F2/F3仍为0.5/1/2倍速，默认1倍速。

战斗界面的阵型切换、开始、暂停、重新布阵按钮已移除。标题“修仙之路”保留，其下旧状态文字移除；当前状态显示在严格居中的计时下方。日志默认隐藏，中列底部“战斗记录”按钮打开可关闭浮层，最多保留500行。查看日志不自动暂停。

## 环境与同步

- 目录 E:\games\path-of-immortals；仓库 [SimonZhangM/path-of-immortals](https://github.com/SimonZhangM/path-of-immortals)，main 跟踪 origin/main，用户授权持续同步。
- Godot 4.7.2.stable.official.ed1daf0bf，E:\games\Godot_v4.7.2-stable_win64.exe，GDScript / Mobile D3D12 / Windows。
- project.godot → scenes/main/main.tscn；Godot F5运行。
- 设计/默认1920×1080，最小1280×720，canvas_items缩放。真实渲染也验证1280×800、1720×720。
- V0.6.1基线提交a2e036e；本轮提交/远程状态须用git status、git log、git fetch后核对，不预填未来哈希。
- 用户指定的 assets/bag-bg-1/2/3.webp 已接入并随本轮同步，原图未修改。assets/backgroundtest.webp及其导入描述尚未指定用途，继续保留未跟踪；战斗背景仍为backgroundtest.png。
- 本轮结束前用户另放入assets/pt01.webp、pt02.webp、pt03.webp，尚未指定用途，保留原样且不纳入本轮提交。

## 储物袋与操作

共享储物袋按全部/法器/防具/丹药/道具/符箓筛选，以五列144×186物品卡片展示竖排名称、数量、占格小图和圆角类别/品质标签，完整效果移入悬停详情。图片去除透明边距后等比缩放，严格居于卡片中央，数量贴合图片右下角；占格小图在2×2区域靠右对齐。原图不修改。道具/符箓仅预留类别。右侧面板覆盖敌方区域，保持我方阵盘原大小。

- 右键储物袋物品：放入当前选中的我方阵盘，随机选择合法位置；放不下保持原状并提示。
- 拖动物品到我方阵盘：选择具体格位。全部大/小阵盘都能操作。阵盘和储物袋两种来源的拖拽图像均以鼠标为中心，保留原有格位落点规则。
- 同种丹药合并到目标阵盘已有堆叠，右下角显示瓶数；追加数量不重置原有瓶的CD，新瓶独立登记入场截止时间。
- 阵盘内拖动或先点物品再点空格位可移动，保留入场和轮转进度、防御及进场状态。右键阵盘物品只收回1件/1瓶；拖回储物袋收回全部可收回的未开瓶，已开瓶留在阵盘。只剩已开瓶时两种收回操作都拒绝。
- 跨角色转移通过先收回再放入另一人阵盘完成；不直接跨阵盘拖放。任何我方存活角色都能装备这些测试物品。
- 储物袋只计未上阵物品；部分丹药放入后仍显示剩余未上阵瓶数。失败落点不扣物品，不能越界/重叠，不旋转。
- 小阵盘的1×1丹药只显示图标和数量；储物袋与阵盘共用ItemTooltip，悬停查看真实效果、基础防御、进场/即刻/轮转说明、瓶数/次数，阵盘还显示佩戴者总防御与当前冷却。默认TooltipPanel使用空样式，只保留自定义详情面板，不再叠加外层半透明底板。

## 两种CD（用户最新定义）

| 名称 | 含义 |
| --- | --- |
| 入场冷却CD | 战斗开始后，从储物袋放入阵盘的等待；当前3游戏秒，模拟配置 insertion_cooldown_usec 可调整。灰层alpha为0.42，中央3s/2s/1s倒计时。 |
| 轮转CD | 每次物品生效前计时；当前武器/丹药均3游戏秒，JSON的既有 cooldown 字段表示此值。仅有alpha为0.55的白色实线自下向上移动，上方灰色蒙版已移除。 |

**战前装入或移动没有入场冷却CD。** 开战直接轮转。战中从储物袋放入，先3秒入场冷却再轮转；即使上次入场冷却尚未结束，收回再放入也重新计满3秒，目前没有豁免物品。阵盘内任意合法移动保留所有进度，不重新attach；仍在入场冷却中也不重置、不跳过。暂停冻结两种CD和持续回复；倍速仅缩放模拟时间，不使用Engine.time_scale。

例：战斗第1秒暂停装入新剑，恢复后第4秒入场、第7秒首次攻击；原有剑即使换格仍在第3秒攻击。第2秒收回新剑再放入，则改为第5秒入场、第8秒首次攻击。卸下/重新插入通过版本号使旧事件失效，不能提前或重复触发。

追加丹药的入场截止时间在模拟的_unit_entry_until中按瓶ID记录；不打断当前瓶。轮到新瓶时，调度至少等待它的入场截止时间加轮转CD。例：第5秒追加，第6秒原瓶用完，追加瓶第8秒入场、第11秒首次使用。整个过程仍按事件调度，不逐帧扫描玩法物品。

## 内容与丹药

| 内容 | 稳定ID / 配置 |
| --- | --- |
| 辰宇、角色2/3 | base.character.chen_yu / role_2 / role_3；各100气血、100体力、100灵力 |
| 野狗 | base.enemy.wild_dog；100气血、100体力、0灵力 |
| 原有玄火剑、铁甲、爪子 | base.test.fire_sword / base.armor.iron_armor / base.weapon.dog_claw，旧ID保留 |
| 青锋剑、赤霄剑 | base.weapon.qingfeng / chixiao；各1件库存，1×2，轮转3秒，10伤害/5体力 |
| 玄铁甲、青鳞甲 | base.armor.xuantie / qinglin；各1件库存，2×2；玄铁甲2基础+2进场防御，青鳞甲3防御+反击1 |
| 回春丹 | base.pill.huichun，气血，10瓶 |
| 蕴灵丹 | base.pill.yunling，灵力，10瓶 |
| 益气丹 | base.pill.yiqi，体力，10瓶 |

三种丹药均1×1，每瓶2次，每次轮转生效后在随后3秒回复5对应资源（整秒1、2、2，避免整数舍入损失），无体力成本。轮转独立继续，不额外串联一个3秒等待。第2次发动时扣除1瓶，已发动回复事件继续完成。例：第3秒首次使用，第4/5/6秒回复；第6秒第二次使用并消耗瓶，第7/8/9秒继续回复，第9秒可接续下一瓶轮转生效。

满对应资源时不启用未开瓶的下一瓶；已开瓶第二次照常使用，即使资源已满也消耗该瓶。第一次实际生效才算开瓶，第一次轮转计时中的未开瓶仍可收回；已开瓶不可收回。回复不超上限、不复活。拆分从瓶列表尾部取未开瓶，保留当前瓶及阵盘实例，返回的瓶按独立ID存放；不重置余下瓶的事件。已发动的回复绑定施用时角色。

新剑/防具沿用已有图片；三种丹药为简单原创SVG占位瓶图。品质暂为凡品，具体分类/功能未来继续扩展。

## 防御与三种触发

- 基础防御：铁甲1、玄铁甲2、青鳞甲3，同一角色多件相加。普通攻击实际扣血max(0,攻击−总防御)，日志记录减免值。
- 进场on_enter：玄铁甲再加2防御，总贡献4。初始装备开战时进场；战中从储物袋放入，3秒冷却结束进场。来源按装备实例记录，卸下移除，重复装卸不累积；阵盘内移动保留防御且不重复进场。
- 即刻on_attacked：青鳞甲受到普通攻击后反击1；0伤害也反击，致死不反击。反击无视防御、无体力成本、不触发反击链。多件反击装备按稳定实例顺序处理，攻击者死亡后停止后续反击。
- 轮转on_activate：原武器和丹药照旧。仅有进场/即刻效果的防具cooldown为0，不排轮转事件。
- 同刻优先级：enter冷却完成事件优先于该时刻的攻击/回复，之后仍按稳定插入序号。攻击与反击整次结算后判断胜负；反击能击败攻击者并结束战斗。
- 权威PartyMemberState.defense_sources按装备实例保存贡献，defense求和；UI只读取。已冷却中的防具既不减伤也不反击。

## 视觉反馈与计时

- 武器/丹药实际轮转生效发送item_activated事件，UI在0.24展示秒内同步完成淡白闪光和缩放（100% → 108% → 97% → 100%）。白闪仅覆盖图片不透明轮廓，峰值alpha 0.26。最后一瓶消耗后用事件快照完成反馈，随后移除，不影响权威库存。
- 展示动画由UI的delta推进，倍速仅影响模拟；动画不推进战斗、不改变伤害/次数/CD，也不因UI刷新重复触发。防具进场和反击暂不额外添加此轮转动画。
- 气血/体力/灵力保持原配色、宽度及居中数字；通用ResourceBar shader绘制圆润两端、细边与斜向明暗层，不改变资源值。
- 计时器严格水平居中，棕色圆角框、两侧短线，无斗字或圆圈。以模拟微秒截取百分之一秒，显示00.00、12.34、59.99；达到一分钟显示01:00.00、01:12.34。状态仍在下方。

## 阵盘图与逻辑格映射

辰宇/角色2/角色3分别绑定base.board.frost / roots / leather，对应bag-bg-1/2/3.webp。每种配置在data/boards/中记录texture、source_size（原图完整像素尺寸）、x_lines/y_lines（各5条格线，分出4×4）。角色JSON的board_layout引用稳定ID。

BoardLayout对整幅图片等比缩放并居中，格位、命中、占位矩形、预览共用映射；画布大小、透明边距、格宽不同都可单独校准。测试也覆盖2000×1000非方形画布和非等宽格。当前三图均1024×1024。装饰外框不是可放置区。

程序绘制的格子底色/边框/圆点、物品常态底板/边框已移除；保留选中边框、拖放合法性和CD反馈。野狗通过敌人JSON引用base.board.roots，使用bag-bg-2.webp及其格线映射；注册表同时校验我方/敌方的阵盘引用。逻辑始终4×4，与美术独立。新增配置说明见docs/board-layout-authoring.md；尚未做可视化校准器、阵盘切换或异形格。

## 战斗及阵型

保留前排优先、同排从上到下、阵亡停止攻击、同刻稳定顺序。初始三人两种阵型、两人一前一后、一人无前后排的规则/API仍在；战斗UI不提供设置，未来接大地图，当前未实现大地图。

武器成本不足停止发动；益气丹恢复体力后可唤醒装备。敌方全灭胜利、我方全灭失败；双方无可发动装备且无待结算回复时平局，待结算回复可恢复体力时不会提前平局。未装备新增物品时默认战斗仍于12游戏秒胜利，辰宇88气血，双方攻击次数10/3。

## 实现入口

| 模块 | 职责 |
| --- | --- |
| scripts/core/shared_storage.gd | 未上阵物品及瓶记录，读取副本、单件取出、收回合并 |
| scripts/core/inventory_state.gd | 4×4占位、put/take、合法落点、同药堆叠、returnable_count/take_returnable、模拟专用use_consumable |
| scripts/core/game_manager.gd | 默认库存、选中目标、编辑门禁、equip/equip_random/unequip/move_item、阶段切换 |
| scripts/data/item_data.gd / registries/content_registry.gd | JSON分类/品质/uses_per_unit与通用效果校验；旧ID和cooldown字段保留 |
| scripts/simulation/battle_simulation.gd | 动态attach/detach、版本号使旧轮转失效、每瓶入场截止时间、回复与item_activated事件、资源变化唤醒 |
| scripts/systems/effect_system.gd | 普通减伤、无视防御的反击、回复及触发/效果组合校验 |
| scripts/presentation/board_layout.gd | 原图格线到显示坐标映射，与模拟无关 |
| scripts/ui/item_tooltip.gd / item_footprint.gd | 共享详情、效果文案、圆角标签与占格小图 |
| scripts/ui/storage_panel.gd / storage_item_card.gd | 右侧共享储物袋、筛选、物品卡片、拖放与右键入口 |
| scripts/ui/inventory_view.gd | 阵盘编辑、选中边框、数量角标、冷却数字、轮转揭示、悬停说明 |
| scripts/ui/main_ui.gd | 清理战斗控件、居中计时/状态、底部按钮、日志浮层 |
| scripts/ui/resource_bar.gd及gdshader / item_flash.gdshader | 可复用资源条明暗与圆角、图像轮廓白闪，仅负责展示 |
| scripts/core/formation_rules.gd / ui/team_panel.gd | 保留阵型规则与固定大小排版 |

模拟仍可不依赖场景树运行，使用微秒事件堆。回复事件绑定角色引用，不依赖UI；卸装使旧装备激活事件失效，阵盘移动不改事件。资源变化事件唤醒待机装备，不逐帧扫描物品。右键随机位置属于GameManager布置命令，模拟内部无随机数和墙钟查询。

## 本轮验证

2026-09-07运行 tests/run.ps1 -Render，最终全部通过、stderr为空：

| 检查 | 结果 |
| --- | --- |
| 纯逻辑 | 1331 checks，0 failures |
| 无窗口UI | 167 checks，0 failures |
| 真实渲染UI | 212 checks，0 failures |
| 导入和启动 | 通过 |

本轮覆盖任意移动保留完整运行态/防御、3秒重新入场与旧事件去重、未开瓶单瓶/整组收回、已开瓶双入口拒绝、追加瓶独立入场。UI覆盖五列实际布局、图片中心/数量跟随、分钟边界、双方武器与丹药白闪/缩放同步，以及最后一瓶消耗后的反馈清理。旧版战斗、丹药、阵型、按键、拖放、悬停和四种分辨率回归保留。

战斗事件增加纯展示item_activated；时序回归筛选damage事件校验原来的双方稳定次序与结果，同时UI验证激活事件。未开瓶退回后保留阵盘当前瓶，旧版允许已开瓶转移的测试已按新规则替换。

真实渲染使用隐藏窗口，不移动系统鼠标；原生鼠标事件在headless虚拟指针验证。实际查看1080p默认界面、五列储物袋、触发峰值截图。动画测试固定UI采样进度抓取峰值/回缩/最后一瓶反馈，未进行人工连续游玩。测试脚本有60秒进程超时。

截图、日志在忽略的artifacts/：storage_default.png、storage_open.png、storage_cooldown_3s.png、storage_cooldown_1s.png、storage_1280x720.png、v07_activation_peak.png、v07_activation_recoil.png、v07_last_bottle_flash.png等。本轮设计和验证见docs/v0.7-design.md、docs/v0.7-validation.md。

## 限制、未完成与后续建议

- 本轮已批准功能无已知阻塞；Git同步以实际状态为准。backgroundtest.webp仍未指定用途，不替换当前战斗背景。
- 没有存档；退出重启库存与布置回到默认。移除“重新布阵”按钮后，本轮结束要重新运行项目开始新的测试；内部restart只作测试/API，未新增战后/大地图流程。
- 无被动资源恢复、复活、旋转、自动整理、批量移动、搜索、品级成长、掉落和跨阵盘直接拖放。
- 丹药支持右键逐瓶装入/收回，拖回所有未开瓶；已开瓶禁止收回。尚无数量输入框或批量指定数量操作。
- 未做大地图、招募、技能/法术、存档、MOD/Lua/PCK、Steam、联网、音效或导出验证。系统中文字体；未来导出需包含data/**/*.json。
- 后续建议（未批准）：指定大地图/战后流程、后续条件触发规则及战斗背景用途。新增玩法歧义继续先确认。

## 历史与文档

V0.1模拟 → V0.2背包 → V0.3三人及图像 → V0.4阵型/野狗/体力 → V0.5共享储物袋/战斗调整/丹药 → V0.6防御/触发/阵盘映射 → V0.6.1操作修正 → V0.7物品流转与战斗反馈。旧文档内2秒入场、换格重置冷却、已开瓶转移、四列卡片等为历史行为。

入口：[README](README.md)、[工程规则](AGENTS.md)、[V0.7设计](docs/v0.7-design.md)、[V0.7验证](docs/v0.7-validation.md)。基础参考文件仍为《基础技术立项说明 + 第一阶段实施规范.md》《口袋修仙-Mod制作与SDK分析总结.md》；用户最新确认优先于历史范围。
