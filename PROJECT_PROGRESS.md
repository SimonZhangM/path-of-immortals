# 项目开发进度 · 修仙之路

> 跨任务交接入口。开始工作先读本文件与 `AGENTS.md`，再核对代码和 Git；完成或中断时更新。建议与用户批准的范围须明确区分。

最后更新：2026-09-06（Asia/Shanghai）。

## 当前结论

**V0.3「三人队伍、独立背包与轮转揭示」及最新布局修正已完成并通过验证。** 当前启动进入布阵：我方为辰宇（炼气初期）、角色2、角色3，每人有独立 4×4 背包。选中角色显示大背包，另外两人按原角色顺序在右上、右下显示小背包。布阵、暂停及结束时可以点选角色或小背包，运行中不能切换。最新反馈截图处于暂停状态，旧实现暂停仍锁定背包；已调整为布阵、暂停均可移动选中角色的剑和甲，恢复战斗后重新锁定，保留轮转进度。

三人分别使用 `portrait1.webp`、`portrait2.webp`、`portrait3.webp` 大头像，头像与名字、三行属性组成透明可点击组合。当前选中组合按 100% 显示，其他两人整体按 66% 显示，切换时尺寸同步变化。属性条显示气血、体力、灵力，条宽 200 设计像素，数值居中。敌人使用 `guaiwu.webp`，时间移到顶部横线上方。背景使用 `assets/backgroundtest.png`，玄火剑图片使用 `assets/weapontest.webp`，保留用户原图。旧角色容器底色/边框、装备详情/冷却说明/拖放说明面板、“法器自动战斗”文字和程序绘制的场景背景均已移除。

三把剑独立轮转，图标上方灰色蒙版随横向虚线从下向上揭开，暂停时冻结。当前三把剑每 3 游戏秒各伤害 10，100 HP 木桩在第 12 游戏秒击破。**本轮先改我方；敌方仍为单个测试木桩，尚未扩展三人敌队。**

本轮代码、输入/渲染验证和文档交接已完成，无已知阻塞。Git 提交与同步状态以实际命令为准，不预填本文自身未来提交的哈希。

最新排版：选中人物组合固定在左，两个 66% 组合按未选角色顺序在右侧上下排列，整体左右边界与三个储物袋对齐。名字和等级按蓝框放在头像旁、属性条上方。右上小背包顶边对齐大背包顶边，右下小背包底边对齐大背包底边。计时严格位于屏幕水平中心、横线上方；速度按钮在计时右侧；敌人标题仅“演武木桩”。生命色 #AF3549、体力色 #8CD259；用户灵力色 `5598eb6` 为 7 位，已说明并暂按 #5598EB 实现。

## 项目与环境

| 项目 | 信息 |
| --- | --- |
| 本地目录 | `E:\games\path-of-immortals` |
| 仓库 | [SimonZhangM/path-of-immortals](https://github.com/SimonZhangM/path-of-immortals) |
| 分支 | `main`，跟踪 `origin/main`；用户已授权同步 |
| 历史基线 | `7487d0f`：V0.1；`176648a`：进度文档；`cf1099f`：1080p 与空格开战 |
| 引擎 | Godot `4.7.2.stable.official.ed1daf0bf` |
| 引擎路径 | `E:\games\Godot_v4.7.2-stable_win64.exe` |
| 技术栈 | GDScript 2.x / 原生 2D Control UI / Mobile / D3D12 |
| 入口 | `project.godot` → `scenes/main/main.tscn` |
| 分辨率 | 设计及默认窗口 1920×1080，最小 1280×720，canvas_items 缩放 |

仓库已有 GitHub 身份及隐私邮箱配置。实时状态用 `git status --short --branch` 和 `git log -5 --oneline`；核对远程先 fetch。

## 已完成内容与入口

| 模块 | 现状与入口 |
| --- | --- |
| 内容注册 | `scripts/registries/content_registry.gd`，加载/校验角色、道具、敌人 JSON，跨类型稳定 ID 去重 |
| 角色状态 | `scripts/core/party_member_state.gd`，独立资源值、只读约定的配置副本与独立 InventoryState |
| 背包状态 | `scripts/core/inventory_state.gd`，4×4、矩形占格、边界/碰撞检查、锁定 |
| 角色选择 | `GameManager.party / selected_member_index / select_member / preview_member_indices`，UI 发命令 |
| 背包交互 | `scripts/ui/inventory_view.gd`，明确绑定角色、大/小两种显示、原生拖放、抓取偏移、非法落点回退、跨角色过期拖放拒绝 |
| 属性卡片 | `scripts/ui/party_member_card.gd`，透明大头像+名字+三行属性组合，条内居中数值、点击角色，选中 100%/未选中 66% |
| 阶段管理 | `GameState.Phase` + `GameManager`，布阵 → 战斗 → 完成，可重新布阵 |
| 模拟 | `SimulationClock / EventQueue / BattleSimulation`，按主动实例独立排程、稳定同时事件顺序，记录 owner_id |
| 轮转表现 | `BattleSimulation.activation_progress(instance_id)` → `InventoryView`，模拟时间驱动蒙版与虚线，大小视图一致 |
| 效果及日志 | `EffectSystem` 仅 on_activate / damage，日志带所属角色名；被动装备不排程 |
| 场景美术 | 用户背景、武器、三张人物头像和怪物图；铁甲仍是 `assets/images/` 内 SVG 占位图 |

正式内容与临时数值：

- `base.character.chen_yu`、`base.character.role_2`、`base.character.role_3`：当前均炼气初期，最大气血/体力/灵力各 100。尚无消耗、恢复、成长规则。
- 每人一把 `base.test.fire_sword`（显示名玄火剑，垂直 1×2，每 3 秒伤害 10）和一件 `base.armor.iron_armor`（2×2，cooldown 0、effects 空）。三人配置相同用于测试。
- 主角旧装备实例 ID `run.item.001`、`run.item.002` 保持不变；队友用 `run.party.1/2.sword/armor`。
- `base.test.dummy`：木桩，100 HP，无反击。铁甲没有防御效果。

## 必须保持的行为与架构

1. JSON → Registry → Simulation / PartyMemberState / InventoryState → UI。模拟与权威状态不依赖场景树、动画或系统时钟；UI 只读并发命令。
2. 背包存实例 ID、内容 ID、格坐标；尺寸从配置读取。不旋转、不交换、不堆叠、不丢弃。非法移动保持原位置，拖动任意一格保留抓取偏移。
3. 选择角色 1 → 小背包 [2,3]，角色 2 → [1,3]，角色 3 → [1,2]，均上到下。小背包可点选所有者，不能直接编辑。
4. 三个人物采用同一个透明头像属性组合；选中者在左整体 100%，未选中者在右侧上下整体 66%，与背包区域外边界对齐。计时固定在顶栏横线上方且严格水平居中，速度控制位于其右边。
5. 布阵时间为零，开战才排程。运行中禁止换人和移动装备；暂停可以换人、编辑选中背包，恢复后重新锁定。暂停/恢复发送 inventory_access_changed 清除过期拖放状态，不重建模拟。重新布阵保留三人布局和选择，重置资源、时间、日志、速度至 1×。
6. 0.5× = 2 真实秒推进 1 游戏秒；1× = 1:1；2× = 1 真实秒推进 2 游戏秒。不改 Engine.time_scale，不加速 UI。
7. 空格布阵时开战、战斗时暂停/恢复，忽略自动重复。F1/F2/F3 = 0.5/1/2×。按钮不抢键盘焦点；暂停中切速不恢复战斗。
8. 多主动实例按同一个稳定事件队列排程，循环从上次到期时刻续排，长帧补算；选中状态不影响装备轮转。三剑木桩 12 秒死亡，总计 10 次有效攻击；旧单剑纯逻辑测试仍为 30 秒。
9. 揭示进度按实例 next_activation_usec 与模拟时间计算，触发后重置；暂停冻结；布阵/完成时图标全部清晰。铁甲不显示主动轮转蒙版。
10. GameState 的旧 item_id/item_instance_id/next_activation_usec 仅保留单剑测试兼容；多武器运行以 item_runtime 为准。

## 最近验证

2026-09-06 运行 `tests/run.ps1 -Render`：全部通过，Godot 标准错误无错误输出。

| 检查 | 结果 |
| --- | --- |
| 纯逻辑 | 1078 checks，0 failures |
| 无窗口 UI | 97 checks，0 failures |
| 真实渲染 UI | 122 checks，0 failures |
| 引擎导入/启动 | 无错误 |
| 多武器 | 0.5/1/2× × 30/60/144 FPS，60 游戏秒共 60 次攻击、600 伤害；不同冷却实例、稳定同刻顺序、来源归属、击破后清队列 |
| 操作 | 三种选中排序及 100%/66% 组合缩放、独立背包、跨角色拖放拒绝、运行禁换人、暂停切换保留轮转、空格开战/暂停/恢复、F1/F2/F3 |
| 原生鼠标 | 无窗口 Input.parse_input_event 验证卡片/缩略背包点选，以及三人各自剑/甲在布阵和暂停时的原生拖放 |
| 渲染 | RTX 2080 Ti / Mobile D3D12；1920×1080、1280×720、1280×800、1720×720；透明人物组合、三种选中尺寸、怪物头像、顶栏计时、1.5 秒暂停蒙版 |

隐藏渲染窗口读取真实系统鼠标，因此渲染模式使用拖放回调与键盘路径，不操纵系统鼠标。画面截图和日志位于忽略的 `artifacts/`；详见 [V0.3 验证记录](docs/v0.3-validation.md)。

运行：Godot 打开 `project.godot` 后 F5。复验：

```powershell
powershell -ExecutionPolicy Bypass -File .\tests\run.ps1
powershell -ExecutionPolicy Bypass -File .\tests\run.ps1 -Render
```

引擎路径不同时传 `-GodotPath`。截图 `artifacts/party_1920x1080.png` 为布阵，`party_battle_paused.png` 为辰宇选中且冷却一半，`party_selected_1/2.png` 分别为角色2/3选中。

## 已知限制与下一步

- 先完成左侧三人；敌方三人、敌方攻击、目标选择和铁甲防御规则尚未实现。体力/灵力为独立状态展示，尚不消耗。背包位置尚不改变收益。
- 未做旋转、相邻触发、完整 Trigger/Condition/Buff、成长、商店、地图、掉落、存档、MOD SDK、Lua、PCK、Steam、联网；退出后不保存布局。
- ItemData 只读为约定，未强制不可变；极端连锁事件预算和循环保护尚未实现。
- 正式角色美术、音效和动画未做，中文依赖系统字体；未导出发行包或验证移动设备。未来导出必须包含 data/**/*.json 并验证加载。
- **后续建议，等待用户确定：**继续敌方三人展示，并明确攻击目标、资源消耗及最小攻防规则。不得自行填入未经确认的防御、成长或相邻加成公式。

## 文档与里程碑

- [工程规则](AGENTS.md)、[运行与架构](README.md)。
- [V0.3 设计审查](docs/v0.3-design-review.md)、[V0.3 验证记录](docs/v0.3-validation.md)。
- V0.1/V0.2 的 docs 文档为历史记录；自动开战、4/8×、单剑队伍、30 秒整队击破、程序背景等旧行为已被后续阶段取代。
- [基础技术立项说明 + 第一阶段实施规范](<基础技术立项说明 + 第一阶段实施规范.md>) 为原始规范，用户本轮明确要求优先于旧范围。
- [口袋修仙-Mod制作与SDK分析总结](口袋修仙-Mod制作与SDK分析总结.md) 实际为玩法分析，不含 SDK 接口。

| 日期 | 里程碑 |
| --- | --- |
| 2026-09-06 | V0.1 模拟原型、进度交接文档完成并同步 GitHub |
| 2026-09-06 | V0.2 战前背包和左右对阵完成；随后改为 1080p、空格开战 |
| 2026-09-06 | V0.3 三人属性、独立背包切换、多武器轮转、用户背景与图标完成并通过全套验证 |
| 2026-09-06 | 三人换用大头像透明组合，加入选中 100%/未选中 66% 缩放、怪物头像及顶栏计时，并复验 |
| 2026-09-06 | 修正暂停背包锁定，三人剑甲均可在布阵/暂停移动；头像改为一大两小堆叠、统一外边界、背包上下对齐、计时居中及速度右置、更新属性色 |

维护时更新阶段、完成项、验证、限制、进行中工作和下一步。中断时务必写明待办与错误；重要细节放 docs 并链接。
