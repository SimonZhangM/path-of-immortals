# V0.3 验证记录

日期：2026-09-06。Windows，Godot 4.7.2.stable.official.ed1daf0bf；RTX 2080 Ti，Mobile / D3D12。

执行 `powershell -ExecutionPolicy Bypass -File .\tests\run.ps1 -Render`，导入、纯逻辑、无窗口 UI、启动和真实渲染全部通过，无脚本错误。

| 范围 | 结果 |
| --- | --- |
| tests/run_tests.gd | 1078 checks，0 failures |
| tests/ui_smoke.gd，无窗口 | 97 checks，0 failures |
| tests/ui_smoke.gd，真实渲染 | 122 checks，0 failures |

## 自动验证

- 保留原单剑事件队列、数值校验、速度、阶段、背包边界/碰撞、锁定等回归检查。
- 三把剑在 0.5/1/2× 和 30/60/144 FPS 下推进 60 游戏秒，高血量目标均受 600 伤害，共 60 次发动。每个实例各发动 20 次，同刻事件归属及顺序一致，铁甲不入队。
- 3 秒与 2 秒冷却的两个实例分别独立排程；1.5 秒时进度为 0.5 与 0.75。
- 100 HP 木桩在第 12 游戏秒第 10 次攻击被击破，队列清空；图标在结束后清晰显示。
- 三人属性初值与 200 设计像素条宽；角色配置深拷贝、非法资源上限校验。
- 三张角色头像分别绑定 portrait1/2/3.webp；怪物绑定 guaiwu.webp。角色组合无底色及边框，选中者为 100%，另外两人整体为 66%，三次切换逐一验证。
- 战斗计时位于顶栏分隔横线上方、屏幕水平中心；多分辨率自动检查计时中心与视口中心偏差小于 1 像素。
- 所有选中组合对应正确的大背包和上下小背包；背包布局独立、跨角色移动拒绝、切换使旧拖放数据过期。
- 无窗口通过 Input.parse_input_event 注入鼠标按下/移动/释放，验证真实 Godot 拖放、卡片点击、小背包点击。
- 空格开始/暂停/恢复及忽略重复按键；F1/F2/F3 速度；所有背包开战锁定、运行禁切换、暂停允许选择和编辑，恢复后重新锁定。
- 三名角色的剑和甲在布阵及暂停时分别拖到新格位，检查权威状态位置，恢复原位再换下一角色。无窗口走原生鼠标拖放，渲染走 UI 拖放回调。暂停编辑不改战斗时间和攻击次数。
- 自动检查一大两小人物无重叠、与下方背包整体左右边界对齐，以及右上/右下背包与大背包顶边/底边对齐。
- 选择不重置攻击次数或冷却；1.5 秒时半透明区域保留，暂停推进无变化，3 秒发动后进度回到零。
- 重新布阵保留布局、重置生命/时间/速度/日志；旧拖放数据失效。
- 引用用户背景和武器资源路径；真实渲染保存多尺寸及不同选中状态截图。

## 画面检查

生成截图：`artifacts/party_1920x1080.png`、`party_1280x720.png`、`party_1280x800.png`、`party_1720x720.png`、`party_battle_paused.png`、`party_selected_1.png`、`party_selected_2.png`。

人工检查确认：三个人物采用透明的大头像与属性组合，名字紧跟境界，气血/体力/灵力各占一行、数值居中；选中组合为 100%，未选中组合为 66%，切换时比例和高亮正确。怪物头像位于右侧，计时在顶栏横线上方。大背包左侧，小背包右侧上下排列。装备详情、冷却说明、拖放说明和“法器自动战斗”已经移除；背景仅用提供图片，无旧程序场景标记。武器进度一半时虚线位于图标中部，上方灰色、下方正常，大图与缩略图一致。1080p 与超宽画面未发现布局溢出。

无窗口测试使用虚拟指针，不移动真实鼠标。隐藏的真实渲染窗口读取系统指针，故渲染测试使用拖放回调及键盘输入，未进行桌面鼠标自动化。测试截图和日志不提交 Git，克隆后运行命令重新生成。

## 范围限制

本次画面复验确认：人物改为左侧选中组合、右侧两个上下排列的 66% 组合；名字位于头像旁属性条上方。采用新的红/绿/蓝属性色，速度控制位于计时右侧，敌人标题仅“演武木桩”。1080p 和 720p 实际截图符合边界对齐，三种选中状态均能正确交换显示。

本轮仅我方三人，敌方仍为单木桩；三项资源均为 100/100，无消耗/恢复，铁甲没有防御数值。蒙版为灰色透明覆盖与虚线揭示，不包含额外模糊滤镜。未验证导出包、正式存档或移动设备。
