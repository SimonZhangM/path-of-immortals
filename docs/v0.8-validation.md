# V0.8 验证记录

日期：2026-09-07。Godot 4.7.2，Windows，Mobile D3D12。执行：

```powershell
powershell -ExecutionPolicy Bypass -File .\tests\run.ps1 -Render
```

| 检查 | 结果 |
| --- | --- |
| 纯逻辑 | 1352 checks，0 failures |
| 无窗口UI | 198 checks，0 failures |
| 真实渲染UI | 243 checks，0 failures |
| 项目导入与启动 | 通过 |
| import/tests/ui/startup/render stderr | 全部为空 |
| git diff --check | 通过 |

新增覆盖六档等级/头像框映射、无效等级与窗口/素材校验、切换等级同步刷新及基础属性保持；两种来源拿起声、成功/失败命令声音、原生拖放失败只反馈一次。音频资源能解码并连接播放器，退出显式停止，最终日志无资源泄漏。

命中检查使用真实战斗事件，验证双方目标、减防后实际伤害（野狗-10、我方前排-4）、af3549颜色、斩击和音效同步触发、同刻数字互不重叠、向下移动/淡出/释放，且展示不推进模拟时间。原有物品、丹药、冷却、阵型、拖放、按键及四种分辨率回归保留。

实际查看artifacts/storage_default.png和v08_hit_start.png，確認三名角色的等级边框与头像裁剪、双方命中反馈及错开的数字。测试也生成v08_hit_fading.png；截图和日志仅存于Git忽略的artifacts/。

自动化音频测试静音运行，验证事件路由、资源与播放集成，未人工设备试听；未进行连续人工游玩或导出验证。等级升级、属性加成、背景音乐、音量设置均未实现，也不属于本轮批准范围。

实现、文档与素材均仅保留本地，无已批准的未完成项；按用户偏好不提交、不推送或同步Git。
