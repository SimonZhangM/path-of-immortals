# 地图节点互动与桥头对话（2026-09-15）

## N20插画与双人对话扩展

用户指定中央小屋旁的N20。沿用到达后再次点击的互动入口，先同时显示map-event-frame.webp（用户称map-event-pic，工作区实际文件名为frame）和map-event-1-1.webp，再点击才显示首句对话。扩展既有dialogue事件的可选illustration阶段、speakers头像表与逐句line_speakers；保留原N37纯文字事件格式及稳定ID。每次点击推进一个段落，按说话者切换npc-xuetu-qlz/zhujue-m1头像，完整保存20句原文。法器、采集、治疗均为台词，此轮不引入相关玩法或奖励。

用户最新确认：事件框顶部仍为屏高30%，水平居中，允许缩小以避免与对话框重叠；对话框中心改为屏高75%，字号和尺寸保持。插画从首次打开起就预留对话空间，与下方对话框间隔16逻辑像素（随窗口宽度缩放），两阶段不跳变。插画在对话期间保留；所有图片保持源比例，内容居中覆盖内框窗口并由边框遮住边缘。N20要求先完成N37的独白，requires_completed引用稳定事件ID。纯状态增加illustration阶段，下次点击才进入dialogue第0段；20段读完再点击一次完成，隐藏N20路标及图标，运行期不重复、重启重置。与N37共享输入锁定和完成记录，不发放台词中提到的法器或任务奖励。

## 已完成的N37机制

本轮首次加入地图事件：截图指定的大桥西端N37。玩家先通过原有寻路到达节点，停稳后再次左键单击该路径节点才触发；路过、刚到达、点击路标图片或远程点击均不直接打开事件。其他路标保留，未配置内容时不编造互动。

事件采用JSON稳定ID、节点ID及类型。MapEventRegistry负责加载/校验，MapEventState负责触发、逐句进度及完成记录，保持独立于节点、动画和时钟。MapScreen处理地图输入并发出命令，MapDialogue负责图片/头像/文字展示。该入口可供后续事件类型扩展，本轮只支持dialogue。

用户确认完成记录仅保留当前运行期间，重启游戏重置。记录以事件稳定ID保存在纯状态的运行期共享字典；不写磁盘、不引入存档。完成后隐藏该节点的Sign及内部图案，保留路径点和连接，不修改布局源场景。重新实例化地图时按完成记录恢复隐藏状态。

对话使用map-dialogue.webp背景（裁去透明边缘，保留源图宽高比），整体中心在屏幕(x=50%,y=75%)，底板和头像统一缩至初版的2/3，文字字号保留初版响应式大小并在缩小后的右侧区域排版，跟随窗口不跟随地图平移/缩放。zhujue-m1.webp按可见范围等比纳入左侧方框并居中，右侧逐句显示用户原文。用户确认对话为全屏输入模态：暂时锁定地图移动、拖动和缩放，任意位置左键单击推进；第5句再点一次关闭并完成。开启事件的原始点击不同时推进第一句，关闭的点击不穿透到地图；右键、滚轮、拖动、失焦后的抬键均不推进。

验证覆盖：未到达不能触发、到达不自动打开、停稳再次点击触发、逐句显示、完成后不重复及只隐藏目标路标；继续覆盖地图拖动/缩放/改道、所有旧战斗检查，并渲染核对画框位置、头像与中文文本。
