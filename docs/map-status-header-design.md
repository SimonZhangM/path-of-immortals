# 地图顶部状态栏（2026-09-16）

最新实际玩家数据：data/characters/chen_yu.json为凡人，初始/最大气血50、体力50、灵力0。MapPlayerStatus等级和可选层级改从角色定义读取，地图配置不再覆盖等级/层级，显示凡人、50/50、50/50、0/0。为避免Godot Range将0..0视为满条，仅显示控件max_value取至少1，实际灵力上限和文字仍为0，条为空。凡人修为初始0、当前需求100，显示修为0%及0/100；历练初始0。修为体系、胜利后修为/历练奖励留待后续明确规则，目前不发奖、不升级。地图与独立战斗共用角色初始定义，运行中资源尚未互通；战斗原随从被动仍可增加最大气血。年龄15、寿元80、头像布局保持。

最新头像边框：以player-level-1.webp替换原程序绘制的细金圆框。原图543×610，实际圆心约(271,300)，采用以此为中心的Atlas[16,45,510,510]，避免透明留白导致偏心；边框显示113.333×113.333，与头像局部中心(126,66)重合，再一起应用原PortraitGroup的1.05缩放。头像原尺寸96×96及圆形裁切保留，头像边缘略压在金环下方，不露缝。边框启用Mipmap，不修改源图，不更改战斗境界框。

最新位置微调：灵石/历练整组右移10px，图标x1735、名称x1767；数字在整体位移之外再右移10px，即相较上一版数字共右移20px，数字位置为名称起点＋原前缀宽度＋15px。地图挂牌下移20px至(18,128)，尺寸与比例保持。以上使用1920设计坐标，按屏宽统一适配。

最新地图挂牌：彻底移除地图右上角SettingsPlaceholder及剩余三横线图标，战斗设置不变。左上新增map-title-qshw.webp（源362×1086），裁去两侧/底部透明边缘，Atlas[46,0,236,1031]保留顶部挂绳；1920设计坐标中位置(18,108)、尺寸84.96×371.16，严格等比例。绘制在head3之前，使挂绳藏入框架下沿，形成悬挂效果。随header按屏宽适配，地图拖动/缩放不改变其位置，事件与对话期间继续显示；忽略鼠标，不增加交互功能或金线下的背景。启用资源Mipmap及对应过滤，不修改源图。微调入口：scripts/map/map_status_header.gd中MapTitle的位置和尺寸。

最新间距调整：head3及其中头像/圆框、两组透明文字容器一起左移2px，原点(-35.32,-19.12)，姓名容器x178、年龄容器x186。四资源状态组整体左移10px至x560，宽度和内部间隔保持。灵石/历练整列右移5px至图标x1725、名称x1757；数值拆成独立Label，基于原名称及空格的字体宽度再右移5px，仍从状态读取。设置占位按钮取消圆角深色底板，使用StyleBoxEmpty，仅保留三横线图标，仍未启用设置功能。

最新居中容器：头像/圆边组再左移2px，PortraitGroup位置(-10.805,-5.805)，5%放大保持。姓名＋等级在透明CenterContainer(180,22,320,62)内用HBoxContainer组合，间隔18，按两者总宽高居中；年龄＋寿元在透明CenterContainer(188,92,244,22)内居中。不绘制容器背景或边线，也不拦截鼠标。金线曾被随后绘制的head2源图局部遮挡，现将connection置于两张装饰图之后绘制；左侧渐隐保持，右端直线覆盖至x1920。

最新主角信息：姓名“张辰宇”，角色稳定ID仍base.character.chen_yu，基础角色数据统一改名，战斗姓名牌也自动沿用。地图等级设为base.cultivation.qi_refining，配置cultivation_stage="一层"，展示“炼气 · 一层”；寿元80年，年龄仍15岁。层级本轮只做配置显示，不引入升级机制，也不将其他角色共用的“炼气”等级名改成一层。姓名槽调整到(190,25,156,58)，等级槽(352,31,148,34)，保持既有字号。

最新头像与右侧排版：头像和圆框放在PortraitGroup内，以原圆框外沿右下角(176.1,116.1)固定放大5%，组scale=1.05、position=(-8.805,-5.805)。前三条仍各232px且不移动，资源整行宽改1140，修为列372px、右端x1710对齐原灵石数字右沿；修为标题显示“修为 0%”，其中一个空格后为按当前/所需修为计算并四舍五入的整数百分比，和原始数值/进度条一起随状态刷新。灵石和历练在原历练列上下排列，图标25px（原27）、字号20（原22），图标x1720、文字x1752，行高25、行距7，总高57在金线上方居中。灵石上、历练下，数值仍0。

最新连接处调整：head3再左移4px，原点(-33.32,-19.12)，右边界529.36；大小及垂直位置保持。背景在此右边界附近为#142230，向右360px平滑过渡回原中央蓝黑色，原右端渐变保持。金线起点前12px透明，至64px恢复完整金色，之后一直到屏幕右沿不淡化；Line2D增加对应共线顶点控制渐变范围，防止两端顶点插值令整条线都渐变。金线分界高度、线下透明区域不变。

最新用户澄清优先：地图分界是横向金色直线，而不是head3最低装饰边。height_for_width现取CONNECTION_Y按屏宽比例缩放，1080p约90.942px。地图裁切区从该线开始；移除上一版128px整宽下衬，也移除head3左下方沿轮廓生成的填色，程序背景仅画到金线。head3源图及头像/年龄条在金线以下的自然延伸仍保留，透明部分透出地图。其余地图坐标、拖动缩放与对话适配保持。

最新显示区域分离：顶部栏不再覆盖地图。以head3最低边计算保留高度并向上取整，1080p为128px（按窗口宽度等比适配）。header下衬补满整条蓝黑区域，MapViewport顶部严格等于header底部、下边贴屏；clip_contents将地图/路线/节点/角色限制在此区域。地图拖动、缩放、缩放中心与窗口变化均按剩余区域计算，map_to_screen统一转换地图点到屏幕坐标；上方整条区域不响应地图操作。事件图与对话仍使用全屏坐标，持续保留顶部栏。缩放基准仍为图片宽度占满屏宽100%，范围100%～175%，默认125%。编辑器路线场景继续可编辑，源图和坐标不改。

最新像素微调：head3从上一版左移5、下移2，原点(-29.32,-19.12)；宽度减少5到562.68，采用统一缩放562.68/887保持原比例（高度同比微收），右边再向内收5。内部金线y约90.942，延伸到x1920；右侧背景直接填至整段水平金线，修复head2透明装饰下方漏地图，不再沿head2凹凸下缘裁填充。head2较上一版右移1、下移1，右沿1921、尖头y90.92。头像及圆边上移4，头像(78,18)、圆心(126,66)。年龄寿元15px、等级25px。灵石/历练的27px图标分别采用player-status-coin/exp，x1598/1720，文字右移34px；均启用Mipmap，保留名称及配置值0。

最新贴边及右栏布局：head3以0.64统一缩放，裁切图原点(-24.32,-21.12)，使原图x45/y78的内侧左边/上边对齐屏幕边缘，外侧第一道金线移出视口。右沿543.36、底沿126.72，金线改y89.92并带动head2/状态条组。年龄寿元16px，槽(204,92,258,22)居中，位于内部横线与底部边框之间；姓名48、等级24保持。四状态列移至x570、总宽1000、间隔24，各条约232px；第四组改“修为”并使用player-status-level（启用Mipmap）。灵石/历练位于x1598/1720，与标题同高，数值由MapPlayerStatus读取配置spirit_stones/experience，初始均0，无增减机制；未加入图标或加号。最右侧60×44圆角菜单样式仅为禁用的设置入口占位，无设置交互。

最新调整覆盖下述旧版尺寸：用户确认map-head3完整框架围住头像与姓名区，显示宽460。源900×350，Atlas[7,45,887,231]，局部位置(0,6)，严格保比例。标注的内部横线位于原图y218.5，因此延伸金线高度为6+173.5×460/887≈95.98，左接点为裁切内(803,173.5)；金线绘制在框架底图之后，保持连续，右侧head2跟随新接线高度，仍高43。头像调为96px、位置(78,22)，姓名区在框内避开顶边：姓名/等级/年龄寿元分别48/24/22px，各比上一版减2；位置分别(202,25)/(332,31)/(204,64)，字体字重颜色保持。四状态组按新金线高度垂直居中。

player-status-health/stamina/spirit三个源图的导入设置启用mipmaps/generate=true，图标TextureRect明确使用TEXTURE_FILTER_LINEAR_WITH_MIPMAPS，实际启用多级缩小过滤。只影响这三处小图标，不更改源像素或全局过滤。源图亮色轮廓风格仍保留。

姓名区按用户独立截图校准：1920宽下姓名50px楷体（轻微加粗0.4），颜色#f9f6d3；等级26px思源宋体Noto Serif SC、字重700，颜色#ece6b5，位于姓名右方。第二行24px宋体、字重600，颜色#d6d7d3，年龄和寿元由竖线分隔。姓名/等级/第二行局部坐标分别为(202,6)/(332,12)/(204,52)。字体使用已安装系统字体，宋体备用SimSun，不复制Windows字体文件。截图里的元婴初期、545岁和2900年仅作视觉参考，内容仍取序章配置。

本轮仅地图常驻，事件和对话期间也可见，战斗界面不变。左map-head1与右map-head2保持原比例，按各自水平尖头锚点连接金色直线；围合区域以#0b1320为中间底色，两侧渐增蓝色，适配窗口宽度，独立于MapContent平移/缩放。左侧头像用player-1-1，首行显示辰宇和凡人，第二行显示年龄15岁、寿元60年。依次显示气血、体力、灵力及修为进度。修为仅测试0/100，不增长或升级；年龄/寿元仅配置展示，没有时间流逝或寿元耗尽机制。不加入神识、精血、货币、菜单或充值按钮。

1920宽基准下，左饰宽230、右饰高43，均为上一版尺寸的50%，贴各自侧边；水平尖头高度保持y85.326。金线宽1.2，向两图内部各延伸3px压住接缝。填充继续沿源图Alpha下轮廓绘制。三状态图标换为player-status-health/stamina/spirit，裁去透明边后保比例纳入27px图标槽。四组标题行统一高27，标题与条间隔5，条高13，总高45；整体中心位于顶部至接线高度的中点，修为条与三资源条共用基线。

data/maps/status_header.json配置角色稳定ID、头像校准、测试修为值、序章cultivation_rank=base.cultivation.mortal及age=15/lifespan=60；MapPlayerStatus从ContentRegistry解析等级及基础属性，属于独立地图运行期状态，不创建战斗、阵盘或随从加成。序章等级覆盖只作用于地图，保留独立F6战斗的炼气测试配置。UI读取状态并监听changed，不能从文字/状态条反推属性；未来地图战斗衔接可更新此状态，目前不连接独立F6战斗、不实现消耗或存档。

MapStatusHeader位于地图内容之外，z=30；状态栏自身及其子控件忽略鼠标，由MapScreen在常规浏览时按填充轮廓拦截栏内地图点击/滚轮，模态对话时仍由原全屏对话处理点击，保持任意位置点击推进的规则。布局沿顶部锚定；绘制区域按装饰曲线轮廓围合，空白的地图区域不扩大拦截。保留源图，不生成或覆盖美术资源。
