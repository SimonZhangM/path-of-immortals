# V0.9 验证记录

2026-09-08，Godot 4.7.2 / Windows / Mobile D3D12。

执行：`powershell -ExecutionPolicy Bypass -File .\tests\run.ps1 -Render`。

| 检查 | 最终结果 |
| --- | --- |
| 纯逻辑 | 1381 checks，0 failures |
| 无窗口UI | 252 checks，0 failures |
| 真实渲染UI | 328 checks，0 failures |
| 导入、启动 | 通过 |
| 撤退实际退出（独立进程） | 通过，退出码0 |
| 六类stderr日志 | 均为空 |
| git diff --check | 通过 |

撤退覆盖战前拒绝、战斗请求、重复请求不重置、0.5/1/2倍速、暂停冻结、精确3模拟秒、倒计时期间及截止同刻攻击照常发生、胜利/失败在截止前或同刻优先、平局取消撤退、完成后不再推进模拟。UI验证中心3s/2s/1s、逐秒放大/模糊/透明度、暂停冻结动画、成功弹框、复盘占位提示、退出请求门禁和实际关闭。

退出测试在多个同刻命中刚播放后立即点击退出，发现并修复音频停止早于同帧播放请求处理的问题。退出先等待一帧处理播放请求，再stop_all并给混音线程0.2秒释放时间；不修改模拟时钟。

排版检查覆盖四按钮顺序、方形尺寸等于计时器高度一半、2d261f背景、缩窄计时器与全屏居中、日志/撤退左右排列、姓名隐藏、资源组/头像垂直居中、队友短条及资源名隐藏、主角条右端对齐阵盘实际图案右端（扣除透明边距）。四种分辨率及原有阵型/背包/音效/命中反馈回归保留。

已实际查看artifacts/storage_default.png、v09_retreat_3s.png、v09_retreat_blur.png、v09_retreat_success.png。所有截图和日志在忽略的artifacts/。音频测试静音，未进行人工试听、连续游玩或导出包验证。

本轮批准范围完成。复盘仅预留GameManager.battle_review_requested，退出通过battle_exit_requested，未来可接地图；尚无复盘实现/存档/地图。交付保持本地，不提交或同步Git。

## 2026-09-08 新版pt00复查

原图更新为1254×1289的细圆环；旧凡人窗口偏小，已改为[0.0586,0.0639,0.8814,0.8670]。以原图透明度测量内外圈，头像椭圆边界位于环线覆盖范围；对0.66/1/2倍的独立透明背景实际渲染做像素和视觉复查，未见内圈透明缺口或外圈溢出。正常比例最外侧少量抗锯齿像素alpha为249/255，位于金环外轮廓，不是内圈漏图。并实际查看更新后的1080p整场截图。

再次运行完整-Render检查，数量与上表一致且全通过。该轮发现退出音频清理的一帧等待偶尔不足，补充0.05秒播放请求处理等待后，独立退出与最终整套检查stderr均为空。临时测量/渲染脚本和预览仅在artifacts/，未加入新的永久测试；用户提供的pt00原图保持原样。
