# V0.10 验证 · 2026-09-08

执行 `powershell -ExecutionPolicy Bypass -File tests/run.ps1 -Render`。

| 检查 | 结果 |
| --- | --- |
| 纯逻辑 | 1066 checks，0 failures |
| 无窗口UI | 211 checks，0 failures |
| D3D12 Mobile真实渲染UI | 293 checks，0 failures |
| 导入 / 项目启动 / 独立撤退退出 | 通过 |

逻辑检查覆盖单主角限制、0–2配角、重复/超量/无效特质拒绝、主动/被动任意组合、相同特质独立计时、双方配角、主角死亡清空事件、攻击扣防御且不反击、治疗封顶、被动重建不叠加及低血量重建、换队清理来源。检查暂停、三档速度、不同帧分割结果一致；同刻装备→配角槽位→撤退的优先级和致死特质覆盖撤退。默认新配置24秒胜利，辰宇114/120气血。原库存、护甲、丹药、入场3秒、移位保持进度、未开瓶退回、撤退规则继续回归。旧多人攻击/阵型断言已迁移，因此检查总数与V0.9不同。

UI覆盖唯一阵盘、0/1/2配角、无资源条、等级边框、两特质槽/悬停详情、环色和秒数、暂停冻结和生效闪光、药瓶数量与圆环分离、右上36px四按钮及快捷键、原生拖拽/右键/Esc/五列储物袋、特效/声音事件和撤退弹框。真实渲染检查1920×1080、1280×720、1280×800、1720×720。静音自动测试，未人工试听或导出。

实际查看artifacts/storage_default.png和v10_rotation.png：主角与单阵盘居中，配角头像/特质清楚，玄火剑红橙圆环、青锋剑金黄圆环、爪子白环均显示2.6s，特质显示5.6s/3.6s。另生成v10_trait_activation.png和原有背包/入场/命中/撤退截图。临时脚本、日志、图片仅在忽略的artifacts中。

已知限制：强制`--quit-after 5`启动检查仍出现2个MP3播放对象退出残留warning，无SCRIPT ERROR/ERROR，退出码0；正常音频清理和独立撤退退出通过。这是先前音频任务已有的限制。其余测试stderr为空。`git diff --check`通过；仅恢复未改变内容脚本的原换行格式，未移除用户修改。未同步Git。
