# 地图储物袋界面（2026-09-18）

## 属性加成完整增益栏与全部二级分类（2026-09-19）

最新颜色／修为行：属性名称与加成数值统一#FBF4BF；灵力下方新增“修为”，使用player-status-level.webp图标，暂固定+0%，表示未来阵盘可提供的修炼速度加成。沿用同款悬浮和手形光标，说明“修炼速度的额外加成，可由阵盘提供。”，当前不接入实际修炼加速机制。加入此行后整组高度635px，继续居中。

最新排版：属性加成标题至雷蕴作为完整内容组在208×910左栏内居中，返回按钮位置不动。标题到题字间距由18减半至9px；题字到第一根分隔线、第一根分隔线到气血、第二根分隔线到反锋的布局间距均为9px。内容组宽164px，左右仍为22px留白。

最新悬浮区域与列位移：三资源和九增益均以“图标＋名称”整组作为统一悬浮区域，手形光标与器具卡片一致；子图标／文字将鼠标交给同一组，数值列不参与说明触发。图标＋名称列在原位置右移5px，居中数值列以原始位置为基准左移10px（撤销此前右移10px，再向左移10px），保留原列槽位、行高、整组居中、标题与分隔线位置。三资源新增同款正文浮框：气血“角色的生命值，降为0时无法继续战斗。”；体力“发动部分武器和道具时消耗，体力不足时无法发动。”；灵力“施展法术时消耗的资源。”。不修改资源机制或物品关键词清单。

“全部”页显示器物／消耗／灵材／蓝图下的全部17个二级按钮，按原组内顺序展开并自动换行，支持在全部页直接筛选和取消细分。收藏、关键、杂物仍无二级。六列卡片宽度与间距不变。

左栏“天地灵物，纳于一盘”与其上分隔线互换，题字在前、分隔线在后。气血／体力／灵力下面追加护甲、护盾，再加同样的分隔线，然后是反锋／生机／润脉／附炎／坚韧／轻灵／雷蕴。图标使用对应buff-*.webp并开启mipmap，沿用27px图标、22px楷体标签、44px居中数值列与9px行距。九行始终显示，无加成为+0。

用户确认统计口径：仅汇总当前修仙等级、已装备阵盘和盘内道具的常驻加成；袋内道具不计，冷却回复量不计。护甲额外计入盘内防具的armor_capacity，表示护甲上限加成，不是战中当前护甲量。等级／阵盘／道具可以配置可选buff_bonuses对象，键为armor、shield、counter、vitality、moisture、flame、tenacity、agility、thunder，数值为有限非负数；缺省为0。当前境界、阵盘和十件道具未额外设置此字段，不虚构初始加成。换装、恢复阵型与玩家状态变化刷新数值，不改存档结构。

名称、图标映射与用户提供的九条完整释义维护在data/ui/map_buffs.json。鼠标悬浮图标使用ItemTooltip的说明专用模式，复用主层正文的540px深色圆角边框、22px内边距、23px楷体和原文字颜色，不带物品头像、标题／标签或关键词层。本轮是界面及加成汇总；不因此新增七系状态的战斗触发实现，护盾及七系完整战斗规则仍按战斗设计另行接入。

## 最新储物袋分类（2026-09-19）

一级固定顺序：全部／收藏／器物／消耗／灵材／蓝图／关键／杂物。保留实际在袋道具数量角标；全部跨所有分类，收藏跨所有分类匹配favorite标签，不再显示常用和旧配方页签。

| 一级 | 二级（按顺序） |
| --- | --- |
| 器物 | 武器／防具／法器／阵盘／符箓 |
| 消耗 | 丹药／掷具／诱饵／食物 |
| 灵材 | 草木／兽材／矿材／异材 |
| 蓝图 | 丹方／器方／符方／饵方 |
| 关键、杂物 | 不设二级 |

全部显示四组的全部二级，收藏不显示二级行。切换或重新点击一级时清除旧二级选择，默认显示该一级全部道具；点击二级筛选，再次点击已选二级可取消细分。隐藏二级时一并隐藏其下方分隔线，不留空行。搜索、品质与排序仍与当前分类组合，重置筛选返回全部。

data/ui/map_inventory.json维护一级、叶子名称与category_groups映射；当前十件数据ID／category未改，器物7件（武器3、防具3、法器1）、消耗3件（丹药3）。其余分类预留，目录已支持19种叶子类别校验和筛选；投掷物的界面名称为“掷具”，蓝图不设“阵图”。未据此扩展非现有道具的战斗／获取机制。原common／recipe布尔字段兼容接收但不再决定一级分类，蓝图按明确的配方类别归属，避免把丹药错归丹方。六列卡片尺寸、左右边距、搜索、品质、排序与存档保持。

## 最新物品悬浮说明（2026-09-19）

本轮最新精简与机制：[物理倍率与毒蚀接入](combat-damage-toxin-2026-09-19.md)。通用词义现为斩击／穿刺／钝击／轻甲／护甲／持续／毒蚀7类，按物品相关性显示。护甲上限及消耗不再列释义，恢复上限与毒蚀免疫只留正文；三物理类型说明与实际倍率共用配置，两处5%已取消，并补“对其他甲型伤害100%”。轻甲词义用“护甲类型”，持续改为通用的“按指定间隔多次生效，直至效果结束。”。气血／护甲支持小数，毒蚀完整跳伤已实现，怪物施毒来源待后续添加；下方旧“只有说明／没有跳伤”的同范围段落已被覆盖。

冷却不再列入关键词释义，正文“冷却：XX秒，……”及其颜色保持。现有通用词义共7类：穿刺、钝击、斩击、轻甲、护甲、持续、毒蚀；每件物品只显示与自身有关的条目。

伤害类型词义直接由当前EffectSystem倍率矩阵生成，和实际结算共用同一配置：斩击克制无甲、穿刺克制轻甲、钝击克制重甲，并显示当前20%加成／减伤。

最新文字／字体修订：物品名称改用宋体；正文中的浅绿具体词与关键词行左侧词名使用宋体，功能正文和词义解释使用楷体；“关键词含义”标题继续用宋体。字号与颜色不变。正文格式为“冷却：XX秒，……”。移除品级文字标签，保留类别、细分类、攻击类型、占格及按品质显示的雾。体力／气血／灵力不列入词义解释，“持续药效”简称“持续”。

地图储物袋／阵盘与战斗储物袋／阵盘统一使用ItemTooltip：最新宽度为540设计px（由720px减少四分之一），深色主面板＋下方左右各内收18px的棕色关键词面板。左上为96×96正方形容器，内置品质雾和等比物品图；右侧30px宋体名称与类别／细分类／攻击类型／文字占格标签，分隔线下23px楷体效果正文。下方仅列该物品相关关键词，18px楷体解释，浅绿词名用宋体；整体高度随收窄后的换行内容自适应。

功能说明换行修订（2026-09-19）：效果正文中的显式换行按独立段落渲染，段间距为8设计px，与关键词含义区域各释义行的VBox间距一致。单个段落因当前540px容器宽度不足产生的自动换行不增加这8px，继续沿用RichTextLabel原默认行距。只调整排版，不改变文字、效果规则或悬浮行为。

移除右上编号／数量、占格图示、信息／感叹号圆圈和效果装饰图标；“轮转”改为“冷却”，“敌人”改“敌方”。用户确认武器本体不消耗，原每击体力花费保留，正文用“耗费X点体力”，不出现药瓶消耗含义。三武器标签从左至右为通用类别／子类／攻击类型／占格，第三项依次显示斩击／穿刺／钝击。止血丹、强筋丸和消毒散正文统一写“使用后消耗一瓶”；恢复护甲正文删除冗余的“不超过护甲上限”。温玉佩合并双资源回复并单列恢复上限。属性、冷却／耗体、药效、存档和鼠标操作时序不变。

地图原纯文字tooltip对接同一组件，原生TooltipPanel外壳透明，防止双层边框。物品图沿用品质光晕／纹理元数据缓存，字体主题复用。

## 最新十件道具与单瓶药效（2026-09-19）

当前保留原六件武器／防具，移除18个a／b／c副本，新增下列四件各1；都为下品、强化0、1×1、无体力消耗，原卡框／品质光晕／1px描边／mipmap显示保持。

| 道具／图片 | CD | 实际效果 |
| --- | --- | --- |
| 温玉佩／artifact-wyp | 4秒 | 不消耗；同时恢复4气血、6体力，各最多至最大值2/3，整数向下取整；已高于阈值不降低资源，也不影响另一项回复 |
| 止血丹／pill-zxd | 6.5秒 | 每次发动消耗1瓶；发动后第2／4／6秒各回2气血，总6 |
| 强筋丸／pill-qjw | 7.5秒 | 每次发动消耗1瓶；发动后第2／4／6秒各回4体力，总12 |
| 消毒散／pill-xds | 5.5秒 | 每次发动消耗1瓶；清空当前毒蚀，获得15战斗秒毒蚀免疫 |

回复药沿用满资源等待、回复不超过资源上限；已发动的三跳不依赖瓶子继续存在，不会因瓶子消耗而中断。消毒散按CD发动，也能提前提供免疫，不要求先中毒。暂停不推进药效／免疫；倍速随战斗时钟。保持现有战中变动不回写地图配置的原型边界。

毒蚀仅接入状态层数、施加入口、清除和免疫截止时间，尚无正式敌方毒蚀来源；本次没有扩展完整木属性生机抵消、层数上限和毒伤逐秒结算，其未决计时仍见战斗总设计。所有毒蚀施加必须经PartyMemberState.apply_toxin，免疫期间拒绝，截止时恢复可施加。

旧装备存档只跳过精确匹配的18个退休副本ID，不放宽未知物品校验；保留原件格位，四件新品进入储物袋。旧阵型保留已退休副本引用，应用时对应位置为空。空盘时共10件，固定六列两行；装备后只显示未装备项。

界面、筛选、行囊双向拖放、本地配置保存及战斗读取已完成，目前包含三件下品武器、三件下品防具。消耗护甲池与护元轮转已接入实际战斗；完整七属性／法术体系仍未迁移。美术细化与阵型效果后续补充。

最新物品图规则（2026-09-18）：weapon-qsdj／lg／dbtc与armor-cby／jtk／myd六张物品纹理的Godot导入配置均生成mipmap；今后加入的每一件物品图也必须生成mipmap。MapInventoryCatalog在加载物品目录时校验图标资源存在且确有mipmap，漏配会拒绝该批记录；MapItemArtwork中的原物品图显式采用LINEAR_WITH_MIPMAPS。高分辨率透明WebP缩小时使用合适层级采样，储物袋、行囊和拖拽预览保持一致。只改变导入缓存及采样方式，不修改原始WebP像素或Atlas裁切。

最新标签与轮廓规则（2026-09-18）：三件武器卡片各显示三个标签，依次为“武器／剑／斩击”“武器／弓／穿刺”“武器／锤／钝击”；category=weapon仍负责类别筛选。当前六件物品全部配置art_outline_px=1，使用统一深灰黑#151515的16方向亚像素平滑轮廓，覆盖储物袋、行囊和拖拽预览。图片几何、品质光晕、占格、属性与存档不变。

## 最新扩展：布袋阵盘与三件下品武器

### 2026-09-18防具与消耗护甲接入（已实现）

新增下品防具各1：粗布甲（衣甲、1列×2行、轻甲、4秒获得3护甲、上限+7）、旧铁盔（头盔、1×1、5秒获得1护甲、上限+2）、木圆盾（盾、1列×2行、5秒获得3护甲、上限+2）。只有衣甲记录甲型，其他部位不设置；本轮不加入尚未最终确认的甲型伤害倍率。用户再次确认无固定减伤，攻击实际消耗护甲、溢出才扣气血；沿用已确认的初始护甲0、上限叠加、提高上限不凭空补甲和卸下按新上限截断规则。用户将随从护元改为主动每5秒获得2护甲，无额外上限加成。三件防具没有资源消耗，轮转恢复受总上限约束，战中入场仍沿当前3秒等待再转轮规则，上限等被动属性在装备生效时提供。

新的实际战斗默认采用护甲池；历史固定减伤回归显式使用隔离测试fixture，不能污染真实入口。保留旧数据与历史报告。旧三武器格位存档继续有效，新增三件防具作为初始测试物品进入储物袋，不清空既有布局。主角资源组下方显示当前护甲／上限及衣甲甲型。护甲满时仍轮转，实际恢复不超过上限；护元不提供上限，无防具时基础上限0，因此不会凭空增加护甲。三件防具分别使用armor-cby、armor-jtk、armor-myd原图，标签、占格与悬浮数值均读同一记录。

Head下方新增inv-background.webp底层背景，50%不透明度；以保持比例并覆盖的方式填充可用区域，必要时裁边，不拉伸。前景面板及文字保持原不透明度，Head不受影响，背景不截获鼠标。

用户确认地图和战斗都改3行×3列，统一使用zhenpan-bag.webp。新增base.board.bag配置，按原图格线校准显示与鼠标映射，原4×4皮肤保留为历史资源。库存边界由角色所用阵盘定义决定，不能只换图而保留隐形第四行／列。正式战斗入口的主角装备改为读取地图最后保存的配置，原伤害与CD规则保留。

右侧新增青石短剑、猎弓、短柄铁锤各1，品质下品，继续使用level-fanpin底框，卡片左上角不再额外显示品质文字。开发过程中用户新增weapon-qsdj／weapon-lg／weapon-dbtc，分别接入短剑／弓／铁锤；Atlas裁去透明边缘，等比居中显示，源图不变。名称和类别标签按参考展示，完整基础属性悬浮显示。用户最新约定占格按列×行表示，三者均1列×2行，右上角用竖向两个小方格表示。记录明确保存footprint_columns=1／footprint_rows=2，悬浮说明也按列×行显示；字段仍分别表示实际列数和行数，独立战斗旧物品定义不变。

数值依次为斩击7／3.5秒／耗体3，穿刺9／4.5秒／耗体5，钝击8／4.0秒／耗体4。地图与战斗从同一记录读取数值，战斗沿用现有普通伤害结算，未加入新的攻击类型系数体系。获取顺序作为初始配置序号保留，三件未自动标收藏或常用。

分类补充（2026-09-18）：三件通用类别category=weapon，界面与筛选统一显示“武器”；可选非空文字字段subcategory分别为剑／弓／锤。卡片内部依次显示通用类别、细分类和伤害类型三个圆角标签；斩击／穿刺／钝击仍是独立伤害类型并保留在详细属性中。三件没有法术属性，不添加法术属性字段或“无属性”标签。本段已按用户最新三标签要求修订。

## 入口与布局

最新道具移动刷新优化（2026-09-19）：目录变化后按物品ID与完整记录复用未变化的储物袋卡片，仅新增／移除／替换实际变化项，并按当前筛选排序重新排列。保持六列固定尺寸、原拖动结束处理和交换语义。图片透明边界与mipmap检查按纹理资源缓存，资源changed时失效，不保留CPU图片副本；名称宋体及字重配置共享。避免每次放下道具重建整批卡片／字体、重复读取图片，外观与源素材不变。当前24件、D3D12环境下，同步放置处理由约402～438ms降为约2～4ms；此数据是调用耗时，不是完整帧率或所有设备性能保证。

最新储物袋正方形布局（2026-09-19）：1920×1080设计基准下，右侧StoragePanel由906×910扩为910×910。左右屏幕边距由24减为23设计px，左栏／中间列与中间列／储物袋的两处横向间距由20减为19px，四处各释放1px。左栏宽208、中间阵盘／阵型列宽718、主体高度910及底边距24不变；对应x坐标为左栏23、中间列250、储物袋987，储物袋右边1897。六列物品卡继续按可用宽度等分，筛选、滚动和交互不变。

最新储物袋底图（2026-09-19）：910×910 StoragePanel使用开启mipmap的inv-bg-3.webp作为75%透明背景。取素材alpha实际绘制范围Rect2(33,32,1192,1196)，排除外侧透明留白后铺满容器，并按14px圆角shader平滑裁切；素材自带画框随底图显示。移除StoragePanelBorder程序金边，StyleBox边线宽度为0；深色底层、内容边距、标题／筛选、固定六列卡片、滚动和交互均不变。

最新储物袋底图可见性（2026-09-19）：右上搜索框由250px收窄为200px，减少对右上美术的遮挡。物品展示区StorageItemWell取消#09151e实色背景并改为全透明，让卡片之间及空白区域透出inv-bg-3。底图沿用上述取景，围绕910×910容器中心放大2%后整体向右移动3px；75%透明度、mipmap、14px圆角裁切、无程序金边及前景布局／交互保持。

最新透明度修订（2026-09-19）：inv-bg-3储物袋底图透明度由75%降至50%；总放大比例保持1.02，不再次累计2%。向右移动3px、透明物品展示区、200px搜索框及其他样式／交互不变。

最新底图变换（2026-09-19）：inv-bg-3在1.02倍基础上再次放大2%，累计比例为1.0404。相对上一版向左1px、向下2px，最终相对居中位置净偏移为向右2px、向下2px。透明度仍为50%，其余储物袋内容与交互不变。

最新储物袋视觉对齐（2026-09-19）：储物袋标题图标补偿title-chuwudai源图42／300的左侧透明留白，在26×26槽内左移约3.64px，使可见图案距容器左边22px，与阵盘标题图标一致。标题／搜索、收藏、类别、排序和卡片区域统一采用左右22px边距。卡片区左右边距为22px，六列按剩余宽度等分；横向布局间距保持15px。筛选和交互语义不变。

最新筛选排版／圆角／金边修订（2026-09-19）：收藏行和类别行不再均匀铺开，分别恢复紧凑左对齐的8px HBox与5px HFlow排版；22px行容器左右边距、标题图标视觉补偿和卡片区22px侧边距保持。阵型记录与储物袋容器圆角均为与阵盘一致的13px，StyleBox与底图shader裁切同步；阵型记录继续无程序边框。储物袋保留与属性加成相同的1.2px抗锯齿shader实现，但颜色单独改为#806d48；属性加成仍为d9bb72。

最新卡片视觉行距／阵型底图修订（2026-09-19）：level-fanpin.webp亮卡框顶部约有30源像素的透明／暗色留白，在当前六列卡片缩放下折合约8～9设计px。横向布局间距继续为15px，纵向布局间距改为6px，叠加素材留白后可见卡框上下净距约15px；卡片尺寸、首行位置、卡内构图与每行六列不变。阵盘inv-bg-2为x=-7、右边+4、顶部0、下边+5及50%透明度。阵型inv-bg-1显示层只把左边向左延伸1px，最终x=-1且右／上／下边不变；透明度最新为50%，原内容取景、横向比例、13px圆角、卡片与交互保持。

最新阵盘与阵型控件细调（2026-09-19）：中间718×718 BoardPanel圆角为13设计px，inv-bg-2保持0.970299内容比例与既有顶部取景，透明度50%；显示矩形为左边-7px、右边+4px，顶部0、下边外延5px。下方阵型记录容器的inv-bg-1主体取景仍以中心为锚放大1%，图片内容横向按714／718比例收缩，高度取景不变；显示层仅左边外延1px，右／上／下边不变，透明度50%、圆角13px。阵型记录卡仍为92×138、图标78×78，图标距卡片左／右／上均为7px；卡片的普通／悬停／按下／禁用状态均无按钮边框与内部底色，名称和三秒修改入口保持。阵型名称输入框及placeholder为17px，与保存布局选项一致。CheckBox使用成对的20px图标：勾选和未勾选都保留同一浅色小方框，仅勾选态增加深色勾；默认状态和保存语义不变。

最新保存布局选项样式（2026-09-19）：阵型弹窗的保存／更新当前布局CheckBox取消悬浮、按下和焦点装饰变化。普通、勾选、悬停、勾选后悬停共用相同边框、底色、12px内边距和文字颜色，避免文字移位与边框消失；仍正常切换勾选并沿用原保存行为。

最新阵型记录容器背景（2026-09-19）：718×174阵型记录容器使用开启mipmap的inv-bg-1.webp背景并以50%透明度显示；容器程序金色边框移除，StyleBox边框宽度为0。以素材不含外侧淡阴影的主体描边范围Rect2(40,99,2052,515)为基准，将取景围绕中心放大1%，再仅扩宽Atlas横向采样，使画面横向按714／718比例压缩；高度和纵向取景不变。显示层仅左边外延1px，右／上／下边保持，并按13px圆角裁切。阵型卡片、垂直居中、滚动与交互不变。

最新四容器底层透明化（2026-09-19）：属性加成InformationPanel、阵盘BoardPanel、阵型记录FormationPanel、储物袋StoragePanel四处StyleBox原不透明深色填充全部改为透明。四张美术底图自身透明度依次为25%、50%、50%、50%；半透明区域直接透出全局inventory背景。各容器圆角、程序边框状态、底图裁切／几何、内容和交互不变。

最新24件测试道具（2026-09-19）：六个原道具各新增a／b／c三个独立版本，名称如青石短剑a、猎弓b、木圆盾c。经用户确认，副本全部属性、品质、强化等级、占格、数量1及美术沿用各自原件；仅ID、名称及取得顺序独立。原ID与取得顺序1至6不变，新ID追加_a／_b／_c、取得顺序7至24。支持现有装备／交换／阵型存档／战斗读取，不是堆叠数量。旧保存格位保留，18件新增物品自动补入剩余储物袋；空阵盘时24件按六列四行滚动显示，装备后右侧只显示未装备项。

最新属性行间距（2026-09-19）：左侧气血／体力／灵力三行之间的间隔由18减半为9设计px，原32px行高、图标／文字样式和对齐保持。

最新阵盘容器背景（2026-09-19）：中间718×718 BoardPanel底图使用inv-bg-2.webp，生成mipmap且透明度为50%。以四边72源像素为基准裁去素材画框，保持累计内容比例0.970299及既有顶部源坐标。显示矩形在上一版横向几何基础上整体向右移动1设计px，最终相对容器左边为-7px、右边为+4px，顶部仍为0、下边仍外延5px。超出部分由面板shader裁切。该容器不绘制程序金色外框，圆角13设计px。22px内容边距、标题／容量、内部3×3 zhenpan-bag及格位／拖拽映射不变；其他三个大容器圆角仍为属性加成18、阵型记录18、储物袋14设计px。

最新气血图标光学校正（2026-09-19）：在三图标共同原图画布缩放基准上，仅将气血心形围绕画布中心放大1.1倍；体力、灵力保持1.0倍。仍不按alpha有效区独立放大，三项27×27槽、位置、文字和数值列不变。

最新属性图标统一尺度（2026-09-19）：气血／体力／灵力三张源图画布尺寸相同，左栏直接以完整原纹理作为共同缩放基准放入27×27槽，不再分别裁取alpha有效区后独立放大。保持比例居中、线性mipmap、左侧排列、7px图文间距、22px名称和右侧44px居中数值列不变；源图与资源加成逻辑不改。

最新属性标签对齐（2026-09-18）：按用户最新决定撤销左列整组居中，恢复资源图标在左、名称紧随其后的原排列，名称占用剩余宽度。保留图标与文字7px间距、原字号和尺寸；右侧44px数值列继续居中。

最新筛选标签样式与补全（2026-09-18）：第一排集合筛选依次为收藏、常用、灵材、配方、储物袋；储物袋是原all完整库存筛选并显示总数，配方读取物品可选recipe布尔标签。第二排补入法器、问答、材料、招式，保留现有项目类别；没有对应物品时显示正常空结果。第一排选中态为金色2px描边／深金底／柔和金光，第二排选中态为亮蓝2px描边／深蓝底／蓝光及冷白字，未选类别保留细蓝灰框，悬停光效较弱。默认选中末尾储物袋和第二排全部；品质与排序行不变。

最新阵型弹窗（2026-09-19）：新增第六项“炼器”，使用开启mipmap的zhen-lianqi.webp。图标分两行各三项，第一行攻击／防御／平衡，第二行修炼／炼丹／炼器；原五个素材路径不变，旧记录继续按路径回选。弹窗宽460px、内容自适应高度；新建模式用默认勾选的“按当前阵盘的道具和格位保存”替代旧说明，取消勾选会建立空布局记录。编辑模式同一控件默认不勾选，文字改为“用当前阵盘布局更新已保存的道具记录”。错误信息仅出错时显示。底部按钮从左到右为新建／保存修改、取消，到外框保留22px内边距。图标卡124×130px、12px间距、输入框与操作按钮最小高48px及持久化规则保持。

最新储物袋计数精简（2026-09-18）：移除储物袋右下角“显示 N / N 项”及其整行占位，释放高度给物品卡片区域。筛选、空结果提示、滚轮与固定六列尺寸保持不变。

最新数值列对齐（2026-09-18）：左侧阵盘三项属性加成数值在各自44px列内水平、垂直居中，覆盖之前的右对齐；列宽和其他布局保持。

最新阵盘属性加成（2026-09-18）：当前普通行囊阵盘base.board.bag配置resource_bonuses={hp:5,stamina:5,spirit:0}。左侧题字下方用三行两列展示：27px既有资源图标＋22px楷体气血／体力／灵力，右列统一对齐+5／+5／+0，0也明确显示。上限加成由阵盘数据提供，地图和战斗共同读取，不改角色基础数值；地图初始55/55气血、55/55体力、0/0灵力。当前战斗主角另有随从20气血，合计75；同款盘对使用它的其他角色也生效（当前野狗亦为该盘）。新会话／战斗重置以加成后上限初始化，反复重置不累加。旧盘未配置按0。阵型只存物品格位，不包含盘属性。

最新标题图标与阵型卡固定布局（2026-09-18，2026-09-19细调）：左栏标题为“属性加成”并使用title-zpsxjc.webp，题字为“天地灵物，纳于一盘”；中间标题由“行囊”改为“阵盘”并使用title-zhenpan.webp；右栏“储物袋”使用title-chuwudai.webp。三个原竖线／无图标标题统一改为26×26图标槽，与26px文字字号一致，保持图片比例并使用LINEAR_WITH_MIPMAPS；四张title素材均生成mipmap。阵型卡片为92×138，78×78图标槽距左／右／上均7px，下方名称区固定为50px，阵名由一行变两行时图片位置不动；名称line_spacing=-6。卡片按钮本身不显示边框或底色。新增入口的图标槽显示title-zhenxing.webp，下方两行分别为22px加粗“＋”和15px“阵型记录”。两行省略、居中、滚动、持久化和悬停交互不变。此段覆盖下方标题竖线、旧名称及阵型入口加号位于图标槽的历史说明。

最新六列卡片锁定（2026-09-18）：卡片布局改为固定六等分列宽，不再把不足六件时的剩余空间分给已有卡片。列宽按（物品区宽－5×15px）÷6计算并保留小数，确保所有列一致且左右净留白仍为15px（实际内边距16px含约1px金线）。数量减少、筛选及多行滚动均不改变卡片尺寸。纵向仍支持滚轮，但不显示占宽滚动条，避免出现／消失时挤压列宽；全游戏窗口缩放仍沿用固定16∶9等比缩放。

最新拖入交换（2026-09-18）：仅针对储物袋拖入阵盘。先确认新物品完整占格在盘内，再按目标矩形实际重叠的不同道具实例数判定：0件可直接放入（绿色），1件可交换（浅蓝色），2件及以上不能放入（红色）。同一物品占多格只计一件；2×2新物品覆盖两个1×2或两个2×1道具均拒绝，即使每件只部分重叠也计数。松手成功后新物品位于提示格位，被替换道具整体回储物袋，先构建有效替换结果再一次提交，失败不改归属。盘内移动仍不交换；右键随机装备仍只选空闲合法位置。此段覆盖旧“所有冲突均拒绝”的储物袋拖入规则，存档格式、战斗整理规则不变。

最新阵型记录排版（2026-09-18）：移除阵型容器内独立的纵排“阵型”标题；阵型卡片滚动区在718×174面板中垂直居中，92×138卡片到上下边等距。新增入口显示“阵型记录”，文字使用与“行囊”标题相同的F9F8C8。卡片图标区保持正方形、无额外边框，尺寸调整为78×78，为下方名称留出两行高度。阵型名称字号为15px，任意字符换行，最多两行；内容继续溢出时在第二行末尾显示“…”省略。已保存卡片的3秒修改计时从指针首次移动进入卡片范围立即开始，只依据指针坐标而不依赖系统tooltip或当前GUI悬停控件；真正移出、失焦、拖动及打开模态框仍重置。多卡横向滚动、裁切命中、持久化与恢复规则不变。此段覆盖下方纵排“阵型”标题、82px图标及16px单行名称说明。

最新阵型可移植规则（2026-09-18）：阵型只记录道具身份和格位，不记录阵盘ID、尺寸或皮肤。按从上到下、每行从左到右保存；在当前阵盘上以左上角为原点恢复原行列位置，换大阵盘不居中、不拉伸、不压紧空位。已不再拥有的道具跳过，其原格留空，不使用同类型其他实例顶替；已经装备在行囊中的道具仍属拥有。缺失道具的原记录继续保留，只有明确更新布局才覆盖。旧阵型中的board_id自动忽略并在规范化后移除。小阵盘无法完整容纳现有道具或道具发生重叠时，恢复失败并保持当前布局与保存记录；仅保存记录不受当前盘容量限制。此规则覆盖先前阵型绑定阵盘及缺失物品整单拒绝的说明。

最新阵型存档（2026-09-19）：标题改为纵排“阵／型”，26px普通楷体、F9F8C8；移除全部预设，初始只显示新增卡片。阵型保存当前行囊内的道具身份与格位，允许多份和空布局；卡片点击恢复布局，超出容器的卡片用滚轮横向浏览。创建／修改可从攻击、防御、平衡、修炼、炼丹、炼器六个图标选择并填写1至12字名称。已保存卡片悬停3秒显示右上三横线修改按钮，移出／失焦／模态遮挡／拖动重置；编辑默认仅改名称和图标，可明确勾选以当前行囊覆盖保存布局。每次新增、修改、恢复均立即落盘，重启保留；不增加战斗加成。此段覆盖下方会话内四槽／三个预设及五图标说明。

最新阵型配置界面（2026-09-18）：下方“阵型配置”改为四字纵排；修炼阵、炼丹阵、攻击阵和一个自定义槽共四张92×138设计px的2∶3卡片。卡片上部使用82×82正方形图标区，不额外绘制图标边框，下部显示名称。点击“新增阵型”打开760×430居中模态框，可从五张zhen-xiulian／liandan／zhandou／fangyu／pingheng.webp中选择图标，填写最多12字且非空的名称，然后取消或新建。五张图标均生成mipmap并用LINEAR_WITH_MIPMAPS显示。新建后替换第四槽；Esc或I优先关闭弹窗。当前自定义阵型仅保留在本次运行会话，不进入inventory_loadout.json，不切换或影响战斗阵型。

最新顶部中心对齐（2026-09-18）：以行囊标题行中心（面板顶部下40设计px）为基准，返回游历图片／按钮整体上移10px、保留56px布局槽和原缩放；储物袋顶部内边距改22px，标题／搜索及下方内容下移8px，与行囊标题中心对齐。外框和行囊位置不变。

最新标题文字颜色（2026-09-18）：行囊属性加成、行囊、阵型配置、储物袋四处标题统一F9F8C8、100%不透明度，保留26px普通字重楷体；竖标记及其他文字颜色不变，覆盖下方标题FFF0BD的历史设置。

最新物品名称颜色（2026-09-18）：储物袋卡片ItemName使用FFD700、100%不透明度，覆盖旧EEE1BD；原字体、字号、布局及标签颜色不变。

最新间距值（2026-09-18）：统一卡片间距从4改15设计px，横向与行间距均15px，左右内边距16px含容器金边约1px，净留白约15px。每行6件、卡框光学边界校准和工具栏对齐保持，卡片按剩余宽度等比缩小。覆盖下方4px／5px的历史数值。

最新可见间距校正（2026-09-18）：level-fanpin的alpha有效区包含暗色外沿，不能代表亮卡框边界；卡片横向改按源图金属直边x27至517适配。相邻卡槽仍4px，物品区左右内边距改5px，补偿容器金线占据的约1px，按可见边框内缘比较净留白。保留六列和其他排版，覆盖下方“alpha有效区＋左右4px即可等距”的说明。

最新六列排版（2026-09-18）：储物袋每行6件，卡槽横纵间距4设计px，物品区距容器左右边界均4px；标题／筛选栏／底部计数仍保留14px横向内边距。卡片按可见卡框有效区域适配可用宽度，移除0.98倍额外留白，内容整体等比缩放，原素材及内部布局保持。覆盖下方五列／横6纵8和叠加物品区内边距的历史说明。

最新四容器边框与抗锯齿（2026-09-18）：行囊圆角恢复18设计px；inv-zhenpan在此前变换后围绕当前图片中心再等比放大3%，alpha0.25保持。属性加成／行囊／阵型配置／储物袋均用Head的d9bb72和1.2设计px边框，前三者圆角18，储物袋原14不变。边框采用map_panel_border.gdshader的圆角距离场与屏幕像素覆盖率，替代实际仍有硬台阶的Line2D；保留2px透明绘制余量。底图用map_inventory_panel_art.gdshader平滑裁切，Atlas UV映射到真实容器坐标，超出边界透明；左栏顶部64px渐隐保持。覆盖下方32px圆角、硬子节点裁切及Line2D绘边的历史说明。

最新外框修订（2026-09-18）：恢复行囊BoardPanel的程序金边，与Head分割线共用d9bb72颜色和1.2设计px线宽，抗锯齿闭合轮廓沿32px圆角绘制于底图上方。容器底色、内边距、图片变换／25%不透明度、圆角裁切保持；覆盖历史“仅使用图片边框”的决定。

最新左栏接缝修复（2026-09-18）：inventory-sidepic继续按宽等比缩放、贴底，顶部约76.58px余量保留容器底色。图片顶部64设计px以smoothstep从透明渐入原25%不透明度，消除图片硬边造成的横向亮度断层；不移动或拉伸图片，不改源素材、外框或返回按钮。

最新微调（2026-09-18）：inv-zhenpan在既有变换后只将底边向下延伸3设计px，上／左／右边不变，超出仍裁切。inv-zhenpan与inventory-sidepic均改为25%不透明度（alpha0.25），覆盖历史75%设置。左栏保留1px淡金外轮廓，返回图片自身也带金边；容器底层边框透明，没有重复两条可见容器边框。

最新底图纵向微调（2026-09-18）：inv-zhenpan在当前尺寸上仅纵向拉伸1%，垂直中心保持，横向尺寸与位置不变。718基准尺寸为746.9354×754.404754、位置(-16.1654,-18.202377)，使用STRETCH_SCALE；75%不透明度和32px圆角裁切保持，覆盖历史KEEP_ASPECT_CENTERED设置。

最新全游戏比例约束（2026-09-18）：项目统一使用1920×1080逻辑画布、canvas_items＋keep。窗口放大缩小及非16∶9显示器均保持16∶9游戏区域并等比缩放，余量为引擎黑边；行囊内部不再因实际窗口宽高比重新分配列宽／位置。下方宽屏贴边与高度余量规则保留为历史，正常运行不再触发。

最新背景透明度（2026-09-18）：inv-zhenpan及inventory-sidepic图片均使用self_modulate.a=0.75（75%不透明度），覆盖下方inv-zhenpan的50%历史设置；只影响图片，不改变前景或容器底色，缩放、位置及裁切保持。

最新缩放纠正（2026-09-18）：取消横向加宽3%。从原居中1.03倍且右移2px的状态，固定右边缘及垂直中心，整体等比放大1%（宽高同乘1.01）；718基准下图片746.9354×746.9354、位置(-16.1654,-14.4677)。恢复KEEP_ASPECT_CENTERED，32px圆角裁切和50%不透明度保持，覆盖下文单侧横向拉伸描述。

最新裁切与位置（2026-09-18）：BoardPanel以原底色圆角形状裁切全部子内容（CLIP_CHILDREN_AND_DRAW），容器外及圆角外的inv-zhenpan不可见。容器圆角从18改32设计px，按素材金边约46源像素折算；原容器尺寸及内边距保持。底图在此前居中放大3%基础上右移2设计px，再固定移动后的右边向左加宽当前宽度的3%，高度不变，因此使用横向拉伸；718基准下图片761.7262×739.54、位置(-30.9562,-10.77)，alpha0.5。覆盖下文的旧居中位置；布袋与前景内容不移动。

最新统一轮廓（2026-09-18）：当前六件物品均配置art_outline_px=1，覆盖此前仅猎弓描边及2px／3px版本。地图储物袋卡片、行囊内及地图拖拽预览共用MapItemArtwork；品质光晕、深灰黑轮廓、原物品图按由后到前的本地层级绘制，不使用会落到静态容器背景后的负层级。轮廓以半径1、16方向的亚像素偏移及线性mipmap采样合成，颜色为比纯黑稍淡的#151515。六张原图、图片尺寸／位置、占格、数值及存档不变；art_outline_px如提供必须为正整数。

最新透明度（2026-09-18）：inv-zhenpan独立底图层self_modulate.a=0.5，图片及其自带边框均以50%不透明度显示；保留原容器底色、居中放大3%以及标题／布袋／物品的原不透明度。覆盖下方历史原不透明度说明。

后续澄清（2026-09-18）：原BoardPanel容器及深蓝0d1c26底色保留，只去掉程序边框线。inv-zhenpan为独立底图层，居中等比放大3%至容器的103%（718px基准为739.54px），位于原底色之上、标题／布袋之下；22px内边距和内容布局保持。覆盖下文用StyleBoxTexture替代容器底色的旧做法。

最新行囊容器底图（2026-09-18）：718×718外层BoardPanel使用inv-zhenpan.webp的完整山水背景及图片自带蓝金边框，StyleBoxTexture替换旧平涂底色／程序细灰边。运行时围绕素材有效区域中心取正方形Atlas，完整包含图框、等比显示并保留透明圆角；不改源图，不额外降低不透明度。22px内容内边距、标题／容量和内部布袋图片及格位映射保持，阵型面板不更换。

最新中间列宽度（2026-09-18）：行囊指外层BoardPanel，不是内部zhenpan-bag图片。固定1920×1080设计画布中，主体高910px，阵型配置固定高174px，中间垂直间距18px，因此行囊外层为718×718px正方形。行囊高度及左边x=252不变，仅右边较旧780px列向左收窄62px；下方阵型配置与行囊同为718px宽。释放的62px全部由右侧储物袋从左侧吸收，其左边为x=990、右边仍为x=1896，行囊与储物袋之间保留20px。内部阵盘图片继续保持原图比例居中，不拉伸；不同窗口下整套设计画布继续统一等比缩放。

最新卡片品质光晕（2026-09-18）：储物袋物品图背后增加更易见的薄圆形色层，按quality查询MapItemArtwork.QUALITY_GLOW_COLORS；当前六件下品均为淡银灰蓝b4c6dc，未知品质暂用同一中性色，不按强化等级上色。直径136卡片设计px，位于物品图中心，alpha最高0.24、半径0.18后平滑渐变、falloff_power1.4，圆周完全透明，不描边；原素材与卡框保持。仅储物袋卡片开启，地图行囊和拖动预览保留原光晕参数，后续正式增加其他品质时扩展配色表。

最新左栏修订（2026-09-18）：标题改为单行“行囊属性加成”，配置bag_caption直接提供整句，不再显示阵盘名称前缀；采用与三处标题相同的26px普通字重楷体、亮金fff0bd，替换旧24px加粗仿宋。题字保留17px楷体，同样改fff0bd。返回游历在上一轮尺寸上再等比放大20%，累计为最初144%，原中心及56px布局槽保持，图片完整位于左栏内；栏宽、原图、按钮行为保持。此段覆盖下方较早的左栏名称／字体与返回图片比例记录。

后续标题与容量修订（2026-09-18）：三处标题取消FontVariation额外加粗，保留26px楷体、亮金fff0bd及竖标记，覆盖下文加粗1.2的历史决定。26px为1920×1080基准字号，汉字排版字宽约26px，随界面等比缩放。行囊右上角不再显示3×3，改为“容量”及亮金“已占用格数/总格数”；两者统一采用Microsoft YaHei／Noto Sans CJK SC无衬线字体，“容量”16px、提亮为c5d0d2，数字17px、fff0bd，间隔8px。占用数直接读取InventoryState.occupied_cells()，总数来自实际board.grid_size乘积；打开／恢复配置及装备变化同步更新，不按物品件数统计。例如图示三件两格物品加一件单格头盔为7/9。

最新排版修订（2026-09-18）：以1920×1080为16:9基准，左栏距左边、储物袋距右边和主体距底边均24px。宽屏下保持内部等比缩放及中间行囊位置，将左栏移至24px缩放边距、储物袋向右扩展，避免旧的两侧居中余量；左栏插画和按钮仍保持固定相对位置。16:10等较高窗口继续保留纵向居中余量。返回游历图片围绕原中心等比放大20%，保留56px布局槽、点击与按压反馈。

行囊、阵型配置、储物袋统一为26px楷体标题，左侧3×23px竖线、10px间距，文字和标记改亮金fff0bd，字体用FontVariation加粗1.2；移除行囊标题下横线。储物袋改每行5件，横向槽间距6px、纵向8px，卡片按可视区域宽度等比缩放、占槽宽98%；第6件换行。滚动容器不再以内容最小宽度撑大，避免第5列越界。以上覆盖本节后面的旧四列及全画布水平居中说明；不改源素材、装备规则或存档。

2026-09-18同名物品素材更新：重新导入weapon-qsdj／lg／dbtc和armor-cby／jtk／myd。地图行囊、双方战斗阵盘内的物品图片以占格中心为锚等比缩小10%，拖拽预览同步；共用ItemDragPreview.BOARD_ICON_SCALE=0.9。占格、命中范围、放置提示及CD／数量标记不变，储物袋卡片尺寸不变。源图不改写，同名素材更新后停止旧运行并重新启动预览。

最新左栏与缩放修订（2026-09-18）：宗门文字清空，保留40px透明位置；以后填写faction_caption即可显示。标题从当前BoardLayout.display_name（board JSON的name）读取，目前base.board.bag名称为普通行囊，下行“属性加成”；24px加粗仿宋，名称与说明分行防止窄栏拆词。题字改为17px金色楷体，横线下间距6px。1px淡金圆角边在插画上层绘制，防止图片遮盖边线，源素材不改。

储物袋内部固定1920×1080设计画布、主体1874×910，在Head以下整体等比适配并居中。当前左右屏幕边距为23px、底边距24px。宽高比不同时多余区域保留底色，内部相对位置不变；原来屏宽缩放与容器高度自适应混用，造成贴底插画／贴顶按钮相对移动，现已修正。地图和Head原有比例规则保持。

左栏美术（2026-09-18）：inventory-sidepic.webp作为背景，按宽等比缩放并贴底，上方余量保留原底色，不拉伸填高；遵循容器圆角裁切且不截获鼠标。返回游历使用button-return.webp图片按钮，保留点击回地图及按压反馈，图片已含文字所以不重复叠字。

名称调整（2026-09-18）：地图中间3×3区域标题现在为“行囊”，独立战斗沿用原术语；左栏底部两行“大道藏于万物／修行自有机缘”已删除。其余占位文字及布局不变。

卡片细调（2026-09-18）：列数保持4，每张卡片在列槽内等比缩小8%。名称使用加粗宋体，名称与分类标签间距2px，组合略下移；物品图片中心在卡片设计坐标下移10px。MapItemArtwork复用完整物品图及淡灰蓝径向透明光晕，不修改原图。左侧信息外框在1080p为208×910px（宽高约1∶4.38）。行囊内和拖动预览复用同一完整图片及光晕，拖动图按目标占格大小等比显示并居中跟随鼠标。

2026-09-18紧凑排版：储物袋顶部严格按参考的“标题＋搜索／收藏页签／类别／排序”四层组织。标题26px，普通控件14–15px，页签30px高、分类28px高、排序30px高；间隔6–8px，按钮取消描边，常态页签为文字、选中暖色胶囊、类别选中青蓝底。顶部占用从约318px缩至约170px，物品区域随之上移。品质、等级、获得时间在同一行，重置筛选保留在行末。缩小范围为右侧储物袋工具栏，原Head、左栏和阵盘区域保持。

- 地图按I打开；再次按I、Esc或“返回游历”关闭。搜索框输入中的字母I按文本处理，Esc始终可退出。
- 打开期间暂停地图行走，阻止底层节点点击、拖动与缩放；关闭恢复原路线、位置和镜头。对话或事件打开期间不能打开此界面。
- 顶部复用原MapStatusHeader及同一个MapPlayerStatus，不复制截图中的角色、货币与修为数据。地图专用青石河湾挂牌在此界面暂隐藏，关闭后恢复。
- Head下方分三列、四块：左侧说明栏；中间上方阵盘／下方同宽的横向阵型配置；右侧储物袋。
- 左侧保留“返回游历”图片按钮、空宗门位置、当前行囊名称与属性加成标题、留空特性区域，不重复气血等资源。背景和按钮美术见本节最新说明。
- 中间行囊沿用主角定义关联的阵盘背景（目前zhenpan-bag.webp），3×3和原图比例，显示当前装备格位，无开始修炼按钮。
- 阵型区域显示玩家保存的布局及新增入口，支持持久化、点击恢复、悬停3秒修改名称／图标，以及显式更新布局；不提供额外战斗加成。
- 右侧命名储物袋，六件初始物品来自data/maps/inventory_items.json，提供搜索、收藏／常用／灵材、物品类别、品质筛选，以及强化等级／获得时间升降序。

## 数据与后续接入

### 行囊交互、持久化与战斗接入（2026-09-18，已实现）

用户确认地图储物袋与行囊双向拖放、盘内移动、右键随机放入／取回、最近占格及冲突拒绝；地图整理无冷却。新增独立纯MapLoadoutState复用InventoryState／SharedStorage，目录只投影未装备物品。UI关闭（I／Esc／返回地图）时保存版本化本地格位快照，退出游戏时也保存最近成功放置；取消中的拖动不改变数据。重新启动恢复上次配置，空行囊也是有效配置。快照用稳定单位ID、物品ID、格位和阵盘ID，验证后整体载入，写入使用临时文件与备份避免半份文件。

正式F6战斗入口读取同一快照，按保存格位替换主角旧测试装备，剩余物品进入战斗储物袋，不混入旧测试库存。地图六件定义经适配器注册为ItemData，不复制数值。武器保留伤害／CD／耗体，防具提供护甲上限并自动轮转补甲；实际伤害消耗护甲，不再固定减伤。敌方、自动战斗和战中整理规则保留，护元按本节新规则运行。此次读取为开战装备快照，不把战斗中的临时调整回写到地图；没有地图遇敌跳转、战后奖励、资源持续或强化成长的额外变更。原战斗回归用显式旧测试模式，测试使用隔离路径，不读写玩家实际保存。

操作：左键按下储物袋物品开始拖动，行囊提示距离最近的完整占格；绿色可放、红色冲突，释放按提示格位放置，不自动跳到其他空位。盘内可挪动，拖回右侧储物袋可取回。右键储物袋物品随机放到合法空位，右键盘内物品取回。越界、冲突、关闭界面或窗口失焦取消拖动时保持原数据，物品仅在成功转移后从原位置消失。地图整理没有入场或轮转CD。

保存位置为`user://inventory_loadout.json`，Windows默认在`%APPDATA%/Godot/app_userdata/Path of Immortals/`。MapLoadoutStore负责版本1快照读写，保存采用.tmp与.bak轮换；没有正式文件时可恢复.bak，有损坏文件时报告错误并保留原文件，不静默重置。没有任何存档时为六件物品全部在储物袋、行囊为空。I／Esc／返回游历会先取消未提交拖动再保存；保存失败提示并保持界面打开。正常退出也保存已成功放置的配置；编辑器强制停止或进程崩溃仅保证已关闭界面时完成的保存。

当前存档包括六件已配置物品的当前归属和格位，以及可选formations数组；每条阵型包含稳定id、name、icon和layout（仅placements，各项为instance_id／item_id／cell）。旧version1文件缺少formations时视为空库；旧阵型layout的version1／board_id可读取，规范化后仅保留placements。阵型读取校验结构、非空唯一物品标识和非负整数格位，不要求道具仍拥有或位置能放入当前盘；应用时跳过缺失道具，按当前盘检验其余道具，成功后整体替换。顶层当前装备快照仍保留version／board_id和原严格验证，阵型不负责切换阵盘。保存失败回滚本次改动，已有文件保留。剩余储物袋从固定初始拥有物品重建，获取时间取静态定义；筛选条件、地图事件／位置、资源、强化成长尚未持久化。未来增加掉落、删除或强化时须扩展存档版本，不可仅靠增加展示记录模拟持久化；本轮已支持阵型中的失去道具引用，但不新增出售／删除玩法或所有权存档。战斗临时变化不回写。

`data/ui/map_inventory.json`保存界面占位文字与类别名称。`MapInventoryCatalog`为独立纯展示／筛选目录；权威归属由MapLoadoutState复用InventoryState／SharedStorage管理，再把未装备物品投影到目录。定义从data/maps/inventory_items.json初始化，战斗通过同一适配器注册到ContentRegistry。

后续用`replace_entries`接入展示记录：稳定实例`id`、`name`、`category`、`quality`、非负整数`acquired_at`（获取顺序／时间）、正整数`quantity`，可选`favorite`、`common`布尔标签及`icon`资源路径。获得时间按明确传入值排序，同值用稳定ID确定顺序，不用UI刷新时刻冒充获取时间。筛选条件交集生效，搜索匹配名称，品质选项根据目录内容生成，关闭后当前条件保留。这是展示接入口，不是存档格式或正式品质体系。

数据先整体验证再替换，拒绝重复ID及错误字段；原数据不因失败被清空。UI呈现目录记录、提交筛选条件和装备命令，不从显示位置推导权威存档。新增card_frame、占格行／列、攻击类型、基础伤害、cooldown秒及基础耗体；完整数值悬浮显示，卡片采用品质底框、类别标签、右上占格和数量。武器图片槽使用以上新素材。掉落获取和完整玩家存档仍未实现。

等级排序使用enhancement_level（非负整数强化等级），与品质／修仙境界分开。用户确认三件武器目前均0级；旧展示记录缺省按0级处理，说明中展示“强化等级：0级”。点“等级”激活强化等级排序，再点切换高到低／低到高；同等级按当前获得时间方向，再按稳定ID排序。点“获得时间”切回时间主排序，再点切换新到旧／旧到新；两种排序互斥，分别保留本次打开期间方向。重置恢复默认时间新到旧与等级降序。本轮仅加入等级数据和排序，未实现强化操作、材料消耗或强化属性加成。

## 验证

`tests/map_formation_smoke.gd`使用独立artifacts存档，覆盖多阵型持久化、旧文件兼容、布局深拷贝、名称／图标编辑、显式更新、物品归属守恒、非法记录原子拒绝、即时保存／场景重启／点击恢复、失败回滚及横向溢出；另覆盖道具移除后留空、不替换同类实例、缺失引用跨重启保留、物品定义缺失、旧阵盘绑定迁移、3×3→4×4左上对齐、小盘不足与重叠原子拒绝。储物袋渲染检查覆盖3秒悬停与修改按钮点击。测试不读写正式玩家存档。

覆盖真实键盘开关、搜索输入、返回按钮、模态锁定、移动续行、Head状态保持、事件互斥、多尺寸布局，以及带临时测试记录的组合筛选／排序。测试记录仅在测试脚本内，不进入正常游戏。更新PROJECT_PROGRESS记录实际检查结果。

`tests/map_loadout_smoke.gd`补充实际鼠标拖放／右键、占格冲突、取消与缩放预览、非法快照原子拒绝、空盘保存、磁盘恢复和真实战斗装备／伤害／耗体检查。所有测试使用隔离文件或禁用保存，不能读写玩家正式配置。手工验收：F5→I放入物品→关闭界面→F8／F5后I查看原格位，再打开scenes/main/main.tscn按F6核对装备。
